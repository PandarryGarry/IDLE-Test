# UI/UX-аудит окон — фактура и предложение единой концепции Aethelia

> **Статус: ПРОЕКТ НА УТВЕРЖДЕНИЕ владельца (2026-09-10, Сессия 31).**
> Код по этому документу не пишем, пока владелец не ответит на вопросы §7.
> Следующий этап после утверждения — миграция по плану §6, один шаг за раз.

**Область (по слову владельца):** навык «Сбор», интерфейс персонажа
(`/hero`: Тело/Экип/Нити/Путь), админ-панель, инвентарь, мелкие окна
(модалки/попапы/тосты). Вне области: бой, удалённые навыки, онбординг-арты,
лендинг/Dashboard (дожидаются своей очереди).

**Метод:** скан кода (hex-цвета, var(--…), примитивы, классы) +
живые QA-снимки на локальном моке (мобилка 390×844, десктоп 1280×820,
`node scripts/qa/hero.mjs` + разовый прогон окон; кадры `/tmp/aethelia-qa-out/`).

---

## 1. Фактура: сколько «стилей» живёт в игре одновременно

### 1.1. Пять слоёв-диалектов цвета

| # | Слой | Что это | Статус |
|---|---|---|---|
| A | `src/index.css` `:root` | ~56 CSS-переменных тёмной таверны; используются `var(--…)` в 38 файлах | **де-факто живая палитра** |
| B | `src/styles/tokens.ts` | Объявляет себя «единым источником правды», описывает **СВЕТЛУЮ** тему «Warm Light Fantasy» (пергамент `#f5ede0`, тёмный текст) | **мёртв: 0 импортов**, шапка врёт |
| C | `shared/ui/gameUI.tsx` | Внутренняя карта `C = {…}` — **57 hex-копий** таверны; шапка файла говорит «не хардкодь» и хардкодит сама | жив, но рифтуется |
| D | `shared/ui/kit/*` | Tailwind-дефолты (`amber-500`, `emerald-600`, `slate-950`) + чужеродные hex (`#1a2538` синий в `IconFrame slot`) | жив, инородные цвета |
| E | Инлайн в страницах | `ForagingPage` — **71 hex** (все пилюли/чипы руками), `Sidebar/MobileNav/TopNavBar/Dashboard` и пр. | главный рассадник |

Итого по коду: **276 hex-вхождений в 19 файлах** вне `index.css`.

### 1.2. Геометрия и ритм — не зафиксированы

- **border-radius в деле:** 4, 6, 8, 10, 12, 18, 20, 9999px + tailwind `lg/xl/2xl` + gameUI 8/10/14/16 — ~12 значений без шкалы.
- **Тени:** `gameUI` держит свои (`0 4px 0 #3d1e08…`), CSS-vars — свои (`--shadow-card/slot/gold`), kit — tailwind `shadow-*`.
- **Табы/вкладки:** как минимум 4 реализации: hero-хаб (тёмные пилюли, активная янтарная), админ-рейл (золотые outline-карточки с иконками), админ-сабнав (заполненные строки списком), админ-подтабы (жёлтая кнопка mustard vs outline) + «Активные/Неактивные» в «Нитях».
- **Кнопки-primary:** 4 насыщенности янтаря/жёлтого: приглушённый янтарь хедера, янтарный градиент «Начать сбор», жёлтый градиент «Сохранить», ярко-оранжевый градиент «Надеть предмет».

### 1.3. Редкость предметов — три шкалы

1. `tokens.ts rarity` — мёртвая (вместе с файлом).
2. `ItemCell` точки: `#4ade80/#60a5fa/#c084fc/#fbbf24/#f87171` (tailwind-спектр).
3. `RarityBadge` — своя конфигурация цветов; попап предмета — четвёртый вариант (ядовито-зелёная пилюля «НЕОБЫЧНЫЙ»).

---

## 2. Главное наблюдение: концепция уже существует — её просто никто не провозгласил

По снимкам видно, что лучшие экраны (хаб героя, «Нити», «Путь», админка)
самопроизвольно собрались в **одну и ту же трёхслойную схему**. Предлагаю
провозгласить её законом, а не изобретать новую:

| Слой | Роль | Живое значение сейчас |
|---|---|---|
| **0 «Таверна»** | Хром: топ-бар, сайдбар, нижняя навигация, оверлеи | почти чёрный шоколад (`#2a1508`-семейство) |
| **1 «Лист»** | Рабочая область окна/страницы — «пергамент» под контентом | `--bg-page: #c4a472` |
| **2 «Карта»** | Контент-блоки на листе (карточки, панели, заголовки секций) | тёмное дерево, градиенты `#845d36→#6e4727` |
| **3 «Ячейка»** | Слоты предметов/иконок | светлый картон с рамкой; пустые — `--slot-wood` |

**Диагноз:** экраны «не сливаются» не потому, что схемы нет, а потому что
её нарушают в разных местах по-разному:

- **Админка** на десктопе кладёт сетку предметов на *тёмный* лист, а на мобилке — на *пергаментный* (несовпадение брейкпоинтов!).
- **«Экип»** в одном окне смешивает два поколения ячеек: старые тёмные
  `.hero-sq-slot` (серые силуэты) и новые светлые `ItemCell` (с Т-бейджами) —
  колонка слева и колонки справа выглядят как из разных игр.
- **Инвентарь**: заполненные ячейки светлые, пустые тёмные → «шахматка» из двух состояний; TierBadge висит за краями ячейки, задевая соседнюю.
- **Модалки** прыгают: столп — средне-коричневый, предмет — почти чёрный.
- **Хром**: активная вкладка нижней навигации жёстко цветокодирована в
  `components/MobileNav.tsx` (Главная/Герой/Настройки — `amber-400`,
  Бой — `red-400`, Профессии — `emerald-400`, Инвентарь — `sky-400`).
  Фича-цветокод или шум — вопрос владельцу (§7).

---

## 3. Аудит по окнам (что видно на снимках + в коде)

### 3.1. Интерфейс персонажа `/hero`

**Хорошо:** шапка героя (тёмная карта, золотые кольца аватара/уровня),
табы, доска тела на пергаменте (цветные активные узлы / сепия запертых —
читаемо), «Нити» и «Путь» — тёмные карточки на листе, ровно.

**Проблемы:**
1. «Экип»: два поколения ячеек в одном окне (см. §2). Сэт-бар и инфо-кнопки —
   свои виджеты (`hero-gear2*`, 232 класса `hero-*` суммарно в CSS).
2. «Бонусы снаряжения» — тёмная карта внутри листа: по схеме, но рядом со
   светлыми ячейками образует резкий маятник «светло-темно».
3. **Модалка столпа** (`02-mobile-pillar-modal`): подписи строк
   («Итого/Раса/Вложил…») почти не читаются — тёмный текст на средне-коричневом
   фоне. Кандидат-баг контраста, чинить с токенами (шаг 6 §6).
4. Модалки хаба — `hero-hub-modal*` классы поверх вёрстки: вторая «оконная»
   реализация рядом с GModal.

### 3.2. «Сбор» (`/foraging`)

**Хорошо:** карточки зон цельные: тёмные на листе, запертые приглушены,
структура «зона → иконка → требование → статы → CTA» едина.

**Проблемы:**
1. Файл-рекордсмен: **71 hex** инлайн (процент встречи, lock-пилюля
   «12 ур.» в грязно-красном, info-кружки, стат-строки).
2. Шапка профессии — средне-коричневая карта, не совпадает с тёмной шапкой
   героя (конкуренция «двух хедеров» в приложении).
3. Место под иконку зоны — пустой тёмный кадр: сейчас контент не привязан
   (зоны/мобы есть в данных, арт-подложки нет — или не грузится в моке).

### 3.3. Инвентарь (`/inventory`)

**Хорошо:** шапка (кошелёк, слоты 11/40, «+ Слоты») читается; сетка 5-колоночная
ровная; ItemCell уже единый рендер заполненных ячеек.

**Проблемы:**
1. «Шахматка» светлых/тёмных ячеек (заполненные vs пустые) — визуальный шум.
2. TierBadge за краями ячейки — намеренный стиль или дефект? Решить и
   зафиксировать (§7).
3. Попап предмета — сам по себе диалект: почти чёрная модалка, ядовито-зелёная
   пилюля редкости, цветные семантические тайлы (золото/рубин/изумруд
   с разными бордерами), оранжевая CTA.
4. Контентный разрыв: legacy-предметы старых сейвов с **английскими** именами
   («Bronze Sword») среди русского каталога.

### 3.4. Админ-панель (`/admin`)

**Хорошо:** рабочий инструмент: rail + subnav + сцена; шапка «КОМУ ВЫДАЕМ»
с целью всегда на виду; сумка «Уники» и фасовка «слот → тир → семья».

**Проблемы:**
1. Три системы вкладок сразу (rail-карточки, subnav-строки, подтабы-кнопки) +
   hero-хаб — четвёртая. Нужен один `GTabs`.
2. Лист: мобилка — пергамент, десктоп — тёмный (несогласованность слоя 1).
3. Кнопки «Сохранить»/«Выдать» — жёлтый градиент максимальной насыщенности;
   в игровой части такой насыщенности нет → админка ощущается «другой игрой».
4. Тост «Прогресс игры сохранён» появляется поверх кнопки «Сохранить»
   в шапке (коллизия слоёв на десктопе).

### 3.5. Мелкие окна

- `GModal` принят в 8 местах — хорошая база (оверлей+карта+крест).
- `UniversalInfoModal` — отдельная fixed-реализация рядом с GModal.
- Тост — коллизия с шапкой (см. 3.4.4); bottom-nav «активный цвет» пляшет
  по разделам; радиусы — хаос (§1.2).

---

## 4. Предлагаемый канон «Aethelia UI» (для утверждения)

### 4.1. Один источник правды

**`src/index.css :root` = единственная палитра.** `tokens.ts` перестаёт быть
«второй правдой»: либо удаляем, либо (предпочтительно) переписываем как
**типизированную карту ролей**, экспортирующую `var(--…)`-строки — тогда
`theme.surfaceCard` в коде и `--surface-2` в CSS не могут разойтись.
Закон «цвета только через токены» начнёт реально соблюдаться.

### 4.2. Ролевая палитра (роли, а не магические названия)

```
// Слои поверхности (§2 — узакониваем существующую схему)
--surface-0   хром (топ-бар, нави, оверлей)      ≈ живой #2a1508
--surface-1   лист/пергамент рабочей области      ≈ живой #c4a472
--surface-2   карта/карточка на листе             ≈ живые градиенты дерева
--surface-3   углубление/вставка внутри карты     ≈ живое тёмное дно

// Линии и текст
--line-default / --line-strong / --line-accent
--ink-primary / --ink-2 / --ink-muted / --ink-inverse (на светлом)

// Акцент (один, тёплый)
--accent / --accent-hi / --accent-soft / --accent-glow   ≈ живой #d4860a-градации

// Семантика (выкормить из живых значений, выкинуть tailwind-дефолты)
--ok (сбор, добыча)        ≈ живой emerald-таверна
--danger (бой, удаление)   ≈ живой ruby-таверна
--info                     ≈ сапфир-таверна (если нужен)

// Редкость — одна шкала (за основу живые точки ItemCell)
--rarity-1..6
```

Все числа берутся из сегодняшних `:root`-значений (переезд без смены вида),
калибровка оттенков — отдельным шагом глазами владельца.

### 4.3. Шкалы

- **Радиусы:** `--r-1:8px` (чеек/пилюли), `--r-2:12px` (ячейки/инпуты),
  `--r-3:16px` (карты), `--r-4:20px` (окна/листы), `--r-full` (плашки-яйца).
  Всё остальное — вне закона. «Сквиркл» ячейки = `--r-2` (его вид уже канон в ItemCell).
- **Тени:** `--shadow-card`, `--shadow-well` (inset ячеек), `--shadow-accent` (свечение).
- **Отступы окон:** лист `16/20`, карта `12`, ячейка-арт `4`.
- **Шрифтовые роли:** display (Cinzel — заголовки окон/числа), sans (Inter — текст),
  mono (JetBrains — цифры/мета). Уже есть в `:root`, остаётся не нарушать.

### 4.4. Примитивы — целевой набор (одно имя на задачу)

| Примитив | Роль | Из чего собираем |
|---|---|---|
| `AWindow` | Любое модальное окно (вариант `sheet` по слою 1) | `GModal` + слияние `UniversalInfoModal`, `hero-hub-modal*` |
| `ASheet` | Лист/страница (слой 1) + заголовок окна | из hero/admin листов |
| `ACard` | Карточка на листе (варианты: обычная/шапка/инфо) | `GPanel`/`GameCard` |
| `ACell` | **Единственная ячейка** предмета во всей игре | `ItemCell` (уже так задумано); `.hero-sq-slot` вычистить из «Экипа» |
| `ATabs` | Вкладки всех уровней (page/sub/rail/mini) | 4 реализации → один компонент с вариантами |
| `AButton` | Кнопка: primary (один янтарь!), secondary, danger, ghost | `GButton`/`GameButton` |
| `ABadge` | Бейдж: tier/УНИК/lock/статус/счётчик | `TierBadge`/`RarityBadge`/`GBadge` |
| `ADot` | Точка редкости (одна шкала §4.2) | из ItemCell |
| `AField` | Инпут/поиск/селект — одна высота/радиус | `GInput` + admin поля |
| `AProgress` | Полоса/круг прогресса | `GProgressBar` |
| `AToast` | Тост — позиция вне шапки | `NotificationToast` |

**`gameUI.tsx` vs `kit/*` — предложение:** объявить **kit атомами** (A-компоненты
выше), а `gameUI` считать *устаревшим*: новые экраны не импортируют его,
существующие переводим шагами. (Альтернатива — наоборот. Вопрос §7.)

---

## 5. Что даст концепцию «одной игры» сразу (дешёвые победы)

1. Один янтарь primary во всех окнах (сейчас 4 разных).
2. Одинаковый лист на мобилке и десктопе в админке.
3. Один поколения-ячейка в «Экипе» (убрать `.hero-sq-slot`).
4. Читаемые подписи в модалке столпа (лейблы по `--ink-*`).
5. Единая шкала редкости (точка = пилюля = бейдж).
6. Тост не перекрывает кнопки.

## 6. План миграции (строго по одному шагу, как заведено)

| # | Шаг | Объём | Риск |
|---|---|---|---|
| 0 | Владелец отвечает на §7 | — | — |
| 1 | Источник правды: `:root` дополняем ролями §4.2 (значения = текущие), `tokens.ts` → карта var() | 2 файла | нулевой (вид не меняется) |
| 2 | `gameUI C` → var() (57 hex) | 1 файл | низкий |
| 3 | `ForagingPage` 71 hex → токены | 1 файл | низкий |
| 4 | «Экип»: `.hero-sq-slot` → `ItemCell` везде | HeroHubPage + CSS | средний (визуальный) |
| 5 | Редкость: шкала → ItemCell/RarityBadge/попап | 3–4 файла | низкий |
| 6 | Окна: `AWindow` + перевод pillar/item/hub-модалок (заодно контраст §3.1.3) | 3–5 файлов | средний |
| 7 | `ATabs`/`AButton` → админка + хаб | 4–6 файлов | средний |
| 8 | Хром: акцент навигации → единый (или токены цветокода — по §7) | 2 файла | низкий |
| 9 | Страж: скрипт «hex вне `index.css`/`tokens` = ошибка» (как `validate:catalog`) + строка-закон в `ARCHITECTURE.md` + обновить `THEME_GUIDE.md` | 1–2 файла | нулевой |

После каждого шага: typecheck + test:pillars + build + QA-снимки того же окна
(сравнение до/после) → владелец смотрит в Replit.

## 7. Вопросы к владельцу (ответ = утверждение канона)

1. **Тема: тёмная или светлая?** Живой код — тёмная таверна; мёртвый
   `tokens.ts` описывает светлую «Warm Light Fantasy». Утверждаем **тёмную**
   (то, во что реально играется сейчас)?
2. **Цветокод разделов в нижней навигации** (Герой — янтарь, Профессии —
   изумруд, Инвентарь — циан): оставить как фичу (тогда вносим в токены
   как `--nav-активность-*`) или единый янтарь?
3. **TierBadge за краями ячейки** (выпирает на соседнюю): фирменный стиль —
   оставляем (зафиксируем), или убираем внутрь ячейки?
4. **Каталог примитивов:** kit = атомы-канон, `gameUI` — deprecated
   (предложение §4.4). Согласны? Или наоборот?
5. **Legacy-предметы с английскими именами** (живут в старых сейвах):
   оставляем как есть до миграции данных, или переименовать/пометить «старьё»?
6. **Пустые ячейки инвентаря:** оставить контраст «заполненные светлые /
   пустые тёмные» (сейчас шумит), или пустые делать такими же светлыми,
   но приглушёнными/пунктирными (спокойнее)?

---

## Приложение А. Снимки аудита (воспроизвести)

```bash
cd artifacts/my-app && corepack pnpm dev --host 0.0.0.0 --port 3000
node scripts/qa/hero.mjs seed && node scripts/qa/hero.mjs   # герой: все вкладки
# кадры: /tmp/aethelia-qa-out/hero (герой), /tmp/aethelia-qa-out/uxaudit (сбор/инвентарь/админка)
```

## Приложение Б. Цифры скана (HEAD на 2026-09-10)

- hex вне `index.css`: **276 в 19 файлах**; рекорд: `ForagingPage.tsx` 71,
  `gameUI.tsx` 57, `Sidebar/MobileNav/TopNavBar` ~40 суммарно.
- var(--…): 38 файлов; топ-переменные: `--text-muted` (86), `--app-font-mono` (83),
  `--text-primary` (60), `--border-default` (43).
- CSS-классы `index.css` (5552 строки): `hero-*` 232, `admin-*` 73,
  `character-*` 68, `story-*` 60, `auth-*` 55, `g-*` 35, `item-*` 22…
- Радиусы в деле: ~12 значений; primary-кнопки: 4 насыщенности янтаря.
- Примитивы: `gameUI.tsx` 14 компонентов (12 импортёров), `kit/*` 7 (11 импортёров),
  модалок-реализаций: 2 (`GModal` × 8 файлов + `UniversalInfoModal`).
