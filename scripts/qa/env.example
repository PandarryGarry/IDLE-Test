# Скопируй в scripts/qa/.env.local (файл в gitignore) или экспортируй в shell.
# Секреты в чат и в git НЕ класть.

QA_BASE_URL=http://127.0.0.1:3000
QA_BROWSER_DIR=/home/user/ui_shot

# Тестовый аккаунт Supabase (отдельный, не боевой).
QA_EMAIL=
QA_PASSWORD=
