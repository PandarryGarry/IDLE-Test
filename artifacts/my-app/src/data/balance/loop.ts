/**
 * Темп движения игры: циклы действий, автосейв, облачный слепок.
 * Всё, что раньше было «числом прямо в сторе», — здесь.
 */

/** Пол цикла «Сбора», мс, если зона не задала свой интервал. */
export const ACTION_DEFAULT_INTERVAL_MS = 3000;
/** Запас при неизвестной зоне (например, сейв ссылается на удалённую зону). */
export const ACTION_UNKNOWN_ZONE_INTERVAL_MS = 4000;
/** Технический минимум цикла: быстрее — UI и тикер не успевают. */
export const ACTION_MIN_INTERVAL_MS = 100;

/** Автосейв в localStorage по умолчанию, сек (настройка его перебивает). */
export const AUTOSAVE_INTERVAL_S_DEFAULT = 30;
/** Ниже этого ставить автосейв бессмысленно: чаще писать — только тормоза. */
export const AUTOSAVE_INTERVAL_S_MIN = 10;

/** Как часто стартует попытка облачного слепка, мс. */
export const CLOUD_CHECK_INTERVAL_MS = 30_000;
/** Собственно облачный слепок — не чаще, чем раз в 3 минуты (+ вынужденные). */
export const CLOUD_PUSH_INTERVAL_MS = 180_000;
