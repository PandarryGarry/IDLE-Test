# Следующий чат — точка продолжения Aethelia

> **Главный принцип владельца:** не торопимся. Одна задача за раз.
> Если неясно — спрашиваем. Превью Arena не чинить. Секреты в чат не просить.
> Мерж в `main` — только по слову **«мержи»** (в этом чате уже смёржено).

## Где работа

| Куда | Что |
|---|---|
| **`main`** | Этапы 1–5B + раунды 5–6 в коде: столпы, хаб, Пульт, Путь, XP 1→100, `ruleRu`, вывеска греет первый кадр. Одна профессия — «Сбор». Replit ходит в **живую БД**. SQL `profiles`/`characters` тот же. |
| **Ветка** | Раунды 5–6 смёржены из `arena/01a07841-idle-test`. Работать дальше — в новой `arena/…` от свежего `main`. |

Агент в боевой аккаунт не входит. Песочница — мок/`localStorage`.

SQL не менялся. Прогресс — JSON `save_data`, мигратор поднимает старого героя.
Расовый старт — **очки 4/3/2/1**.

## Что сделать в НАЧАЛЕ нового чата (обязательно)

1. Прочитать (в этом порядке):
   `NEXT_CHAT_HANDOFF.md` (этот файл) →
   `DEVLOG.md` (верх, последняя сессия) →
   `ROADMAP.md` (текущий статус) →
   `artifacts/my-app/src/ARCHITECTURE.md` →
   `.agents/memory/aethelia-arch.md` →
   `BALANCE_FOUNDATION.md` → `STAGE5_FOUR_PILLARS_HANDOFF.md` →
   `scripts/assets/README.md` → `scripts/qa/README.md` → `scripts/qa/ACCEPTANCE.md`.
2. Изучить **архитектуру проекта, общую оценку архитектуры и корректное
   размещение файлов**: где чистая логика (`domain/`, `data/`, `core/`),
   где экраны (`features/`), где числа (`data/balance/`), где примитивы
   (`shared/ui/`), какие маршруты и где что нельзя переносить. Краткую
   сводку контекста положить в новый `NEXT_CHAT_HANDOFF.md` / DEVLOG-запись.
3. Установить **QA-браузер** (в песочнице, один раз): `node scripts/qa/setup-browser.mjs`.
   Инфо и канон — `scripts/qa/README.md` (Chromium `@sparticuz/chromium@131.0.1`
   + `puppeteer-core@23.11.1`, не Playwright/apt; БД в `/home/user/ui_shot`).
   Для прогонов нужен dev-сервер `corepack pnpm --dir artifacts/my-app dev --host 0.0.0.0 --port 3000`.
4. Подготовить **всё необходимое к работе**: `corepack pnpm --dir artifacts/my-app install`
   (если node_modules нет), `typecheck`, `build`, `validate:catalog`, `test:pillars`.
5. Вникнуть в работу: понять, **на чём мы остановились**, какие решения уже
   приняты и что нельзя трогать/возвращать. После этого — **ждать, когда
   владелец попросит продолжить**; ничего нового не начинать самому.

## Канон после раунда 6 (не нарушать)

- Единственная профессия сейчас — **«Сбор»** (`SkillId = 'foraging'`).
- Характеристики — **четыре столпа** (Стойкость/Мощь/Сноровка/Чутьё),
  их ветви, пассивки и нити. Старых «атака/сила/защита/охота/магия» как
  навыков НЕТ и не возвращать.
- Легаси-домены/страницы/типы (`woodcutting`, `mining`, `fishing`,
  `cooking`, `smithing`, `firemaking`, `prayer`, `spell`, `slayer`,
  `ranged`, `magic` как навыки) удалены. Не переименовывать и не
  поднимать «брак» без явного решения владельца.
- Все числа — только `src/data/balance/`; UI не хардкодит цвета.

## Что уже сделано и в `main` (не переделывать без фидбека)

- Хаб `/hero`: Тело, Экип, Нити («Пульт» 3×N), Путь (шесть дощечек + 12 пачками).
- Админ «Персонаж» вкладками: Характеристики / Профессии / Сумка /
  Снаряжение; сохранение пишет только тронутые блоки и перечитывает цифры.
- Админ «Предметы»: снаряжение/оружие видно через `getAllItems()`, дубли по `id` скрыты.
- Оффлайн — только «Сбор» (`offlineProgress.ts`, кап 24 ч).
- Старт расы **4/3/2/1**, нити 15/10 · 35/20 · 55/35, тройные 35/35/35.
- XP героя: `data/balance/xpRates.ts` (dedicated30, профессии ×0,35, стена 97–100 ≈ 5д). **Не подключено к тикам.**
- Тексты карточек — `ruleRu`, без детских аллегорий. Неподключённое честно «не действует».
- Удар на Пути: отображаемый диапазон ±15%. Крит 5% — заглушка. Удача — живой стат.
- Настройки героя (сброс, ник, аватар, смена, удаление) — в персонаже, не в игре.

## Следующая работа (одна задача, начинать только по просьбе владельца)

**Привязать уже лежащие картинки предметов / экипа / UI к данным через
`iconUrl()`.** Не качать каталог на вывеске. Не начинать тики, бой и новую
схему БД в том же заходе. Новый контент строить на столпах + «Сборе», без
легаси-навыков.

Конвейер: `scripts/assets/README.md`. Предметы живут в клиенте; облако —
по-прежнему `characters.save_data`. Storage под иконки и новые таблицы —
только если владелец отдельно попросит.

## Что нельзя

- Хардкод цветов; новые экраны не на `gameUI.tsx`.
- `.png` в `<img>` — только `iconUrl()` / `getAvatarPath()`.
- Подключать `heroXpFromAction` к тикам и неподключённые пассивки «чтобы было».
- Менять `.replit` / порты без необходимости.
- Возвращать легаси-навыки/профессии/боевые навыки из раунда 6.
- Google Fonts. Превью Arena. Playwright.
- Параллельные задачи.

## Документы

| Файл | Зачем |
|---|---|
| `DEVLOG.md` | Журнал. Сессия 27 — раунды 5–6 (большая чистка). Новые записи сверху. |
| `ROADMAP.md` | План; текущий статус обновлён на раунд 5–6 + мерж. |
| `artifacts/my-app/src/ARCHITECTURE.md` | Карта `src/`, правила размещения файлов, вывеска, проверки. |
| `.agents/memory/aethelia-arch.md` | Краткая память архитектуры/маршрутов/сейвов. |
| `.agents/memory/aethelia-workflow.md` | Правила работы в Arena. |
| `BALANCE_FOUNDATION.md` | Числа столпов/нитей. |
| `STAGE5_FOUR_PILLARS_HANDOFF.md` | Смысловой контракт Этапа 5 (код ушёл дальше — смотри баланс и хаб). |
| `SUPABASE_SETUP.md` + `artifacts/my-app/SUPABASE_STAGE4.sql` | Ключи и схема. |
| `REPLIT_SETUP.md` | Порты 8080 / 5000 / 8081. |
| `scripts/assets/README.md` | PNG → WebP. |
| `scripts/qa/README.md` + `scripts/qa/ACCEPTANCE.md` | Установка QA-браузера и прогоны без облака. |

Исторические контракты Этапов 3–4 (`STAGE3_*`, `STAGE4_*`) не переписывать —
это решения, не карта файлов. Мокапы `design/niti/` сняты: «Пульт» уже в игре.

## Стартовая фраза (вставь владельцу в новый чат)

```text
Продолжаем Aethelia. В `main` уже смёржены Этапы 1–5B + раунды 5–6
(админ-каталог с снаряжением, перечитывание после сейва, чистка
легаси-навыков). Сейчас канон: 4 столпа + одна профессия «Сбор».

Сначала прочитай NEXT_CHAT_HANDOFF.md, DEVLOG.md (верх, Сессия 27),
ROADMAP.md (текущий статус), artifacts/my-app/src/ARCHITECTURE.md,
.agents/memory/aethelia-arch.md, BALANCE_FOUNDATION.md,
STAGE5_FOUR_PILLARS_HANDOFF.md, scripts/assets/README.md,
scripts/qa/README.md и scripts/qa/ACCEPTANCE.md.

Изучи архитектуру проекта и дай краткую оценку архитектуры и корректного
размещения файлов (где числа/логика/экраны/примитивы/маршруты и что нельзя
переносить). Затем установи QA-браузер: node scripts/qa/setup-browser.mjs
(канон в scripts/qa/README.md, не Playwright/apt). Потом подготовь всё
необходимое к работе: если нет node_modules — corepack pnpm install,
плюс typecheck/test:pillars/validate:catalog/build. После этого расскажи,
на чём мы остановились и как ты понял текущее состояние, и жди моего
следующего указания — пока я не попрошу, ничего нового не начинай.
```
