# Следующий чат — точка продолжения Aethelia

> **Главный принцип владельца:** не торопимся. Одна задача за раз.
> Если неясно — спрашиваем. Превью Arena не чинить. Секреты в чат не просить.
> Мерж в `main` — только по слову **«мержи»**.

## Где работа

| Куда | Что |
|---|---|
| **Эта ветка** `arena/01a07240-idle-test` | Вся игра после Этапа 4: столпы, хаб, Пульт, Путь, XP 1→100, тексты правил, прогрев вывески. **Не смёржена.** |
| **`main`** (`591696e`, PR #12) | Этапы 1–4, дорога 0–7, конвейер WebP, окно «Экип», 30 манекенов. **Подключён к Supabase.** Предок этой ветки: `origin/main` не содержит коммитов, которых нет у нас. |

Replit владельца на `main` ходит в живую БД. Песочница агента — без ключей, герой смотрится через мок/`localStorage`. Поэтому агент **не логинится** в боевой аккаунт и не судит облачный сейв глазами.

Подтянуть ветку в Replit (тест, не мерж):

```bash
git fetch origin arena/01a07240-idle-test
git checkout arena/01a07240-idle-test
# или поверх текущего: git reset --hard origin/arena/01a07240-idle-test
```

Обратно на боевой `main`: `git checkout main && git pull`.

## Перед мержем в `main`

SQL **не менялся**. Таблицы `profiles` / `characters` те же (`SUPABASE_STAGE4.sql`).
Облако по-прежнему один JSON `save_data`. Новые поля (столпы, XP героя, нити)
подхватывает **мигратор** при загрузке — старый герой не должен потерять
инвентарь и уровень ремёсел. Расовый старт в коде — **очки 4/3/2/1**, не проценты.

Что обязан сделать владелец на Replit **с реальным аккаунтом**, до «мержи»:

1. Зайти своим героем на этой ветке (секреты `VITE_SUPABASE_*` уже в Replit).
2. Проверить: сессия не слетела, ник/аватар на месте, сумка и ремёсла целы.
3. Открыть «Герой»: Тело / Экип / Нити / Путь — без пустых иконок первого кадра.
4. Холодный заход: вывеска держится, пока тело и лицо героя готовы.

Если облако новее локального — игра возьмёт облако. Если ветка писала
локальный сейв с новой схемой — reconcile зальёт его в облако. Не тестировать
двумя вкладками одновременно.

## Вывеска (закон загрузки)

Файл: `artifacts/my-app/src/lib/bootPreload.ts`.

Вывеска / акт 0 **держат кадр**, пока декодированы:

- арт самой вывески + шрифты (со своими cap);
- дорога онбординга (`ONBOARDING_ART_PRELOAD`);
- первый кадр героя: 4 столпа, 12 веток, иконки вкладок хаба, герб;
- лица и манекены **уже известных** героев (после `authReady`).

Не ждём каталог из сотен предметов. Нити, глубинные пассивки и силуэты экипа
греются в фоне. В игре у слотов профессий/`ItemIcon` остаётся `loading="lazy"`.

Страховка: cap ~5 с на пакет героя. Маршруты не монтируются под заставкой
(iOS autofill). `minDisplayTimeMs=4000` и `SIGN_MIN_MS` не менять без владельца.

## Что уже в этой ветке (не переделывать без фидбека)

- Хаб `/hero`: Тело, Экип, Нити («Пульт» 3×N), Путь (шесть дощечек + 12 пачками).
- Старт расы **4/3/2/1**, нити 15/10 · 35/20 · 55/35, тройные 35/35/35.
- XP героя: `data/balance/xpRates.ts` (dedicated30, профессии ×0,35, стена 97–100 ≈ 5д). **Не подключено к тикам.**
- Тексты карточек — `ruleRu`, без детских аллегорий. Неподключённое честно «не действует».
- Удар на Пути: отображаемый диапазон ±15%. Крит 5% — заглушка. Удача — живой стат.
- Настройки героя (сброс, ник, аватар, смена, удаление) — в персонаже, не в игре.

## Следующая большая работа (после мержа, по слову владельца)

1. **Картинки предметов / экипа / UI** — привязать уже лежащие WebP к `items.ts`
   через `iconUrl()`. Не качать каталог на вывеске. Конвейер: `scripts/assets/README.md`.
2. **Supabase** — не новая таблица на каждый предмет. Предметы остаются в клиентских
   данных; в БД по-прежнему `save_data` jsonb (+ профили/герои). Отдельный перенос
   «все иконки в Storage» не делать, пока владелец не попросит.
3. Механики (тики XP, бой, эффекты пассивок) — **после** картинок, отдельными задачами.

## Что нельзя

- Хардкод цветов; новые экраны не на `gameUI.tsx`.
- `.png` в `<img>` — только `iconUrl()` / `getAvatarPath()`.
- Подключать `heroXpFromAction` к тикам и неподключённые пассивки «чтобы было».
- Менять `.replit` / порты без необходимости.
- Google Fonts. Превью Arena. Playwright.
- Параллельные задачи.

## Документы

| Файл | Зачем |
|---|---|
| `DEVLOG.md` | Журнал. Новые записи сверху. |
| `artifacts/my-app/src/ARCHITECTURE.md` | Карта `src/` + вывеска. |
| `BALANCE_FOUNDATION.md` | Числа столпов/нитей. |
| `STAGE5_FOUR_PILLARS_HANDOFF.md` | Смысловой контракт Этапа 5 (код ушёл дальше — смотри баланс и хаб). |
| `SUPABASE_SETUP.md` + `artifacts/my-app/SUPABASE_STAGE4.sql` | Ключи и схема. |
| `REPLIT_SETUP.md` | Порты 8080 / 5000 / 8081. |
| `scripts/assets/README.md` | PNG → WebP. |
| `scripts/qa/README.md` | Мок без облака. |

Исторические контракты Этапов 3–4 (`STAGE3_*`, `STAGE4_*`) не переписывать —
это решения, не карта файлов. Мокапы `design/niti/` сняты: «Пульт» уже в игре.

## Стартовая фраза

```text
Продолжаем Aethelia. Работа — ветка arena/01a07240-idle-test, в main не смёржена.

Прочитай NEXT_CHAT_HANDOFF.md, DEVLOG.md (верх), ARCHITECTURE.md.
Мы не торопимся. Одна задача. Мерж только по слову «мержи».
Секреты не просить. Превью Arena не чинить.
```
