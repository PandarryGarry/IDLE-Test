# Следующий чат — точка продолжения Aethelia

> **Главный принцип от владельца:** мы не торопимся. Создаём всё осторожно,
> корректно и правильно. Если что-то не понятно — сначала спрашиваем, а не
> пытаемся сделать всё и сразу, потому что потом придётся всё переделывать.
> **Никаких параллельных работ: одна задача за раз.**

## ⚠️ ПЕРВЫЙ ШАГ — статус ветки и фидбек владельца. Не код.

**Открытая работа лежит в ветке `arena/01a06167-idle-test` → PR #11 (не смёржен).**
В `main` — Этапы 1–4, дорога 0–7, слой 1 адаптива, конвейер картинок и контракт
Этапа 5 (PR #9). 5A и 5B — **только в ветке**.

1. Прочитать `DEVLOG.md` (Сессии 18–19), этот файл,
   `STAGE5_FOUR_PILLARS_HANDOFF.md`, `scripts/qa/README.md`,
   `scripts/assets/README.md`.
2. Спросить владельца: **смотрел ли экран «Герой → Тело» в Replit и что правим.**
   Без его фидбека ничего не переделывать.

Дорога проверена владельцем 2026-09-02: **холодный 0→игра и возвращение того же
героя — ок.** Вопрос «дорога ок?» больше не актуален.

## Что где лежит

| Куда | Что |
|---|---|
| `main` | Этапы 1–4, дорога, QA-мок, `tour.mjs`, конвейер WebP, контракт Этапа 5 |
| PR #11 | хотфикс создания героя, **5A** (столпы, матрица рас, мигратор), **5B** (хаб героя, доска тела, паутина пассивок) |

PR #10 (`arena/01a05550-idle-test`) **закрыт** — его работа перенесена в PR #11.
Мёртвые ветки прошлых сессий (`arena/01a048df…` … `01a0526e…`) — история мержа.

## Как владелец смотрит работу

Replit обновляется кнопкой **Pull** на `main`, поэтому ветку он тянет через shell:

```bash
git fetch origin
git checkout arena/01a06167-idle-test
```

Обратно на `main` — `git checkout main`.

**Превью Arena у владельца белое / connection reset — не чинить.**
Google Fonts сняты: в Replit системный serif. Cinzel/Inter не возвращать без вопроса.

## Тесты агента (документация + код)

Канон: `scripts/qa/README.md`.

| Файл | Зачем |
| --- | --- |
| `scripts/qa/setup-browser.mjs` | Chromium в песочнице Arena (один раз) |
| `scripts/qa/hero.mjs` | **снимки экрана героя** (сид + Тело/Экип/Нити/Путь) |
| `scripts/qa/tour.mjs` | холодный 0→игра + возвращение Каеля, без Supabase |
| `scripts/qa/road.mjs` | короче: акты 0–6 + возвращение |
| `scripts/qa/auth.mjs` | живой login/register (нужен реальный аккаунт в env) |
| `artifacts/my-app/src/lib/qaMock.ts` | мок auth/CRUD в localStorage вкладки |

**Браузер не Playwright и не apt.** Канон:

- `@sparticuz/chromium@131.0.1` + `puppeteer-core@23.11.1`
- **не** `@sparticuz/chromium@149` (ломает CJS)
- каталог браузера: `/home/user/ui_shot` (не в git)
- `LD_LIBRARY_PATH=/tmp/al2023/lib`, `FONTCONFIG_PATH=/tmp/fonts`

Прогон в Arena:

```bash
corepack pnpm --dir artifacts/my-app typecheck
corepack pnpm --dir artifacts/my-app test:pillars     # 15/15
# dev фоном, не одноразовый bash:
corepack pnpm --dir artifacts/my-app dev --host 127.0.0.1 --port 3000
node scripts/qa/setup-browser.mjs   # если нет /tmp/chromium
node scripts/qa/hero.mjs seed       # один раз: сид учётки и героя
node scripts/qa/hero.mjs            # снимки окна героя
node scripts/qa/tour.mjs            # ожидание: exit 0, ~45–60 с
```

Мок включается если:

- localhost + `localStorage.aethelia_qa_mock_v1=1` (ставит `hero.mjs`/`tour.mjs`), или
- Vite DEV **без** `VITE_SUPABASE_URL`, или
- сборка `VITE_QA_MOCK=1`.

Replit с ключами — **реальный Supabase**, мок выключен.
Учётка тура (только мок): `qa.tour@aethelia.local`, герой `Каель`.

### Не ретраить

- Playwright / apt / chromium 149.
- `enableQaMock({reset})` на каждый document (стирает БД на Vite reload).
- Returning + `dropSession` + логин + `navigate('/rules')` → пустой `#root`.
- Менять `minDisplayTimeMs` (4000) и `SIGN_MIN_MS` без владельца.
- Чинить белый экран Arena iframe.

## Дорога (не переписывать)

```text
ПЕРВЫЙ ЗАПУСК:
акт 0 «ЗНАК» → пролог 6 битов → толчок в auth (без вывески) →
rules → ложа → create → утро и первый шаг → игра

ПОВТОРНЫЙ ВИЗИТ:
вывеска ~4с → «Знакомый скрип вывески.» → выбор героя →
«Снова в путь, {ник}.» → игра
```

Пролог один раз на устройство (`aethelia_prologue_seen_v1`).
Закон арта: полный кадр без обрезки.

## Этап 5 — что решено и что сделано

Полный контракт: `STAGE5_FOUR_PILLARS_HANDOFF.md`.

**Сделано (5A + 5B, PR #11):**

- 4 столпа × 3 ветви; очки: +1 столп / уровень, +1 ветвь / 5 уровней, старт 0.
- Мигратор `attributes` в `save_data`, калькулятор `characterAttributes.ts`,
  тесты (15/15), каркас `src/data/balance/`.
- Хаб героя `/hero`: вкладки **Тело / Экип / Нити / Путь**, шапка с ником,
  уровнем, расой и свободными очками, 2 бесплатных respec.
- **Доска тела**: 4 столпа по сторонам, от каждого — 3 луча; на луче
  **ветвь + две глубинные пассивки** = **36 узлов**. Ранг узла **1–3**.
  Лестница: следующий узел открыт, когда предыдущий выкачан до конца.
  Запертые узлы — пунктир с точкой. Пан/зум, кнопка «к центру».

**Решено и НЕ сделано (долги):**

- **Эффекты пассивок не подключены** — в окне узла честно написано «Эффект:
  не подключён». Решение владельца: подключать позже, отдельной задачей.
- **Иконки 24 глубинных пассивок временные** — взяты существующие из набора
  (`skills/*`, `stats/*`). Нужен свой арт через `scripts/assets/optimize.mjs`.
- **Сброс за золото** — цены нет, кнопка пишет «цена не назначена».
- Экип: надеть/снять можно только из сумки; сумки в окне героя нет.
- Экран «Путь» — пока сухой список чисел.

## Что нельзя

- Хардкод цветов; новые экраны не на `gameUI.tsx`.
- `.png` в `<img>` — только `iconUrl()` / `getAvatarPath()`.
- Секреты в чат. Просить ключи — запрещено.
- Монтировать маршруты под заставкой (iOS autofill).
- Параллельные задачи: владелец просил брать одну за раз.

## Замечено, не блокер (спросить владельца)

- У части пунктов правил нет иконки.
- На дашборде много пустого поля под карточкой героя.
- «ЭТЕЛИЯ» системным serif (Google Fonts сняты).

## Обязательно прочитать

1. `DEVLOG.md` — Сессии 18–19.
2. Этот файл.
3. `scripts/qa/README.md` + `scripts/assets/README.md`.
4. `STAGE5_FOUR_PILLARS_HANDOFF.md`.
5. `ROADMAP.md`.

## Стартовая фраза — вставить в новый чат

```text
Продолжаем Aethelia. Открытая работа — ветка arena/… → PR #11 (5B, экран героя).

Прочитай DEVLOG.md (Сессии 18–19), NEXT_CHAT_HANDOFF.md,
STAGE5_FOUR_PILLARS_HANDOFF.md, scripts/qa/README.md, scripts/assets/README.md.

ПЕРВЫМ ДЕЛОМ спроси меня, что правим в экране «Герой → Тело» — я смотрел
ветку в Replit. Дальше — одна задача за раз, никаких параллельных работ.

Проверки агента: typecheck + test:pillars + node scripts/qa/hero.mjs
(мок, без Supabase; браузер @sparticuz/chromium@131.0.1, не Playwright).
Превью Arena не чинить. Секреты в чат не просить.

Мы не торопимся.
```
