# Следующий чат — точка продолжения Aethelia

> **Главный принцип владельца:** не торопимся. Одна задача за раз.
> Если неясно — спрашиваем. Превью Arena не чинить. Секреты в чат не просить.
> Мерж в `main` — только по слову **«мержи»**.

## Где работа

| Куда | Что |
|---|---|
| **`main`** | Этапы 1–5B в коде: столпы, хаб, Пульт, Путь, XP 1→100, `ruleRu`, вывеска греет первый кадр. Replit ходит в **живую БД**. SQL `profiles`/`characters` тот же. |
| Новая ветка чата | Только новая `arena/…`. Старая `arena/01a07240-idle-test` после мержа запечатана. |

После мержа в Replit: `git checkout main && git pull`.

Агент в боевой аккаунт не входит. Песочница — мок/`localStorage`.

SQL не менялся. Прогресс — JSON `save_data`, мигратор поднимает старого героя.
Расовый старт — **очки 4/3/2/1**.

## Вывеска (закон загрузки)

Файл: `artifacts/my-app/src/lib/bootPreload.ts`.

Вывеска / акт 0 **держат кадр**, пока декодированы:

- арт самой вывески + шрифты (со своими cap);
- дорога онбординга (`ONBOARDING_ART_PRELOAD`);
- первый кадр героя: 4 столпа, 12 веток, иконки вкладок хаба, герб;
- лица и манекены **уже известных** героев (после `authReady`).

Не ждём каталог из сотен предметов. Нити, глубинные пассивки и силуэты экипа
греются в фоне. В игре у слотов профессий/`ItemIcon` остаётся `loading="lazy"`.

Страховка: cap ~5 с на пакет героя. Маршруты не монтируются под заставкой
(iOS autofill). `minDisplayTimeMs=4000` и `SIGN_MIN_MS` не менять без владельца.

## Что уже в этой ветке (не переделывать без фидбека)

- Хаб `/hero`: Тело, Экип, Нити («Пульт» 3×N), Путь (шесть дощечек + 12 пачками).
- Старт расы **4/3/2/1**, нити 15/10 · 35/20 · 55/35, тройные 35/35/35.
- XP героя: `data/balance/xpRates.ts` (dedicated30, профессии ×0,35, стена 97–100 ≈ 5д). **Не подключено к тикам.**
- Тексты карточек — `ruleRu`, без детских аллегорий. Неподключённое честно «не действует».
- Удар на Пути: отображаемый диапазон ±15%. Крит 5% — заглушка. Удача — живой стат.
- Настройки героя (сброс, ник, аватар, смена, удаление) — в персонаже, не в игре.

## Следующая работа (одна задача)

**Привязать уже лежащие картинки предметов / экипа / UI к данным**
через `iconUrl()`. Не качать каталог на вывеске. Не начинать тики, бой и
новую схему БД в том же заходе.

Конвейер: `scripts/assets/README.md`. Предметы живут в клиенте; облако —
по-прежнему `characters.save_data`. Storage под иконки и новые таблицы —
только если владелец отдельно попросит.

## Что нельзя

- Хардкод цветов; новые экраны не на `gameUI.tsx`.
- `.png` в `<img>` — только `iconUrl()` / `getAvatarPath()`.
- Подключать `heroXpFromAction` к тикам и неподключённые пассивки «чтобы было».
- Менять `.replit` / порты без необходимости.
- Google Fonts. Превью Arena. Playwright.
- Параллельные задачи.

## Документы

| Файл | Зачем |
|---|---|
| `DEVLOG.md` | Журнал. Новые записи сверху. |
| `artifacts/my-app/src/ARCHITECTURE.md` | Карта `src/` + вывеска. |
| `BALANCE_FOUNDATION.md` | Числа столпов/нитей. |
| `STAGE5_FOUR_PILLARS_HANDOFF.md` | Смысловой контракт Этапа 5 (код ушёл дальше — смотри баланс и хаб). |
| `SUPABASE_SETUP.md` + `artifacts/my-app/SUPABASE_STAGE4.sql` | Ключи и схема. |
| `REPLIT_SETUP.md` | Порты 8080 / 5000 / 8081. |
| `scripts/assets/README.md` | PNG → WebP. |
| `scripts/qa/README.md` | Мок без облака. |

Исторические контракты Этапов 3–4 (`STAGE3_*`, `STAGE4_*`) не переписывать —
это решения, не карта файлов. Мокапы `design/niti/` сняты: «Пульт» уже в игре.

## Стартовая фраза

```text
Продолжаем Aethelia. Предыдущая ветка смёржена в main.
Следующая задача — привязать уже лежащие картинки предметов/экипа/UI
к данным через iconUrl(). Каталог на вывеске не грузить. Тики и новую
схему БД не начинать.

Прочитай NEXT_CHAT_HANDOFF.md, DEVLOG.md (верх), ARCHITECTURE.md,
scripts/assets/README.md. Мы не торопимся. Одна задача.
Секреты не просить. Превью Arena не чинить. Мерж только по слову «мержи».
```
