# Следующий чат — точка продолжения Aethelia

> **Главный принцип владельца:** не торопимся. Одна задача за раз.
> Если неясно — спрашиваем. Превью Arena не чинить. Секреты в чат не просить.
> Мерж в `main` — только по слову **«мержи»**.
> **Единая правда о текущем состоянии — этот файл + верх `DEVLOG.md`.**
> Если другой документ противоречит им — устарел он.

## Где работа

| Куда | Что |
|---|---|
| **`main` (актуально с 2026-09-11)** | Всё по PR #20 включительно: PR #16–18 (предметная система: 630 предметов, 12 тиров, уники, 5 зон «Сбора») → сессия 31 (уборка документации + «Стекло таверны» шаги 1–2) → **сессия 32** (инфраструктура: `runtimePorts`, `saveSchema`, `tabAuthority`, числа в `data/balance/`, persist настроек, CI-охрана, SQL-страж). Детали: `DEVLOG.md` → «Сессия 32» |
| **Следующий заход** | **UI/UX-этап:** шаги 3–11 плана `UI_UX_AUDIT.md` §6 (внешний вид), затем UX-доработки. Инфраструктура сессии 32 больше не цель правок |
| **Закрыто** | PR #19 закрыт как избыточный (содержимое вошло в #20). `artifacts/mockup-sandbox/` больше нет |

**Хвосты владельца (разовые, после мержа #20):** скопировать `.ci/ci.yml` →
`.github/workflows/ci.yml` (агент без scope `workflows` не может) и применить
`artifacts/my-app/SUPABASE_STAGE4_1_PROFILE_GUARD.sql` в Supabase SQL Editor.

Агент в боевой аккаунт не входит. Песочница — мок/`localStorage`.
Прогресс — JSON `characters.save_data`, мигратор поднимает старые сейвы.
SQL `profiles`/`characters` не менялся. Расовый старт — очки **4/3/2/1**.

## Что добавилось в кодекс после сессии 32

- **Домен не импортирует сторы.** Нужен внешний факт → порт в
  `src/domain/runtimePorts.ts` + подпись из стора. `test:pillars` обязан
  запускаться без `node_modules` (сейчас 76/76 на голом дереве).
- **Сейв = недоверенный ввод.** Все новые поля сейва нормализуются в
  `src/lib/saveSchema.ts`; там же единый критерий «полного сейва» и
  кодирование импорта/экспорта.
- **Числа — в `data/balance/`** (плюс файлы `combat.ts`, `inventory.ts`,
  `loop.ts`, `offline.ts`). Боевые коэффициенты в сторе = нарушение закона.
- **CI** (исходник `.ci/ci.yml`, скопировать в `.github/workflows/`) повторяет
  приёмку сам; `pnpm build` работает с корня.
- **Живую БД правит только владелец:** `SUPABASE_STAGE4_1_PROFILE_GUARD.sql`
  (триггер на `role`/`donate_currency`) — применить в Supabase вручную.

## Что сделать в НАЧАЛЕ нового чата (обязательно)

1. Прочитать (в этом порядке):
   `NEXT_CHAT_HANDOFF.md` (этот файл) → `DEVLOG.md` (верхняя запись) →
   `ROADMAP.md` (блок «Текущий статус» и «Дальше») →
   **`UI_UX_AUDIT.md` целиком (это рабочий документ ближайшего этапа: канон +
   §6 план из 11 шагов, шаги 1–2 сделаны)** →
   `artifacts/my-app/src/ARCHITECTURE.md` → `.agents/memory/aethelia-arch.md` →
   `.agents/memory/aethelia-workflow.md` → `BANK_FOUNDATION.md` (терминология
   инвентарь/банк — закон).
   По теме задачи: числа — `BALANCE_FOUNDATION.md`; цвета/токены —
   `src/styles/THEME_GUIDE.md`; картинки — `scripts/assets/README.md`;
   QA/скриншоты — `scripts/qa/README.md`.
2. Подготовить окружение:
   `corepack pnpm@10 install` (явно `@10`: голый `corepack pnpm` тянет v12 и
   портит lockfile), затем зелёный контур:
   `pnpm run typecheck`, `pnpm --filter @workspace/my-app run test:pillars`,
   `pnpm --filter @workspace/my-app run validate:catalog`,
   `pnpm --filter @workspace/my-app run build`.
   QA-браузер (один раз на песочницу): `node scripts/qa/setup-browser.mjs`
   (Chromium `@sparticuz/chromium@131.0.1` + `puppeteer-core@23.11.1`, не Playwright).
3. Понять, **на чём остановились** и что нельзя трогать. После — **ждать
   указания владельца**; ничего нового не начинать самому.

## Канон сейчас (не нарушать)

- Единственная профессия — **«Сбор»** (`SkillId = 'foraging'`, 5 зон).
  Характеристики — **четыре столпа** (Стойкость/Мощь/Сноровка/Чутьё),
  12 ветвей, нити, пассивки. Легаси-навыки не возвращать.
- Предметы — **только каталог** `src/domain/items/catalog/` (630 шт.);
  вход — `src/domain/items/index.ts` (`getItem()`/`getAllItems()`).
  Легаси-эмодзи (`items.ts`) — только для чтения старых сейвов.
- Числа — только `src/data/balance/`. Тиры снаряжения 1–12 — генерация
  `база тира 1 × gearTierScale(tier)` в `data/balance/gear.ts`
  (`GEAR_TIERS_NOW = ALL_GEAR_TIERS`). Уники — флаг `gearUnique`, не тир.
- Картинки — только WebP через `iconUrl()` / `getAvatarPath()`;
  `.png` в `<img>` запрещён. Конвейер: `scripts/assets/README.md`.
- Вывеска греет первый кадр, каталог НЕ качает (`lib/bootPreload.ts` не трогать).
- **Банк** — будущая отдельная система, сейчас не существует: инвентарь — это
  «сумка» (`/inventory`). Терминологический закон: `BANK_FOUNDATION.md`.
- Тексты игроку — `ruleRu`. Неподключённое честно помечаем «не действует».
- XP-таблица (`data/balance/xpRates.ts`) и боевые пассивки **не подключены**
  к тикам — не подключать «чтобы было».

## Что нельзя

- Хардкод цветов в НОВЫХ экранах (старые экраны с hex ждут отдельный
  этап UI/UX-унификации — не править точечно без него).
- Новые экраны не на `gameUI.tsx` / `shared/ui/kit` примитивах.
- Менять `.replit` / порты без необходимости. Менять SQL без владельца.
- Возвращать легаси-навыки. Google Fonts. Playwright. Превью Arena.
- Подключать `heroXpFromAction` к тикам. Параллельные задачи.

## Следующая работа (только по слову владельца)

**Текущий этап (утверждён владельцем 2026-09-11): внешняя UI/UX-унификация,
затем UX-доработки.** Инфраструктура («порядок» сессии 32) закрыта в `main`;
её не рефакторим попутно.

1. **Внешний вид — план `UI_UX_AUDIT.md` §6, строго по одному шагу (3 → 11):**
   3 — `gameUI`+`kit` на `var()`-шкалу таверны; 4 — попап предмета по эталону;
   5 — «Экип»: `.hero-sq-slot` → `ItemCell`; 6 — эмблема уникальности (ждёт
   ответ владельца, шаг 0); 7 — удаление старых предметов из каталога+сейвов;
   8 — модалки → `AWindow`; 9 — `ATabs`/`AButton` (+истребление ручных цветов,
   ~164 hex в 14 файлах); 10 — нижняя навигация (ждёт арт-иконки); 11 —
   build-страж «hex вне темы = ошибка» + закон в `ARCHITECTURE.md`.
   После каждого шага: typecheck + pillars-тесты + сборка + скриншоты
   «до/после» того же окна; смотрит владелец в Replit.
2. **Не смешивать:** перенос `liveAttributes` в zustand — сразу ПОСЛЕ шага 5
   отдельным заходом (перечислен в `ROADMAP.md` «Технические задачи»);
   переезд PNG-мастеров из `public/` — только после всего UI-этапа.
3. **После внешнего вида — UX-доработки** (порядок, клики, состояния, подсказки);
   их план составляется по итогам пройденных шагов, в `UI_UX_AUDIT.md` §7
   остаются открытые вопросы владельцу (эмблема, правило фона).

Тактические кандидаты после этого (выбирает владелец, по одному):
износ прочности экипа (поле `maxDurability` уже в данных) — вместе с боем;
бой и мобы по `COMBAT_MODEL_PILLARS.md` (проект на согласовании);
банк — по `BANK_FOUNDATION.md` (не в ближайшее время).

## Документы

| Файл | Зачем | Статус |
|---|---|---|
| `DEVLOG.md` | Журнал всех сессий (новые сверху; сессии 28–30 дописаны задним числом в сессии 31) | живой |
| `ROADMAP.md` | План: что сделано, что дальше, техдолг | живой |
| `artifacts/my-app/src/ARCHITECTURE.md` | Карта `src/`, правила размещения, проверки | живой |
| `BANK_FOUNDATION.md` | Банк — будущая система; терминология | живой |
| `UI_UX_AUDIT.md` | Канон «Стекло таверны» + план §6 (шаги 1–2 сделаны) | **рабочий документ текущего UI-этапа** (§7 — открытые вопросы владельцу) |
| `BALANCE_FOUNDATION.md` | Конституция баланса столпов/нитей | утверждён, действует |
| `GEAR_TIER_FOUNDATION.md` | Карта снаряжения: диск → слоты → тиры | реализован (PR #17/#18) |
| `ITEM_SYSTEM_PLAN.md` | Контракт предметной системы | реализован, §12–15 — видение |
| `FORAGING_V2_DESIGN.md` | Дизайн «Сбора» v2 (5 зон) | реализован |
| `EQUIP_WINDOW_DESIGN.md` / `NITI_WINDOW_DESIGN.md` | Журналы выбора окон «Экип»/«Нити» | сделано, история |
| `COMBAT_MODEL_PILLARS.md` | Боевая модель на столпах | проект, бой не строен |
| `AUDIT_BRANCHES_THREADS.md` | Аудит эффектов ветвей/нитей к реальным системам | проект (для будущего подключения) |
| `PLAYER_GLOSSARY.md` | Словарь для игрока | проект-кандидат в справку |
| `CHARACTER_FOUNDATION_AUDIT.md` | Аудит стартового бюджета 2026-09-05 | история, решения применены |
| `STAGE3_*`, `STAGE4_*`, `STAGE5_*` | Контракты этапов 3–5 | история, не переписывать |
| `SUPABASE_SETUP.md` + `artifacts/my-app/SUPABASE_STAGE4.sql` | Ключи и живая схема | живой |
| `REPLIT_SETUP.md` | Порты: 8080 (web), 5000 (api). Canvas 8081 удалён | живой |
| `replit.md` | Краткий паспорт для Replit-агента | живой |
| `src/styles/THEME_GUIDE.md` | Токены и тема | живой (для этапа UI/UX) |
| `scripts/assets/README.md` | PNG → WebP конвейер | живой |
| `scripts/qa/README.md` + `ACCEPTANCE.md` | Проверки без облака | живой |

Удалено в сессии 31 (не искать): `HANDOFF.md` (поглощён DEVLOG),
`ROADMAP_ITEM_SYSTEM_REBUILD.md` (build-спека исполненной работы),
`ITEMS_CATALOG.md.markdown` (вёл на несуществующий генератор; лестницы
материалов теперь в `data/balance/gear.ts`), `artifacts/mockup-sandbox/`.

## Стартовая фраза (вставь владельцу в новый чат)

```text
Продолжаем Aethelia. В main смёржено всё по PR #20 включительно:
предметная система (630 предметов, 12 тиров, уники, «Сбор» 5 зон),
сессия 31 (уборка документации + «Стекло таверны» шаги 1–2) и сессия 32
(инфраструктура: runtimePorts, saveSchema, tabAuthority, числа в
data/balance, persist настроек, CI, SQL-страж). UI до сих пор не тронут.
Канон: 4 столпа + одна профессия «Сбор»; банк — будущая система
(BANK_FOUNDATION.md), пока не существует.

Текущий этап — внешний UI/UX: по одному шагу плана UI_UX_AUDIT.md §6
(3 → 11), после каждого — зелёный контур + скриншоты «до/после»;
после внешнего вида перейдём к UX-доработкам. Живую БД и .replit не
трогать, мерж только по моему слову «мержи».

Сначала прочитай полностью: NEXT_CHAT_HANDOFF.md, DEVLOG.md (верх),
ROADMAP.md, UI_UX_AUDIT.md, artifacts/my-app/src/ARCHITECTURE.md,
.agents/memory/aethelia-arch.md и aethelia-workflow.md,
BANK_FOUNDATION.md. Подготовь окружение (corepack pnpm@10 install,
typecheck, test:pillars, validate:catalog, build; при первом заходе
QA-браузер: node scripts/qa/setup-browser.mjs). Расскажи, как понял
текущее состояние и что будешь делать на шаге 3, и жди моего указания.
```
