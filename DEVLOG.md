# 📓 Aethelia RPG — DEVLOG (журнал разработки)

> **Назначение файла:** память между чат-сессиями Arena.
> Каждый новый чат агент ЧИТАЕТ этот файл ПЕРВЫМ ДЕЛОМ, а в конце сессии —
> ОБНОВЛЯЕТ его (новая запись сверху в «Журнале сессий») и мержит в main вместе с кодом.

---

## ⚙️ КАК МЫ РАБОТАЕМ (правила процесса — читать агенту в начале каждого чата)

### Участники
- **Разработчик (владелец):** PandarryGarry — контролирует всё, тестирует в Replit на iPhone.
- **Агент (Arena):** пишет код, работает в клоне репозитория, пушит через свою ветку.

### Жизненный цикл одного чата = один этап работы
1. Агент читает `DEVLOG.md` (этот файл), `ROADMAP.md`, `git log --oneline -15` — восстанавливает контекст.
2. Агент работает: код → коммиты → push в свою ветку `arena/...` → открывает PR.
3. Владелец тестирует: в Replit подтягивает ветку PR (`git fetch` + checkout) или ждёт мержа.
4. Правки по фидбеку — агент пушит в ту же ветку, PR обновляется сам.
5. **В САМОМ КОНЦЕ чата** (по команде владельца «мержи»):
   - агент обновляет `DEVLOG.md` (запись о сессии) и `ROADMAP.md` (галочки),
   - коммитит, пушит, мержит PR в `main`.
6. ⚠️ **После мержа PR сессия «запечатывается»** — push в GitHub больше не работает.
   Поэтому мерж — строго ПОСЛЕДНЕЕ действие. Всё, что после мержа — потеряется
   (придётся переносить руками через Replit).
7. Владелец в Replit: `git pull` → тестирует в Webview (превью Arena на iPhone не работает).

### Технические правила
- **Картинки в UI — только WebP через `iconUrl()` / `getAvatarPath()`.**
  Мастер PNG 1024 не ставить в `<img>`. После нового PNG:
  `node scripts/assets/optimize.mjs`. Канон: `scripts/assets/README.md`.
- **Тестирование владельцем — только через Replit** (превью Arena на iOS показывает
  «Preview Unavailable» — известное ограничение, не чинится со стороны агента).
- **Хардкод цветов запрещён** — только CSS-переменные (`index.css`) / токены (`tokens.ts`).
- **Новые экраны — только на примитивах** `src/shared/ui/gameUI.tsx` (GButton, GModal, GCard...).
- **`pnpm typecheck` должен быть чистым** перед каждым мержем.
- **Changelog** (`src/data/changelog.ts`) — только важные для игрока изменения.
- **Коммиты подробные** — они тоже часть памяти между сессиями.
- Токены/секреты в чат НЕ вставлять никогда. Supabase-ключи — через Secrets в Replit / .env.

### Структура репозитория (главное)
- `artifacts/my-app/` — игра (Vite + React + TS + Tailwind + zustand + wouter).
  Запуск: `cd artifacts/my-app && pnpm dev` (порт 3000).
- `artifacts/my-app/public/assets/icons/` — 1000+ иконок: мастер PNG + рантайм WebP
  (точное число: `find public/assets/icons -name '*.webp' | wc -l`).
- `artifacts/my-app/src/domain/items/catalog/` — все предметы (630 шт.,
  вход — `domain/items/index.ts`); тиры снаряжения генерируются из
  `src/data/balance/gear.ts`.
- `scripts/assets/` — конвейер картинок (`optimize.mjs`, README).
- `artifacts/my-app/public/assets/art/` — заставочные арты (WebP wide/tall/square).
- `artifacts/my-app/src/features/hero/HeroHubPage.tsx` — хаб героя.
- `artifacts/my-app/src/lib/bootPreload.ts` — что греть на вывеске.
- `artifacts/my-app/src/data/balance/` — все ЧИСЛА Этапа 5 (нигде больше).
- `scripts/qa/` — headless-браузер и прогоны без облака (`hero.mjs`, `tour.mjs`).
- `ROADMAP.md` — план из 8 этапов с принятыми решениями.
- `DEVLOG.md` — этот файл.

---

## 📍 ТЕКУЩИЙ СТАТУС

**`main`:** Этапы 1–5B + раунды 5–6 + предметная система (PR #16–18).
В коде: хаб (Тело/Экип/Нити/Путь), Пульт, Путь, XP-таблица, `ruleRu`,
вывеска греет первый кадр. Каталог **630 предметов** на реальных WebP
(вход — `src/domain/items/index.ts`); снаряжение всех **12 тиров**
(генерация из `data/balance/gear.ts`, ~498 позиций), **уники** — флаг
`gearUnique`, отдельная категория в админке/пикере. Экип → 12
подхарактеристик тела (`domain/attributes/equipmentSubstats.ts`).
Сейвы: авторитетный — самый свежий (`reconcileCharacterSave`, PR #17).
Одна профессия — «Сбор» (5 зон). Replit ходит в живую БД. SQL не менялся.

**Текущий статус:** ветка `arena/01a08a68-idle-test` — **Сессия 31,
большая уборка документации и репозитория** (НЕ смёржено на момент
записи). Проверки на HEAD: typecheck 0, test:pillars 63/63,
validate:catalog 630 (0 ошибок), build ok.

**Следующий чат:** читает `NEXT_CHAT_HANDOFF.md` (единый вход),
готовит окружение и **ждёт указания владельца**. Следующее стратегическое
направление (зафиксировано 2026-09-10) — этап **UI/UX-унификации**:
палитра, централизация цветов/ячеек/окон, единый стиль игры. До него
точечные цветовые правки не делать. Банк — только по `BANK_FOUNDATION.md`
(не в ближайшее время).

**Манекены (арт 5B):** все 30 собраны и апскейлены до 1024; люди
утверждены, эльфы/гномы/орки/зверолюди прошли полировку. Рост у всех
рас одинаковый, комплекция различна: люди 22514, эльфы +2%,
орки +32%, гномы +46% по массе силуэта. Правка посадки ладоней
остановлена владельцем — оставляем как есть. Подробности:
`artifacts/my-app/art_masters/paper_dolls/HANDOFF.md`.

История решений — журнал сессий ниже. Точка входа следующего чата — только
`NEXT_CHAT_HANDOFF.md`. `intro_sign.webp` **используется** (акт 0).

---

## 📜 ЖУРНАЛ СЕССИЙ (новые записи — СВЕРХУ)

### Сессия 31 — 2026-09-10 — Большая уборка документации и репозитория

**Ветка:** `arena/01a08a68-idle-test`. **НЕ смёржено.** Мерж — только по слову владельца.

Повод: документация отстала от кода на три смёрженные сессии (28–30 существовали
только в PR). Владелец зафиксировал программу: навести порядок в документации и
репозитории, банк — отдельная будущая система (не путать с инвентарём), этап
UI/UX-унификации — следующим, цвета сейчас не трогаем.

- **Полный аудит** (до правок): typecheck 0, test:pillars 63/63, validate:catalog
  630, build ok. QA-браузер установлен в песочнице.
- **DEVLOG восстановлен:** сессии 28/29/30 дописаны задним числом (по PR #16/#17/#18).
- **Удалено:** `HANDOFF.md` (слепок сессии 29 — поглощён журналом);
  `ROADMAP_ITEM_SYSTEM_REBUILD.md` (build-спека уже исполненной работы);
  `ITEMS_CATALOG.md.markdown` (вёл на несуществующий генератор `tools/build_items_catalog.py`
  и на недисковые имена; лестницы материалов давно в `data/balance/gear.ts`);
  `artifacts/mockup-sandbox/` целиком + его Canvas-workflow в `.replit`
  (мокапы экранов реализованы в игре, Canvas больше не нужен; порт 8081 свободен).
- **Перенесено:** `artifacts/foraging_v2_design.md` → `FORAGING_V2_DESIGN.md`
  (корень, статус «реализовано»). Все гейм-доки снова в корне.
- **Создано:** `BANK_FOUNDATION.md` — банк как будущая система + терминологический
  закон «инвентарь/сумка — это inventory; банк не существует». Зафиксированы
  3 места legacy-чтения `src.bank` (это груз старых сейвов, не фича) и мёртвый
  редирект в `App.tsx` (известный, чиним вместе с банком).
- **Статусы приведены к коду:** `BALANCE_FOUNDATION` (утверждён/действует),
  `GEAR_TIER_FOUNDATION` (реализован, PR #17/#18), `ITEM_SYSTEM_PLAN`
  (реализован, §12–15 — видение), `EQUIP_WINDOW_DESIGN` (иконки привязаны).
- **Ориентиры обновлены:** `NEXT_CHAT_HANDOFF.md` переписан (новый канон
  и карта документов), `ROADMAP.md` (статусы этапов 5–8, техдолг актуализирован,
  направление UI/UX), `README.md` (инструкция добавления предмета ведёт в
  `domain/items/catalog/`, таблица документов), `replit.md`, `REPLIT_SETUP.md`
  (порты 8080/5000, Canvas удалён), `ARCHITECTURE.md` (честная строка про
  шаблонные пакеты + правило инвентаря/банка), память агентов.
- Комментарии в каталоге предметов больше не ссылаются на удалённый
  `ITEMS_CATALOG.md`.
- Открытые техдолги разнесены в `ROADMAP.md` → «Технические задачи»: сводка
  validate:catalog не считает 498 позиций снаряжения, мёртвый редирект
  `/inventory`→`/inventory` (вместе с банком), hex-цвета вне токенов
  (этап UI/UX), вынос мастер-PNG из `public/` (~0,75 ГБ репо), PWA-манифест.

### Сессия 30 — 2026-09-10 — Тиры 1–12, уники отдельной категорией, раскладка «Экип»

**Ветка:** `arena/01a086df-idle-test` (поглотила 4 коммита `arena/01a0858e-idle-test`).
**Смёржено: PR #18.** Запись восстановлена в сессии 31.

- **Все 12 тиров снаряжения:** `GEAR_TIERS_NOW = ALL_GEAR_TIERS`; статы и
  прочность старших тиров = база тира 1 × `gearTierScale(tier)`
  (`data/balance/gear.ts`). Каталог вырос до **630 предметов** (498 — снаряжение).
- **Уники — не «тир»:** флаг `gearUnique`; вместо «Тир N» метка «Уник.»,
  в ячейке золотой бейдж «УНИК» (тир остаётся только в расчётах).
  Уник-варианты — последние 5 вариантов каждой семьи (тировых колец 10→5,
  ожерелий 15→5). Админка: сумка «Уники» с фильтрами «слот»/«тип»,
  сортировка по типу. Пикер и окно «Экип»: фасовка «слот → тир → семья»
  + отбор «Все / Тировое / Уникальное», уник-группы после тировых.
- **Раскладка «Экип»:** браслеты — в 3–4-й ряды (рядом со штанами),
  плащ — в нижнюю ячейку рядом со щитом.
- Проверки: typecheck 0, test:pillars 63/63, validate:catalog 630, build ok;
  QA-снимки окна «Экип» и админ-«Уники»/«Бижутерия».

### Сессия 29 — 2026-09-09 — Фикс сохранения + тир-1 снаряжения + чистка «банка»

**Ветка:** `arena/01a08236-idle-test` (вобрала и содержимое `arena/01a0815a`).
**Смёржено: PR #17 (14 коммитов / 59 файлов).** Запись восстановлена в сессии 31.

- **Фикс регресса сохранения:** `selectCharacter` затирал свежий локальный
  прогресс (сумка/XP/«Сбор») старым облачным слепком `save_data`.
  `reconcileCharacterSave` переписан: валиден только ПОЛНЫЙ сейв;
  авторитетный — самый свежий из локального и облачного; память приводится
  к нему; локальный поднимается в облако. Слепой `applySaveData` убран.
  Файлы: `lib/characterSave.ts`, `store/characterStore.ts`.
- **Слово «банк» убрано из игры:** инвентарь везде `inventory`/«сумка».
  Legacy-чтение `src.bank` из старых сейвов сохранено (см. теперь
  `BANK_FOUNDATION.md` §2).
- **Экип → 12 подхарактеристик тела:** мост `domain/attributes/equipmentSubstats.ts`
  (`sumEquipmentBonuses` / `foldBonusesIntoRaw`).
- **Тир-1 снаряжение на реальных картинках:** `catalog/gear/gearItems.ts` +
  `data/balance/gear.ts`. Все бонусы лежат в осях подхарактеристик (тест).
- **UI «Экип» героя:** реальные картинки в слотах/сумке, ожерелье рядом
  со шлемом, виден второй браслет, показаны бонусы снаряжения.
- **Админка:** только настоящие картинки (без силуэтов/эмодзи); в
  «Персонажи → Снаряжение» inset-кольцо выбора, компактные ячейки, показан тир.
- Проверки: typecheck 0, build 0, test:pillars 48/48, validate:catalog 162.

### Сессия 28 — 2026-09-09 — Чистка предметов: только наши с картинками

**Ветка:** `arena/01a0815a-idle-test`. **Смёржено: PR #16.**
Запись восстановлена в сессии 31.

- `getAllItems()` больше не склеивает каталог с `LEGACY_ITEMS` (~100 эмодзи):
  в админ-каталог/выдачу/инвентарь попадают только реальные предметы с
  картинками. `getItem()` достаёт легаси из старых сейвов — как и сейчас.
- Каталог 120 → **132**: +12 предметов охоты (трофеи/шкуры/сырое мясо) на
  существующих картинках; 6 иконок особых находок «Сбора» дорисованы по
  конвейеру (их не было ни на диске, ни в плане). Эмодзи-слотов в каталоге
  не осталось. `items.ts` — только снаряжение (28 силуэтов) для совместимости.
- Дроп монстров — только наши id; руны удалены; поле `Monster.bones` и
  двойное добавление костей убраны.
- Миграция старых id в сумках при загрузке (игра + админ-черновик),
  стопки складываются.
- Админка: фильтры «Еда»/«Трофеи», пустые категории убраны.
- Проверки: tsc чисто, test:pillars 36/36, validate:catalog 132, build;
  headless-QA: админка 160/160, инвентарь со старым сейвом 10/10.

### Сессия 27 — 2026-09-08 — Раунды 5–6: фидбек по админке + большая чистка легаси-навыков

**Ветка:** `arena/01a07841-idle-test`, всё в той же НЕ смёрженной ветке.
`main` не трогали. Мерж — только по слову владельца «мержи».

**Раунд 5 — фидбек владельца (закрыт в `2a731a0`):**
- В админ «Предметы» не попадало **снаряжение/оружие**.
  `AdminItemsPage` подключён к `getAllItems()` (каталог + легаси-семейства,
  включая оружие/шлемы/нагрудники/щиты), категории и подписи добавлены,
  дубли по `id` не показываются.
- После сохранения характеристик персонажа **отображаемые цифры не обновлялись**.
  В `AdminCharactersPanel` после `save()` сделан повторный
  `normalizeSave(updated.saveData, …)` → `setDraft(...)`, а для активного персонажа
  ещё и `applySaveData(...)`. Числовые поля «Уровень героя / Столпы / Опыт /
  Очки / Репутация / Энергия» теперь перечитываются после сейва.

**Раунд 6 — «брак» удалён. Осталась одна профессия (Сбор) и столпы.**
Всё легаси-боевое/профессиональное убрано, чтобы не путало будущие сессии:

- **Типы** (`data/types.ts`): `SkillId = 'foraging'`, `ALL_SKILL_IDS`,
  `GATHERING_SKILLS`, `ACTIVE_SKILLS` — только `foraging`; `CombatStats`
  только attack/strength/defence. Удалены интерфейсы `WoodcuttingTree`,
  `MiningRock`, `FishingSpot`, `CookingRecipe`, `SmithingRecipe`,
  `FiremakingLog`, `ThievingTarget`, `Prayer`, `Spell`.
- **Домены/модели**: удалены `domain/professions/{woodcutting,mining,fishing,
  cooking,smithing,firemaking}.ts`, `domain/combat/{prayers,spells}.ts`.
  `Monster` больше не содержит `combatStyle`/`slayerXp`; `slayer_coin` удалён
  из предметов. `domain/professions/foraging` + `professionStats` +
  `attributes` остались каноном.
- **UI**: удалены страницы Woodcutting/Mining/Fishing/Cooking/Smithing/
  Firemaking и `ActionGrid.tsx`, `shared/ui/kit/SkillCard.tsx`,
  `ActionCard.tsx`. `SkillHeader` — только «Сбор»; `SkillIcon`,
  `GlobalActiveBar`, `MobileNav`, `Sidebar`, `App`, `Dashboard`,
  `HeroHub`, `CombatPage` приведены к столпам+«Сбору».
- **Оффлайн**: `core/offlineCalc.ts` удалён; `core/offlineProgress.ts`
  переписан foraging-only (кап 24 ч, `rollOfflineForaging`, XP+лут,
  levelUps). `saveManager` теперь берёт его + `skillNameRu` + иконку 🌿.
- **Реестры/формулы/иконки**: `skillRegistry` только foraging;
  `formulas.ts` вычищен (мастерство/preservation/doubling/XP-per-hour/
  time-to-level/formatTime/smithing/cooking/junk) — остались боевые формулы
  и drop-sim; `skillIcons.ts` только foraging/gathering; `uiIcons.tsx`
  убраны изображения attack/magic/ranged; `tokens.ts` — только палитра
  4 столпов/боя и зелёная «Сбор», без wood/mining/fishing/...
- **Данные**: `data/balance/professions.ts` -> `PROFESSION_FEEDS` только
  foraging; `data/balance/professions` остальные (переработка/экномика) не
  трогали. `i18n.ts` — удалены все legacy `skill.*`, группы/дашборд/
  подписи старых профессий; остались «Сбор», группа «Бой/Профессии»,
  навигация и уведомления.
- **Прочее**: `notificationsStore` иконка только 🌿; `AdminCharactersPanel`
  старт `hitpoints`=10 убран (все навыки 1); `guestMode`, `playerStore`,
  `gameStore`, `adminConfigStore`, `AdminItemEditor`, `ItemInfoPopover`,
  `equipmentStats` — вычищены до актуальной модели.

**Проверки (все зелёные):**
- `corepack pnpm exec tsc -p tsconfig.json --noEmit` — 0 ошибок.
- `corepack pnpm build` — vite 7.10s, dist собран.
- `corepack pnpm validate:catalog` — 120 предметов, ошибок нет.
- `corepack pnpm test:pillars` — 36/36.

**Что осталось (не код):** не осталось — смёржено в `main` (PR #15).
Обновлён `NEXT_CHAT_HANDOFF.md` + `ARCHITECTURE.md` для следующего чата.

### Сессия 26 — 2026-09-08 — Админ «Персонаж»: разделил характеристики и профессии, починил выдачу предметов (раунд 4)

**Ветка:** `arena/01a07841-idle-test`, всё в той же НЕ смёрженной ветке.

**Фидбек владельца:** в выборе предмета непонятно, как выдать (просто
подсвечивается); в админке фигурировали «старые, ненужные характеристики»
(атака/сила/защита) и путались характеристики с профессиями.

**Сделано:**
- **Характеристики и профессии разведены.** Вкладки «Персонаж» теперь:
  Обзор / **Характеристики** / **Профессии** / Сумка / Снаряжение.
  - «Характеристики» — это **Четыре Столпа** (Стойкость/Мощь/Сноровка/Чутьё),
    их ветви (12 подхарактеристик), глубинные пассивки и нити. Плюс уровень
    героя, опыт героя, очки столпов/ветвей, репутация, энергия.
  - «Профессии» — только навыки/ремёсла (Сбор, Лесорубство, Кузнечное дело…)
    с уровнем и XP; подпись прямо объясняет разницу. Убраны «старые» поля
    атака/сила/защита/охота/магия как характеристики — это профессии.
- **Выдача предмета стала явной.** Внизу модалки каталога всегда видна
  закреплённая панель: выбранный предмет + «кол-во» + кнопка
  **«Выдать в сумку»** (или «Надеть» для экипа). Пока ничего не выбрано —
  панель подсказывает «Выбери предмет из каталога». Кнопка «Добавить/Выдать
  предмет» тоже переименована в «Выдать предмет».
- Та же логика сохранения без сброса: пишутся только затронутые блоки
  (характеристики / профессии / сумка / экип), attributes сохраняются только
  после правки вкладки «Характеристики».
- Каталог: русские подписи категорий, qty «−/+» и входное поле.

**Проверки:** `pnpm typecheck`/`build` чисто; headless Chromium desktop +
mobile — вкладки «Характеристики», «Профессии», выбор предмета и «Выдать в
сумку», сохранение — `pageerror: 0`.

### Сессия 25 — 2026-09-08 — Админ «Персонаж» вкладками + хардкорный XP «Сбора» (раунд 3)

**Ветка:** `arena/01a07841-idle-test`, всё в той же НЕ смёрженной ветке.

**Почему переделка:** владелец отклонил длинную одностраничную админку
(«ужасное оформление», «хлам — выпадающее меню на 600+ предметов», «скролинг
до самого низа») и потребовал современную админ-панель без радужных кнопок.
Плюс «Сбор» прокачивался до 10 уровня за ~2 минуты — нужно хардкорное XP.

**Сделано:**
- `AdminCharactersPanel.tsx` переписан в многостраничный формат: 4 вкладки
  внутри секции «Персонаж» — **Обзор / Навыки / Сумка / Снаряжение**.
  Вместо бесконечного скролла — сегментированные вкладки (тёмные, один
  акцентный жёлтый у активной), каждая вкладка — своя компактная карточка.
- Шаг в сторону современных CRUD-панелей: карточки-статы на обзоре, поиск
  навыка, «Всем: 1/10/50/99/База», быстрые кнопки уровня/опыта, сетка предметов.
- **Выдача предметов — через каталог в модалке** `GModal` (как item-info/
  инвентарь), а НЕ через `<select>` на сотни пунктов: поиск по названию,
  фильтр-чипы категорий (с русскими подписями), сетка-каталог, выбор предмета,
  поле количества `−/+/input`, кнопка «Добавить». Тот же модал для экипа —
  фильтрует по слоту.
- **Без радужных кнопок:** вся палитра монохромная (тёмно-коричневые кнопки,
  тёмно-бежевые вторичные, один главный жёлтый на действие). Убраны
  красно-зелёно-синие `quickBtn` раунда 2.
- **Читаемость:** текст светлый на тёмных карточках, панели затемнены
  (`#241408` / `#150c04`); модалка каталога тоже тёмная поверх коричневого
  GModal — проверено скриншотами headless Chromium.
- **Сохранение без сброса сохранено:** пишутся только затронутые блоки
  (`touched.skills / bank / equipment`); проба — добавили ×7 «Дикие ягоды»,
  нажали «Сохранить» («Персонаж сохранён»), Атака осталась ур. 1.
- **Хардкор XP «Сбора»** (`src/domain/professions/foraging.ts`):
  зона 1 — 1 XP/5с, зона 2 — 4/6.5с, зона 3 — 14/8с, зона 4 — 50/9.5с,
  зона 5 — 350/11с. Ориентир по RS-таблице: до 10 ур. ≈1.6 ч, до 20 ≈3.5 ч,
  до 50 ≈13 ч, до 99 ≈73 ч (~3 дня активного сбора) — не минуты.

**Проверки:** `pnpm typecheck` и `pnpm build` чистые; headless Chromium
(админ-вкладки, каталог, qty, добавление, сохранение, навыки после сейва) —
`pageerror: 0`.

### Сессия 24 — 2026-09-08 — Фидбек владельца по «Сбору» + админ «Персонажи»

**Ветка:** `arena/01a07841-idle-test`, всё в той же НЕ смёрженной ветке.

**Сделано по фидбеку:**
- **Уведомления больше не спамят.** `gameStore.ts` / `combatStore.ts` больше не зовут
  `notifyItem` на каждую находку/дроп. Остались только важные: level-up, мастерство,
  переполнение сумки, сохранение, ошибки боя/профессий. Тексты тостов переведены на
  русские имена навыков (`skillNameRu`), «→ N» заменено на «уровень N».
- **Убраны крафтовые верёвки из «Сбора».** `rope_fiber`/`cord_sinew` удалены из всех
  пяти lootTable зон; веса перераспределены на грибы/ветки/травы. Верёвки — только
  крафт (`src/domain/items/catalog/foraging/bits.ts` оставлен как материал).
- **Русские имена профессий и понятный уровень.** Новый `src/lib/skillNames.ts`
  (канон `SKILL_NAMES_RU`/`skillNameRu`). `SkillHeader` всегда показывает русское
  название и явный «ур. N»; сайдбар, мобильный меню и глобальный актив-бар переведены
  на `skillNameRu` (не зависят от временной локали).
- **«Сбор» помещается на экран.** `SkillHeader compact`, сетка зон 2/3/4 колонки,
  карточки ужаты (описание/дроп/опасность перенесены в инфо-окно зоны), лента находок
  ограничена высотой 150px. Мобильный 390×844: все 5 зон видны, высота страницы ~1000px
  вместо ~1800px, верт. скролл минимальный.
- **Каждая зона — своя инфо-кнопка.** Кнопка «ⓘ» на каждой карточке открывает
  `ZoneInfoModal`: описание, параметры (требуемый уровень/опыт/цикл/мастерство),
  полная таблица добычи с %, редкие находки, опасность (мобы/веса/боссы).
- **Шансы в инфо-окне зон исправлены.** Раньше показывались «доли внутри таблицы»,
  поэтому у редких предметов был 40–60 %. Теперь показывается реальный шанс за цикл:
  обычные = вес/сумма обычных весов; редкие = общий шанс редкой находки × доля в
  rareTable. Итог: ветки/камни всегда заметно выше редких грибов/ягод.
- **Админ «Персонажи».** Новая вкладка `/admin/characters` + `AdminCharactersPanel`:
  выбор персонажа, редактирование всех навыков (уровень + опыт), золото,
  добавление/удаление предметов, экип по слотам; сохранение через
  `updateCharacter(characterId, { saveData })`. Проверено в QA-моке — уровень Атаки
  меняется и реально пишется в `aethelia_qa_db_v1`.
- **Фикс «скидываются на 1» + удобство админки:** при сохранении пишутся только те
  блоки, которые реально менял админ (навыки / сумка / экип), остальное берётся из
  исходного сейва. Если в старом сейве нет `player.skills`, подставляются текущие
  навыки активного героя (не дефолтные 1). Добавлены кнопки: −1/+1/+10/MAX для
  уровня, +1К/+10К/MAX XP для опыта, «Всем: 1/10/50/99/База», быстрые +/− для
  золота и количества предметов. Текст админки затемнён (`#2f2010`/`#4a3520`),
  читается на бежевых панелях.

**Проверки:** `pnpm typecheck` и `build` чистые; headless Chromium 390×844 —
`/foraging`, инфо-окно зоны, активный «Сбор», `/admin/characters` — `pageerror: 0`.

### Сессия 23 — 2026-09-07 — Foraging v2 «Сетка зон» (реализация, НЕ смёржено)

**Ветка/PR:** `arena/01a07841-idle-test` → запушено (`d73ba4f`). **НЕ мержить** до
слова владельца «мержи» (мерж закрывает доступ сессии).

**Дизайн:** `artifacts/foraging_v2_design.md` — утверждён владельцем («Ок»).

**Сделано (код):**
- `src/domain/professions/foraging.ts` — новый 5-зонный «Сбор» (Лесные заросли → Тёмная
  чаща → Пещера → Руины → Тайник): свои lootTable/rareTable/danger-лестница,
  чистые `rollForagingCycle()` / `rollOfflineForaging()`, формулы от персонажа
  (Темп/Находчивость/Удача/Интуиция), капы. Джекпота нет.
- `src/domain/professions/professionStats.ts` — модель `ProfessionStatDef`
  (tier early/master, unlockLevel, base/perLevel/cap/k) + 6 стат «Сбора»
  (`forage_speed`, `forage_double_loot`, `forage_rare_find`; `forage_shop_discount`,
  `forage_attention`, `forage_full_profession`). `forage_sell_bonus` удалён.
- `src/store/professionStatsStore.ts` — эффективные статы: дефолты + админ-overrides +
  уровень; `getProfessionStatValue()`, `useProfessionStatsStore`.
- `src/store/foragingStore.ts` — сессия: activeZoneId, lootFeed (кап 30), sessionXp,
  start/stop/pushCycle/clearLootFeed/reset.
- `src/store/adminConfigStore.ts` — `professionStatOverrides`,
  `professionFeedOverrides` (+actions/getters/export/import/reset).
- `src/data/balance/professions.ts` — `PROFESSION_FEEDS` + `foraging → instinct/resourcefulness`.
- `src/domain/attributes/characterAttributes.ts` — оживлён `professionBonus`
  (дефолт 0 %; включается админ-override `percentPerLevel`/`percentCap`).
- `src/core/offlineCalc.ts` — оффлайн «Сбора» через `rollOfflineForaging`
  (только базовый предмет + XP, без мобов/редких/x2), с общими рейтами.
- `src/features/professions/ForagingPage.tsx` — переписан render-only «Сетка зон»:
  карточки зон, статы профессии, живая лента находок.
- `src/features/admin/AdminProfessionsPanel.tsx` — новая вкладка **«Профессии»**
  (`/admin/professions`): рейты профессий, доступность профессий, характеристики
  профессий, «Профессия → персонаж». Цены продажи НЕ редактируются здесь.
- `src/features/admin/AdminSettingsPanel.tsx` — приведено к «Экономика» +
  «Доступность боя»; профессионные рейты/тумблеры перенесены в «Профессии».
- `src/App.tsx` — зарегистрирован роут `/admin/professions` (был 404).
- `src/core/skillRegistry.ts`, `src/components/GlobalActiveBar.tsx`,
  `src/store/gameStore.ts` — переведены на новый API «Сбора» (старый
  `FORAGING_ACTIONS_MAP`/`processForagingAction` удалён из использования).
- Предметный каталог `src/domain/items/catalog/` (foraging: wood/fungi/bits/special)
  + `getItem()`/`getItemVisual()` — фундамент дропа.

**Проверки:**
- `pnpm typecheck` — чисто.
- `pnpm --filter @workspace/my-app build` — успешно.
- `pnpm test:pillars` — 36/36.
- Headless Chromium (канон `scripts/qa/setup-browser.mjs`) — `/foraging`,
  `/admin/professions`, `/admin/settings`, `/admin` открываются; `pageerror: 0`,
  `failed request: 0`, `console error/warn: 0`. Живой цикл: старт «Лесные заросли» →
  прогресс, `+18 XP`, в ленту пришёл предмет, уведомление показалось.

**Важно:** удалённая ветка уже содержала старый отклонённый «Сбор» (4 участка);
новое дерево v2 оставлено через merge-коммит `-s ours` (история сохранена, дерево — v2).

**Точечный рефакторинг UI (после отзыва владельца):**
- Убран дублирующий прогресс-бар (страница больше не рисует свой
  `ActionProgressBar` поверх глобального).
- Блок «Характеристики профессии» убран из постоянной сетки и перенесён
  в инфо-окно «О профессии» (кнопка у заголовка участков).
- Карточки зон переработаны: тёмные плитки с чипами дропа (%),
  опасность — только иконками мобов (шанс встречи в шапке), кнопка
  «Начать сбор»/«Остановить», заблокированные зоны затемнены.
- Поправлены показательные цифры и тексты; добавлены i18n-ключи.
- Проверено headless Chromium: мобильный 390×844 и десктоп 1280×900 —
  `pageerror: 0`, console/request ошибок нет.

**Важно (механика push):** после merge-ours случайно потерялась часть
файлов в `HEAD`; рабочие файлы возвращены отдельным коммитом
`3a885d4` (дерево рабочей копии = источник истины). Ключевые файлы
Foraging v2 теперь точно в `HEAD`, рабочий индекс чистый.

**Следующее:** ждать теста владельца в Replit на `arena/01a07841-idle-test`;
правки по фидбеку — в ту же ветку. Не мержить без «мержи».

### Сессия 22 — 2026-09-06 — Тексты героя, вывеска, порядок в репо

**Ветка:** `arena/01a07240-idle-test` (не смёржена; `main` — предок `591696e`).

**Сделано в этой ветке за сессию (и сразу до неё):**
- Карточки тела/нитей/рас: `ruleRu` вместо аллегорий; неподключённое честно «не действует».
- Вывеска больше не отпускает пустой хаб: пока шкала на экране декодируем столпы,
  12 веток, иконки вкладок, герб, лица известных героев (`lib/bootPreload.ts`).
  Каталог предметов не ждём. Cap ~5 с. Нити/экип — фон.
- Документы приведены к ветке: `NEXT_CHAT_HANDOFF.md`, `ARCHITECTURE.md`,
  шапки STAGE3/4/5, README, ROADMAP, DEVLOG.
- Сняты ~39 МБ мокапов `design/niti/` (Пульт уже в игре) и черновое ТЗ Melvor
  из `attached_assets/`. Иконки предметов **не** трогали — это следующая работа.
- Починен импорт `EQUIP_SLOT_ICON` / `SLOT_FRAME` в `bootPreload.ts`.

**Main vs ветка:** SQL `profiles`/`characters` тот же. Diff облачного кода —
пути импортов и `lastSavedAt`. Мигратор JSON поднимет старый `save_data`.
Агент в боевой аккаунт не входит.

**Мерж:** по команде владельца «мержи» — в `main`.
**Не сделано в этом чате:** привязка 800+ иконок; новая схема Supabase; тики XP.

**Уроки:** вывеска, которая ждёт только себя, — дырка, не «задумка».
Документы с чужой веткой в шапке вреднее отсутствия документов.

### Сессия 21 — 2026-09-04 — Окно «Экип» (перенос веток) + единый цвет ячеек «каштан»

**Перенос прошлой работы.** Ветка сессии из прошлых чатов (`arena/01a067ac`)
не была смержена и не попала в main — на ней висела вся доработка окна
«Экип» после 5B: редизайн вкладки «Экип» (манекен по центру, колонки
слотов 2×7 слева/справа, мини-сумка, 3 набора экипировки, сравнение,
блок бонусов), векторные силуэты слотов (`EquipSlotSilhouette`),
апскейл всех 30 манекенов до 1024 и полировка арта. Промежуточные ветки
`arena/01a063ea/01a06659/01a067ac` строгие потомки main — перенесены в
нашу ветку `arena/01a06c40` fast-forward'ом (без потери истории),
работа продолжена поверх.

**Фидбек владельца по ячейкам.** Старые слоты: тёмно-коричневые
(`#3d2010`) с бронзовой рамкой 2px в инвентаре и светлые пергаментные
рамки-картинки (WebP `slot_parchment_*`) в «Экип» — не подошли. Владелец
выбрал из 4 визуализированных вариантов цвет дерева **«каштан»**
(`#845d36 → #6e4727`).

**Сделано с ячейками (инвентарь + Экип единообразно):**
- Все слоты теперь плоские, **без рамки** (`border: none`), единый цвет
  дерева через CSS-переменные `--slot-wood` / `--slot-wood-hover` /
  `--slot-wood-locked` и мягкую внутреннюю тень `--shadow-slot-wood`
  (эффект «утопленности в панель» без канта).
- Убран «кружок» в центре пустых ячеек инвентаря (был в `SquircleSlot`).
- Убрана пергаментная текстура `slot_parchment_empty.webp` с пустых
  ячеек мини-сумки/инвентаря.
- Занятые ячейки показывают предмет **векторным силуэтом**
  (`EquipSlotSilhouette`, цвет `#f0d6ab`) прямо на дереве — раньше
  рисовалась растровая WebP-иконка с запечённым светлым кремовым
  квадратом-подложкой, который выглядел как «вторая рамка». Затронуты
  `SquircleSlot` (инвентарь) и слоты/сумка окна «Экип».
- Редкость читается яркой точкой в углу (палитра обновлена под светлый
  фон дерева: зелёный/синий/фиолет/золото/красный), тёмные рамки-канты
  редкости убраны.
- Состояния выбора/совместимости в «Экип» оставлены золотым/синим
  свечением (без рамки).
- Из репозитория удалён случайно закоммиченный каталог `qa_inspect/`
  (15 скриншотов-скретчей); он и `.shots/` добавлены в `.gitignore`.

**Цветовое решение (палитра дерева ячеек):**
- `--slot-wood: linear-gradient(160deg,#845d36,#6e4727)` — базовый каштан;
- `--slot-wood-hover: linear-gradient(160deg,#926a3f,#7c5230)` — наведение;
- `--slot-wood-locked: linear-gradient(160deg,#4a3320,#38251a)` — 14-й слот;
- силуэт предмета `#f0d6ab`, точка редкости — яркая.

**Проверка:** `pnpm typecheck` чистый; headless-QA (`scripts/qa`) снял
инвентарь и «Экип» на мобилке и десктопе — слоты плоские каштановые,
силуэты читаются, рамок и кружков нет, стили едины на обоих экранах.

### Сессия 20 — 2026-09-02 — 30 манекенов по аватарам (арт 5B)

Продолжение PR #11. Задача владельца: 30 манекенов (по 6 на расу) по
их аватарам, расовые особенности переносить полностью (эльфийские уши,
оркские клыки, гномьи бороды, звериные уши и хвост). Рост у всех рас
одинаковый — решение владельца; расы различаются комплекцией.

**Сделано:** конвейер `art_masters/paper_dolls/tools/make_body.py`
доведён до полного цикла (снять фон → канон 384² → приглушить фиолет →
подогнать тон кожи под лицо аватара → автопроверка фиолета → метрики →
листы). Собраны все 30 кадров: люди утверждены и легли в ассеты, эльфы,
гномы, орки и зверолюди ждут вердикта владельца по листам.

Комплекция вышла различимой при одинаковом росте: люди 22514, эльфы
+2%, орки +32%, гномы +46% по массе силуэта.

**Найдено и вылечено:** `orc_female_01` светилась сиреневым (18.7%
пикселей). Причина — в нашем же коде: тело вышло желтее лица, и подгон
тона усилил синий канал на 13% по всему кадру. Проверка фиолета
встроена в конвейер и стоит **после** подгонки; она же нашла два
пропущенных кадра у серокожих эльфов.

**Остановлено владельцем:** правка посадки ладоней на семи кадрах.
Две попытки (править весь кадр; вырезать кисть и вклеивать) не дали
приемлемого результата — модель либо растаскивает руки в стороны, либо
рисует кисть крупнее. Решение владельца: оставляем как есть. Всё
откачено, скрипт `hand_fix.py` оставлен в репозитории на будущее.

**Документация:** `art_masters/paper_dolls/HANDOFF.md` — канон,
устройство конвейера, таблицы по всем тридцати, три открытых вопроса к
владельцу, 13 граблей. `public/_review/bodies2/metrics.md` — рабочий
журнал замеров.

**Уроки/грабли:**
- Подгонка тона кожи сама разводит фиолет — проверять долю пикселей
  после неё, а не до.
- Медиана цвета кожи брак не ловит: у orc_female_01 она совпадала с
  аватаром один в один при 18.7% сиреневых пикселей.
- Асимметрия меряется по зонам: перекос в голове — причёска, в тазу и
  ногах у зверолюдей — хвост, и то и другое не брак.
- Перегенерация ради одной метрики почти всегда портит кадр: сначала
  проверять саму метрику.
- Лимит генерации — 10 кадров за ход, партии планировать по ≤10.
- Сброс песочницы стирает системные пакеты: numpy/scipy ставить заново
  (`pip install --break-system-packages numpy scipy`).

### Сессия 19 — 2026-09-02 — Перенос ветки, паутина пассивок, чистая эмблема
**Ветка/PR:** `arena/01a06167-idle-test` → PR #11 (открыт)

**Сделано:**
- Работа Сессии 18 перенесена из `arena/01a05550-idle-test` (PR #10) в ветку этой
  сессии: 36 коммитов целиком, PR #10 закрыт со ссылкой на #11. Причина —
  предыдущий чат оборвался, пушить в старую ветку было нельзя.
- Починен `pnpm typecheck`: последний коммит Сессии 18 оставил лишний параметр
  `raceId` в `HeroDetailModal` (caller его не передавал, внутри он не использовался).
- `scripts/qa/hero.mjs` — снимки окна героя без облака: сид (регистрация + герой
  30 уровня с вложенными очками) и прогон вкладок Тело/Экип/Нити/Путь на мобилке
  и десктопе. Прямой `/hero` не проходит — пока герой не выбран, дорога ведёт на
  выбор персонажа, поэтому сценарий сначала заходит в игру.
- **Паутина пассивок** по решениям владельца (опрос в чате):
  • луч = ветвь + две глубинные пассивки → **36 узлов** вместо 12;
  • ранг узла **1–3** (`NODE_RANK_CAP` в `src/data/balance/pillars.ts` вместо
    заглушки 999);
  • лестница: следующий узел луча открывается, когда предыдущий выкачан до конца;
  • эффекты пассивок **пока не подключены** — в окне узла честно написано.
  Код: `DEEP_PASSIVES`, `PASSIVES_BY_BRANCH`, `rayNodes`, `passiveRanks` в
  `save_data` (мигратор добирает нули для старых сейвов), `nodeRank`,
  `previousNode`, `isNodeUnlocked`, `nodeBlockReason`. Доска рисует три кольца и
  дороги по лучу; запертые узлы — пунктирная рамка с точкой.
- **Эмблема центра доски**: сначала убраны кольцо, подложка и золотое свечение
  (CSS, эмблема приглушена до 0.55), затем обнаружено главное — сама картинка
  `emblem_axe_quill.webp` была **непрозрачной** (фон `#281205`, 69% кадра) и
  читалась тёмной плитой. Сделан мастер `emblem_axe_quill.png` с альфой
  (flood-fill по краям, допуск 4% — тёмный контур знака цел), рантайм `.webp`
  пересобран конвейером `scripts/assets/optimize.mjs` (1 файл из 995).

**Проверки:** `pnpm typecheck` (libs + app + scripts) — чисто; `test:pillars` — 15/15
(добавлены тесты лестницы, потолка ранга и миграции `passiveRanks`); снимки
`hero.mjs` — окно героя открывается, все вкладки на кадрах; DOM-аудит: 40 узлов
(4 столпа + 36), 23 запертых, 36 дорог, переполнений нет.

**Чистка:** удалён мёртвый `scripts/in-game-screenshots/` (замещён `scripts/qa` —
скрипт ссылался на несуществующий `/home/user/scratch_axe/…`); из `DEVLOG.md`
выброшен обрезанный дубль «Сессии 1» и битый символ в строке про PR #1.

**Уроки/грабли:**
- Агент не видит картинки (нет зрения в сессии) → любая визуальная правка
  проверяется либо замерами (яркость/геометрия через ImageMagick и DOM), либо
  глазами владельца. Прежде чем «убрать фон», проверь альфу: `alpha extract`.
- Владелец обновляет Replit кнопкой Pull на `main`, поэтому ветку смотрит через
  shell: `git fetch origin && git checkout <ветка>`.

**Следующий шаг:** владелец смотрит экран «Герой → Тело» в Replit; правки —
строго по одной задаче, без параллельных работ.

### Сессия 18 — 2026-08-31…2026-09-02 — Хотфикс создания героя + 5A + 5B
**Ветка/PR:** `arena/01a05550-idle-test` → PR #10 (закрыт, работа перенесена в #11)

**Сделано (ветка не была смёржена — чат оборвался):**
- Хотфикс дороги: вход без героя → reload → создание без ошибки; «Сменить
  аккаунт» на правилах/создании/выборе; проверка ника через RPC больше не валит
  первого героя; кэш правил привязан к `userId`; аварийный экран по-русски.
- **5A**: типы 4 столпов / 12 ветвей, матрица рас вместо `StatKey`, versioned
  `attributes` в `save_data` + мигратор, калькулятор + тесты, каркас
  `src/data/balance/` (XP-кривая, % профессий, репутация, энергия — заглушки).
- **5B**: хаб героя у зеркала ложи (`HeroHubPage`), доска тела (`HeroBoard`,
  компас столпов с панорамой и зумом), компактные карточки столпов, кукла
  экипировки, 2 бесплатных respec, вкладки Тело/Экип/Нити/Путь, сигнальные нити.
- Арт: плоская эмблема «Топор и Перо» для центра доски (`emblem_axe_quill`),
  реестр иконок `src/data/attributeIcons.ts`.

**Где остановились:** последний коммит `ff00cb4` оставил сломанный typecheck,
`DEVLOG.md` и `ROADMAP.md` обновлены не были — доделано в Сессии 19.


### Сессия 17 — 2026-08-31 — Контракт Этапа 5 + мерж PR #9
**Ветка / PR:** `arena/01a0526e-idle-test` → PR #9 → `main`

Владелец доработал идею «Четыре Столпа», затем команда «маржу». 5A в этом чате нет.

**Закон смысла (не код):** нет ярлыка класса; профессии = ремёсла с бонусами;
энергия вместо маны (бой да, домашний крафт нет); репутация ≠ rebirth;
offline: еда → 1 отдых → город без сознания, −15–25% лута этой ночи;
очки: старт 0; +1 столп/уровень; +1 ветвь / 5 ур.; 1 бесплатный respec;
выход без еды — предупреждение, не блок. Паутина — позже. Цифры — `src/data/balance/`.

Канон: `STAGE5_FOUR_PILLARS_HANDOFF.md`. Тесты: `scripts/qa/tour.mjs` + README/ACCEPTANCE.

**Следующий чат:** спросить владельца, ок ли дорога в Replit после `git pull` на `main`.
5A — только если дорога отличная.

### Сессия 16 — 2026-08-30 — Конвейер картинок (PR #9)
**Ветка / PR:** `arena/01a0526e-idle-test` → PR #9 (открыт, НЕ смёржен)

**Вердикт владельца (ноутбук, Replit, ветка PR):** дорога «работает шикарно»;
аватарки при создании героя грузились долго.

**Причина:** 994 иконки — PNG 1024×1024, ~312 МБ, avg ~314 КБ. В UI кружок 48px
и слот 56px качали полный мастер. Разовая конвертация аватарок недостаточна:
впереди 800+ предметов.

**Система (закон на все следующие этапы):**
- мастер PNG остаётся;
- рантайм — соседний `.webp` (аватар 384px, прочие иконки 256px);
- код: `iconUrl(...)` / `getAvatarPath(...)`, никогда `.png` в `<img>`;
- скрипт: `node scripts/assets/optimize.mjs` (повтор пропускает свежие);
- канон: `scripts/assets/README.md`, хелпер `src/lib/assetUrl.ts`.

Прогон: 994 WebP, ~6 МБ вместо ~312 МБ. Prefetch облика выбранной расы на create.

**Не сделано:** мерж PR #9; 5A; вынос PNG-мастеров из `public/` в `art_masters/`.

### Сессия 15 — 2026-08-30 — Мок без облака, полный тур, подготовка мержа PR #9
**Ветка / PR:** `arena/01a0526e-idle-test` → PR #9 (открыт, НЕ смёржен)

**Контракт:** показать всю дорогу кадрами (холодный 0→игра и возвращение того же
Каеля). Тест агента **без Supabase**. Не мержить. 5A не начинать. Секреты в чат нет.

**Сделано:**
- `src/lib/qaMock.ts` — auth+CRUD в localStorage; гейт: localhost+флаг, либо DEV
  без `VITE_SUPABASE_URL`, либо `VITE_QA_MOCK=1`. Replit с ключами не трогает.
- Auth: после signIn/signUp — `loadCharacters` затем `navigate('/rules')`.
- Router: mismatch `loadedUserId` → форма auth, не пустой `#root`.
- `scripts/qa/tour.mjs` — холодный 01–17 + returning 18–19, 21–23. Exit 0.
- Документы приёмки: `scripts/qa/ACCEPTANCE.md`, обновлён `NEXT_CHAT_HANDOFF.md`.

**Превью Arena:** владелец видел белый экран / connection reset. Пробовали:
выключить HMR/WebSocket, статическую сборку, убрать Google Fonts (блокировали CSS).
Не помогло. **Не ретраить.** Тест владельца — только Replit. Побочный эффект:
Cinzel/Inter больше не грузятся с fonts.googleapis.com (системный fallback).

**Не ретраить в коде:** returning+`dropSession`+логин → 20с пустой `#root` на `/rules`;
`enableQaMock({reset})` на каждый document (стирает БД на Vite reload).

**Не сделано:** мерж PR #9; live QA владельца в Replit; 5A; починка Arena-превью;
возврат webfonts (спросить после QA).

**Следующий чат:** строго чат проверки. Тур → ждать Replit → мерж только по команде.
5A в новом чате после мержа.

### Сессия 14 — 2026-08-30 — Hotfix вывески (PR #9) + прогон дороги + QA-скрипты
**Ветка / PR:** `arena/01a0526e-idle-test` → PR #9 (открыт, НЕ смёржен)

**Контракт владельца:** экран «Aethelia / Загрузка...» убрать насовсем;
единственные загрузки — вывеска и акт 0 «ЗНАК»; они держатся, пока
сессия и персонажи не готовы. `minDisplayTimeMs=4000` не менять без согласования.

**Патча `QA_HOTFIX_auth_loading.patch` в дереве не было** (сессия PR #8 запечатана).
Hotfix собран по контракту.

**Баги, из‑за которых auth «не закрывался» (найдены прогоном дороги):**
- повторный `loadCharacters` после заставки снова ставил `loading=true` → пустой кадр;
- CSS `story-scene--finale` висел на любом последнем бите (`isLast`), ложа вспыхивала как дверь;
- акт 0 считал арты готовыми по первой картинке;
- Router монтировался раньше очереди «Знакомый скрип вывески.» → вспышка auth.

**Сделано:** проп `authReady` у SplashScreen и акта 0; `loadedUserId` + inflight
в characterStore; finale только по флагу `finale`; прогрев всех артов пролога;
очередь возвращения до монтирования Router.

**Прогон агентом:** JSDOM-бандл (холодный 0–6, возвращение, гость) и живой Chromium
`node scripts/qa/road.mjs` (~25с) — 0 «Aethelia / Загрузка...». Канон браузера:
`@sparticuz/chromium@131.0.1` + `puppeteer-core@23.11.1` (`setup-browser.mjs`),
не Playwright/apt. Latest `@sparticuz` 149 ломает CJS require.

**Постоянные скрипты (чтобы не править «тест» каждый чат):**
- `scripts/qa/setup-browser.mjs` — установка Chromium в `/home/user/ui_shot`;
- `scripts/qa/road.mjs` — дорога;
- `scripts/qa/auth.mjs login|register` — учётка из `QA_EMAIL`/`QA_PASSWORD` / `.env.local`.

**Не сделано:** мерж PR #9; live QA владельца в Replit; 5A; смена 4000ms.

### Сессия 13 — 2026-08-30 — Live-QA фиксы дороги + слой 1 адаптива + мерж PR #8
**Ветка / PR:** `arena/01a051db-idle-test` → PR #8 (смёржен merge-коммитом по команде владельца)

**Вердикт live QA дороги (Replit, владелец):** арт «Тракт» прыгал/перезагружался;
вывеска в городе «незаконченная» (без надписи); жёлтый кружок слева от кнопки
«Войти в таверну»; после заставки вместо auth вечный «Aethelia / Загрузка...»;
акт 0 — «прямоугольник» арта + размытая подложка.

**Сделано (каждый пункт — отдельный коммит):**
- duplicate React keys в StoryScene (слои enter и текст делили `key={beat.id}`)
  → арт путника стабилен, консоль чистая;
- точки прогресса — блочный ряд над кнопкой; у одно-битных сцен не рендерятся
  → «жёлтый кружок» исчез;
- фраза возвращения → «Знакомый скрип вывески.» (решение владельца);
- зависание после заставки: `withTimeout` в utils (10с restoreSession, 12с
  fetchCharacters) + кнопка «Повторить» на экране загрузки через 12с;
  воспроизведено сценарием «протухшая сессия + зависший refresh» (до фикса
  экран вис 24с+, после — auth за ~10с);
- `IMG_4927.png` удалён;
- надпись «ТОПОР И ПЕРО» на вывеске в городе: гибрид AI-варианта только в зоне
  доски (0.85% пикселей, остальное пиксельно нетронуто) — одобрено владельцем
  по листу ревью;
- акт 0 «ЗНАК»: арт стал полноэкранным загрузочным экраном (cover, вариант
  wide/tall/square через `pickSplashArt`; `index.html` уже предзагружает);
  размытая подложка удалена. `intro_sign.webp` остался неиспользуемым —
  удаление по слову владельца;
- слой 1 адаптива (`RESPONSIVE_SYSTEM_PLAN.md`): шкала токенов в `:root`
  (`--text-*`, `--space-*`, `--radius-*`, `--control-*`, все clamp) + auth
  целиком на токенах; 3 размерных медиа-блока → 1 структурный (<1024 стек /
  ≥1024 композиция). Проверено скриптом на 13 вьюпортах: overflowX=0,
  minFont≥13px, пересечений нет; листы и GIF до/после.

**Проверено агентом:** полный прогон дороги скриптом (холодный старт 390×844,
возвращение, ложа, выход героя, гость, reduced-motion) — 0 ошибок консоли,
0 переполнений, все заголовки/действия на местах, диффы «путника» плавные;
typecheck + production build чисто. Художественную оценку даёт владелец
по контактным листам и Replit.

**Мерж PR #8:** по команде владельца («давай мержить все, чтобы я протестировал»).
Следующий чат ПЕРВЫМ делом уточняет у владельца, прошла ли проверку вся
предыдущая работа; затем — фундамент 5A строго по `STAGE5_FOUR_PILLARS_HANDOFF.md`.

### Сессия 12 — 2026-08-30 — Кинематографическая дорога (акты 0–7) + план адаптива
**Ветка / PR:** `arena/01a04eba-idle-test` → PR #7 (открыт, НЕ смёржен)

**Задача владельца:** вся дорога первого запуска — как работа мастера киноэффектов,
торжественно-медленный темп, каждый арт живой; видео каждого акта ДО wiring,
одобрение каждого акта владельцем. Плюс фиксы по live QA Stage 4.

**Сделано (каждый акт — отдельный коммит, все одобрены владельцем по видео):**
- iOS-фиксы: маршруты не монтируются под заставкой (autofill), инпуты 16px на touch
  (автозум Safari), заставка 4с + прогрев четырёх артов дороги.
- Акт 0 «ЗНАК»: `intro_sign.webp` фулскрин (одобренный арт 2), свет разгорается как
  угли, «ЭТЕЛИЯ» tracking-in, уход в bloom. Закон: арт НИКОГДА не обрезается —
  `contain` + размытая заливка краёв тем же артом (жалоба владельца на обрезанное
  перо/рукоять = урок на всю дорогу).
- Акт 1 «КОНТИНЕНТ» (dawn): облака-полосы, дыхание солнца, туман; непрерывная
  панорама бит 1→2 (object-position 2.6с), crossfade артов, mist-in.
- Акт 2 «ТРАКТ»: ритм шага (sway), пыль дороги; FIX движка: settle-эффект не
  перезапускался на кэшированных артах → арт навсегда невидим (deps `[imageReady, beat?.image]`).
- Акт 3 «ГОРОД»: уличное свечение, дымы, бродячие огни, наезд камеры, glow-in.
- Акт 4 «ПОРОГ»: дыхание очага, финальный толчок+вспышка; ПОРЯДОК (решение владельца):
  после «Войти в таверну» игрок сразу в auth — вывеска-заставка ТОЛЬКО для повторных визитов.
- Акт 5 «ТАВЕРНА»: возвращён бит «Дверь узнаёт тебя» (ENTRANCE_RETURNING) для
  повторных визитов; хореография входа комнат онбординга.
- Акт 6 «ЛОЖА»: лампа дышит у реального источника света арта (35% 62%), луч из окна.
- Акт 7 «ВЫХОД»: утро героя — свет дышит у распахнутой двери (60% 45%), туман у земли;
  первый шаг = наезд + финал; возвращение героя встречает тот же рассвет.
- FIX финала: толчок+вспышка были на ЛЮБОМ последнем бите (isLast) — теперь явный
  флаг `finale` только на реальных входах; ложа заканчивается тихо.
- FIX очереди: `handleSplashLoaded` не затирает уже стоящую сцену (guard).

**Процесс:** видео (GIF) каждого акта с живого приложения до wiring; владелец
утверждал каждый акт;Ник героя в акте 7 интерполируется в реальном потоке (в песочнице пусто — там нет Supabase).

**Параллельно (по требованию владельца, до мержа):** план централизации адаптива —
`RESPONSIVE_SYSTEM_PLAN.md`. Решения владельца: слоёная система (шкала токенов →
два эталонных кадра 1440×900 + 360×800 → `--art-focus` артов → QA-матрица 13 вьюпортов
скриптом); ретрофит — новые экраны + худшие старые по одному (первый auth); тайминг —
после QA дороги. Ничего не внедрено.

**Мерж PR #7:** выполнен merge-коммитом по прямой команде владельца («пора тестить,
делай маржу») — владелец решил тестировать дорогу в Replit ПОСЛЕ мержа. Возможные
правки по итогам QA — следующая сессия, маленькими изолированными коммитами
(в новой arena-ветке, PR заново). Открытый вопрос по тексту: бит возвращения
«Дверь узнаёт тебя.» владелец назвал спорной фразой — варианты замены предложены
(«С возвращением, путник.» / «Знакомый скрип вывески.» / «Огонь всё ещё горит.»),
решение после QA. В `main` владельцем приложен `artifacts/my-app/IMG_4927.png`
(фото-вложение; назначение уточнить у владельца при необходимости).

### Сессия 11 — 2026-08-29 — Merge Stage 4 и подготовка следующего чата
**Ветка / PR:** `arena/01a04e4b-idle-test` → PR #6 → `main`

**Перед merge зафиксировано:**
- `NEXT_CHAT_HANDOFF.md` — самостоятельная стартовая точка следующего чата: что
  смёржено, какие файлы читать, что нельзя переписывать, порядок Stage 5 и точная
  инструкция владельца не спешить, спрашивать при неопределённости и делать работу
  малыми проверяемыми шагами.
- `DEVLOG.md`, `ROADMAP.md`, Stage 4 и Stage 5 handoff приведены к состоянию после
  merge: Этап 4 завершён кодом, owner live QA всё ещё обязателен; Stage 5 утверждён
  только как контракт, без преждевременной реализации.
- PR #5 не мержится отдельно: его девять патчей уже перенесены в PR #6 с patch-identity
  1:1. После merge PR #6 он закрывается как `superseded`.

**Дисциплина следующего этапа:** не пытаться реализовать «Четыре Столпа» целиком за
один заход. Сначала QA Stage 4, затем изолированный фундамент 5A с миграцией и тестами;
все непрояснённые цифры/правила — только после вопроса владельцу.

### Сессия 10 — 2026-08-29 — Этап 5 «Четыре Столпа»: контракт принят
**Ветка:** `arena/01a04e4b-idle-test`

**Зафиксировано по решению владельца:**
- Следующий большой этап — **Этап 5 «Четыре Столпа»**, а не прежняя отдельная
  задача визуальной доводки Sidebar/MobileNav.
- Четыре характеристики: Стойкость, Мощь, Сноровка, Чутьё; у каждой ровно три
  ветви, всего 12. Их общая логика связывает active RPG и idle/offline.
- Шесть ролей называются **специализациями**, чтобы не смешивать их с ремёслами
  (лесорубство, рыбалка, кузница и др.).
- Утверждены: diminishing-return кривая, 6 видимых пороговых синергий, матрица
  рас «2 плюса + 1 минус» в процентах столпов, отсутствие штрафа Сноровке и
  5 расовых/6 специализационных пассивов.
- Создан полный технический handoff `STAGE5_FOUR_PILLARS_HANDOFF.md`: данные,
  формулы, миграция character save, UI, phased implementation, Definition of Done
  и открытые числовые решения, которые нельзя самовольно выбирать в коде.
- `ROADMAP.md` обновлён: Stage 5 заменён на эту систему; старый sidebar/mobile
  follow-up сохранён отдельной технической задачей, не потерян.

**Важно следующему чату:** PR #6 смёржен и включает весь Stage 4 с cinematic onboarding.
Сначала завершить owner live QA, затем читать handoff Stage 5 перед первым изменением
кода. Не оставлять временные расовые `StatKey`-бонусы действующими параллельно с новой матрицей.

### Сессия 9 — 2026-08-29 — Цельный cinematic onboarding Aethelia
**Ветка:** `arena/01a04e4b-idle-test`

**Сделано:**
- Утверждённые кадры безопасно переведены в компактные production WebP и подключены как
  `character_creation_lodge.webp`, `cutscene_tavern_entrance.webp`,
  `cutscene_character_departure.webp`.
- Добавлены `CinematicDirector`, `CinematicScene` и `cinematicState`: после splash
  незалогиненный путник один раз за вкладку видит короткий вход в таверну, а после
  успешного создания/выбора героя — короткий выход в город. Сцену можно пропустить
  тапом/кликом/Escape/Enter/Space; `prefers-reduced-motion` не рисует даже один кадр
  и сразу продолжает маршрут.
- `OnboardingScene` объединил атмосферные background/UI-слои: правила в общей зале,
  двухшаговый create в правой зоне обжитой ложи, выбор героя в левой зоне у выхода.
  Добавлены тёплый свет, пылинки, glass-панели, safe-area и mobile/reduced-motion ветки.
- Ник аккаунта удалён из registration: имя теперь появляется только у героя. Первый герой
  корректно берёт authenticated `userId`, а departure ставится в очередь лишь после
  успешного сохранения героя/выбора.
- Модалка «Что нового» теперь ждёт playable-состояния и окончания перебивки; добавлена
  пользовательская версия `0.2.0 «Первый шаг»`.
- Очищены неиспользуемые runtime/черновые арты: legacy `splash.png`, `reserved.png`,
  review-исходники и неиспользуемая пара `aethelia_seal_clean.*`. Обновлены asset README,
  README проекта, ROADMAP и Stage 4 handoff; метаданные страницы переименованы из `My App`.

**Проверено агентом:**
- `corepack pnpm --dir artifacts/my-app typecheck` — чисто.
- `corepack pnpm --dir artifacts/my-app build` — успешно, 2247 modules transformed.
- Dev server и маршруты `/`, `/login`, `/register`, `/rules`, `/create-character`,
  `/select-character` возвращают HTTP 200; новые WebP отдаются как `image/webp`.
- Визуально просмотрены исходные кадры и representative mobile-crops 390×844:
  вход сохраняет герб/двери/камин, ложа — камин и барда, выход — свободный проём и город.
  Полный browser screenshot-тест в sandbox не выполнен: установленного браузера нет,
  а скачивание Chromium оборвалось сетевым `ECONNRESET`. Нужен финальный Replit/iPhone QA.

**Ожидается от владельца:** пройти оба маршрута в Replit и сообщить, если требуется
изменить crop, плотность панели, длительность или текст конкретной сцены. Не мержить в
`main` до этого подтверждения.

### Сессия 7 — 2026-08-29 — Согласование Этапа 4 (персонаж) + контракт реализации
**Ветка:** `arena/01a04ceb-idle-test`

**Обсуждено и согласовано (без кода) — Этап 4 «создание персонажа»:**
- **Один персонаж на аккаунт.** Создание нового жёстко удаляет старого (перс + инвентарь).
  Это убирает лазейку «оффлайн-фарм за всех» и мультипликатор экономики.
- **Раса + аватар (двухступенчатый выбор):** сначала раса (1 из 5), потом **ровно 6
  аватаров этой расы**; перевыбор расы на лету. Бонусы расы — структура «2+ / 1−»
  (конкретика настраивается позже, сейчас влияния на формулы нет).
- **Никнейм = персонажу**, не аккаунту; глобально уникален (RPC `is_nickname_taken`).
  Поле «Имя» из регистрации убираем.
- **Аккаунт и персонаж — в РАЗНЫХ таблицах** (я (интро-шаг).
- **Melvor → Aethelia** — переименовать ВСЁ и сразу, без миграции ключа сейва.

**Написано:** `STAGE4_CHARACTER_HANDOFF.md` (полный контракт), обновлены README/ROADMAP.
Правки в самом коде НЕ начинались (по договорённости — сначала контракт).

### Сессия 8 — 2026-08-29 — Этап 4: реализация кода персонажа (онбординг, выбор, перс в игре, сейвы)
**Ветка:** `arena/01a04ceb-idle-test`

**Сделано (код Этапа 4):**
- `src/data/characters.ts` — 5 рас, бонусы строго «2+ / 1−» (значения черновые), 30 аватаров
  (`{race}/{gender}_{NN}.png` в `avatars/`), хелперы `getAvatarPath`/`getAvatarsForRace`/`raceIdFromAvatar`.
  Исправлены подпапки аватаров: `humans/elves/dwarves/orcs/beastfolk` (не совпадают с raceId).
- `src/data/rules.ts` — `RULES_VERSION='1.0.0'`, блоки правил, локальный кэш принятия.
- `src/lib/characterApi.ts` — CRUD `characters`, `is_nickname_taken` (RPC), облачные сейвы,
  `setSelectedCharacter`, мягкое удаление; поля `profiles` (role/donate/rules/selected).
- `src/store/characterStore.ts` — load/select/create/rename/avatar/delete; один перс + мягкое
  удаление старого при создании; `resetGameToFresh` при создании (общие старт-статы).
- Онбординг: `RulesPage` (галочка принять), `CreateCharacterPage` (раса → 6 аватаров → ник,
  двойной confirm при замене), `SelectCharacterPage` (карточки + время в игре, always_select).
- Перс в игре: Dashboard (Герой), Sidebar (карточка перса), Settings (секция «Персонаж»:
  смена ника/аватара 1× бесплатно, смена/удаление с двойным confirm).
- Сейвы 3-уровневые: `characterSave.ts` (reconcile «кто новее» + push в облако ~3 мин +
  pagehide/visibilitychange), локальный 30с остаётся на `saveManager`.
- `authStore.acceptRules` + профили; роутинг в `App.tsx` с гейтингом правил и персонажа.
- `pnpm typecheck` + `pnpm build` — чисто.

**Отложено/осознанно не сделано:** lz-string (опционально), «Что нового» как отдельный
онбординг-шаг (используется существующая модалка Changelog), кинопревью/интро (deferred),
полная система характеристик/профессий/скиллов (в следующие этапы), трата донат-валюты
при смене ника/аватара во 2-й раз (заглушка, система доната позже).

**Ожидается:** тест владельцем в Replit (найти VITE_SUPABASE_URL/ANON_KEY в Secrets, схему
`SUPABASE_STAGE4.sql` уже вставил). Далее — команда «мержи».

### Сессия 6 — 2026-08-29 — Мобильная доводка auth-экрана (login/register) + разбор «Load failed»
**Ветка:** `arena/01a04ceb-idle-test`

**Диагноз «Load failed» при регистрации — РЕШЁН владельцем:**
- Причина была НЕ в коде и НЕ в отсутствии SQL. Опечатка в `VITE_SUPABASE_URL`:
  владелец вставил `https://dnygpoldmtrhpspacipt.supabase.comm` — лишняя буква `m` в конце.
  После исправления на `https://dnygpoldmtrhpspacipt.supabase.co` регистрация работает.
- Симптом «Load failed» на iPhone/WebView — это сетевой сбой запроса к Supabase
  (не наш текст; в коде такой строки нет). Это полезный маркер для будущих
  диагностик: если «Supabase не настроен» предупреждения нет, а «Load failed» есть —
  значит переменные подхватились, но URL/key неверные или проект недоступен.

**Что проверено владельцем:** регистрация (email/password) считается завершённой.

**Что осталось проверить (по STAGE3 handoff):**
- session restore после перезагрузки (не выкидывает на /login);
- guest mode: лесорубство+рыбалка, 24 слота, блок остального, баннер «Зарегистрируйся...»;
- sign out из Sidebar и Settings;
- Google OAuth (после настройки Google provider в Supabase).

**Сделано (по фидбеку владельца из скриншотов на iPhone):**
- Переработана мобильная раскладка авторизации: вместо жёстких абсолютных координат
  (`.auth-card { top: 30.75rem }` и т.п.) теперь flex-центрирование по вертикали/горизонтали
  с `justify-content: safe center` — контент централизован и не требует прокрутки вниз,
  а при переполнении аккуратно уходит наверх (без обрезки формы).
- Поля ввода/кнопки на мобильных сделаны аккуратными и читаемыми: поднят контраст фона
  и рамки поля, увеличен размер шрифта инпута, подсветка фокуса, аккуратные радиусы.
- **Кнопки вместо текстовых ссылок:** «Войти» (primary) → «Создать аккаунт» (secondary) →
  разделитель «или» → «Google» → внизу мелким текстом «Войти гостем».
  На регистрации: «Создать» (primary) → «Уже есть аккаунт? Войти» (secondary), гость не показывается.
- Добавлены `describeAuthError()` и логирование ошибок API в консоль (для диагностики сетевых сбоев).
- Проверки: `corepack pnpm --dir artifacts/my-app typecheck` — чисто; `build` — чисто.

**Что НЕ сделано (намеренно):**
- Этап 4 (создание персонажа) — не начинался; аватары и уникальность никнейма отложены.
- SQL `profiles`/`is_nickname_taken` — будут в Этапе 4 + `SUPABASE_SETUP.md`.

### Сессия 5 — 2026-08-29 — Этап 3: Supabase foundation + AuthGate + guest mode (готово, смёржено)
**Ветка/PR:** `arena/01a04c15-idle-test` → PR #4.

**Сделано:**
- Установлен `@supabase/supabase-js`.
- `src/lib/supabase.ts` — frontend-safe клиент (только `VITE_SUPABASE_URL` + `VITE_SUPABASE_ANON_KEY`), graceful missing-config state, `service_role` не используется.
- `src/store/authStore.ts` — `session/user/profile/loading/isGuest`, `restoreSession`, `signIn`, `signUp`, `signInWithGoogle`, `signOut`, `continueAsGuest`, подписка `onAuthStateChange`.
- `AuthPage` подключён к реальным формам: email/password, регистрация, Google OAuth, guest-вход, валидация и сообщения об ошибках/подтверждении.
- `App.tsx` — AuthGate: незалогиненный → `/login`, гость → limited game shell, зарегистрированный → full shell; session restore при старте.
- Guest mode в `src/lib/guestMode.ts` и `src/store/*`:
  - прогресс в `sessionStorage` (изменён `saveManager.ts`);
  - доступны лесорубство + рыбалка + 24 слота;
  - заблокированы бой, крафт/cooking/firemaking/smithing, горное дело, расширение инвентаря;
  - баннер «Зарегистрируйся, чтобы сохранить прогресс.» в Dashboard/Sidebar/MobileNav/Inventory/Settings.
- `Sidebar`/`MobileNav` скрывают недоступные гостю разделы; в Sidebar/Settings добавлены статус аккаунта и sign out.
- `vite.config.ts` — отдельный vendor-chunk для Supabase; build чистый.
- Добавлен `SUPABASE_SETUP.md` — инструкция по подключению в Replit (URL + anon key, email/Google provider, таблица `profiles`).
- `.gitignore` — исключены `.env`, `.env.local`, `.env.*.local`.

**Проверки:**
- `corepack pnpm --dir artifacts/my-app typecheck` — успешно.
- `corepack pnpm --dir artifacts/my-app build` — успешно, без Vite warnings.

**Что НЕ сделано (намеренно):**
- Cloud save / профиль прогресса через Supabase (следующий этап).
- Google provider может требовать настройки в Supabase (код готов, реальность зависит от владельца).
- Этап 4 (создание персонажа).

**Следующий шаг после мержа:**
- Новый чат начинает с тестирования в Replit: `/login`, `/register`, guest-режим, session restore, Google OAuth, sign out.
- Затем Этап 4: создание персонажа (nickname, аватар из `icons/characters/heroes/`, стартовые характеристики, переход в игру).

### Сессия 4 — 2026-08-29 — Этап 3: визуальный auth-shell Login/Register (внедрён, Supabase ещё нет)
**Ветка:** `arena/01a049b7-idle-test`

**Сделано:**
- После серии визуальных итераций утверждён auth background: тёплая таверна у камина, герб Aethelia с топором+пером над камином, bard, elf+dwarf, правая часть без пустой заглушки.
- Approved background оптимизирован и добавлен в приложение: `public/assets/art/auth_tavern_background.webp`.
- Clean-печать сохранена отдельным asset-файлом: `public/assets/icons/ui/auth/aethelia_seal_clean.webp` / `public/assets/icons/ui/auth/aethelia_seal_clean.png`; в маленькую auth-форму её не ставить, т.к. на малом размере выглядит шумно/криво.
- Добавлена страница `src/pages/AuthPage.tsx` с визуальными режимами login/register.
- `App.tsx`: routes `/auth`, `/login`, `/register` выводят fullscreen auth-screen без sidebar/topnav.
- `index.css`: добавлены стили Stage 3 visual auth — прозрачный frameless glass/tint, компактные поля/кнопки, desktop/mobile адаптив, fire glow, embers/dust, soft entrance, reduced-motion.
- Mobile-правка владельца учтена: поля и кнопки короче и меньше, auth поднят выше, фокус crop на камине/гербе.
- Временные review-мокапи удалены перед коммитом; подробный handoff оставлен в `STAGE3_AUTH_HANDOFF.md`.

**Проверки:**
- `corepack pnpm --dir artifacts/my-app typecheck` — успешно.
- `corepack pnpm --dir artifacts/my-app build` — успешно, без Vite warnings после тех-правки `tooltip.tsx`/`vite.config.ts`.
- Dev preview поднимался командой `corepack pnpm --dir artifacts/my-app dev --host 0.0.0.0`; маршруты `/login` и `/register` отдавали HTML 200.

**Что ещё НЕ сделано:**
- Supabase клиент/пакет не добавлен.
- `authStore`, session restore, Google OAuth, sign out, AuthGate и guest restrictions ещё не реализованы.

**Следующий шаг:**
Сначала владелец проверяет live-визуал `/login` и `/register`; после подтверждения — Supabase foundation по `STAGE3_AUTH_HANDOFF.md` и `ROADMAP.md`.

### Сессия 3 — 2026-08-28 — Этап 2: финальная полировка SplashScreen (завершён)
**Ветка:** `arena/01a049b7-idle-test`

**Сделано:**
- По фидбеку владельца доработана ключевая анимация заставки: теперь спокойно и осторожно качается именно центральная вывеска/щит на цепях, а не весь фон таверны.
- `artEngine.ts`: добавлены `SignMotionConfig` и `LightBloomConfig`; слой вывески вырезается мягкой маской, рисуется отдельно от статичного фона, под ним создаётся clean-plate, чтобы не было двойного силуэта при микропокачивании.
- `artRegistry.ts`: для `splash_wide.webp`, `splash_tall.webp`, `splash_square.webp` заданы отдельные области вывески, pivot-точки и одинаково спокойные параметры движения (амплитуда < 1°, период ~8.2 сек).
- Добавлены локальные пульсации света от фонаря, камина и свечей; старые эффекты (пыль/искорки, виньетка, тёплое мерцание, fade-in/fade-out) сохранены.
- `SplashScreen.tsx`: в `AnimatedArt` передаются параметры `signMotion`/`lightBlooms`; исправлена готовность арта — прогресс считает арт завершённым только после реального decode/load, а не просто по известному режиму.
- `index.css`: мягкая полировка HUD заставки — reveal арта, появление нижнего блока, shimmer прогресс-бара, дыхание процентов/подсказки, fallback-анимация герба; учтён `prefers-reduced-motion`.
- Временные review-скриншоты/контакт-листы удалены из рабочего дерева, чтобы не тащить мусор в репозиторий.

**Проверки:**
- `pnpm typecheck` — чисто.
- `pnpm --filter ./artifacts/my-app build` — успешно; остались только старые предупреждения Vite про sourcemap/chunk size.

**Следующий шаг:**
ЭТАП 3 — Авторизация + Регистрация на Supabase: клиент, authStore, Login/Register на gameUI, protected routes, гостевой режим с ограничениями.

### Сессия 2 — 2026-08-28 — Этап 2: SplashScreen (завершён)
**Ветка/PR:** `arena/01a0494e-idle-test` → PR #2 (смёржен в main)

**Сделано:**
- `WhatsNewModal.tsx` — модалка «Что нового» на gameUI (GModal/GBadge/GButton/GDivider);
  показывается после fade-out заставки при непросмотренных версиях changelog;
  по закрытию `markChangelogSeen()`. Подключена в `App.tsx`.
- SplashScreen: реальный прогресс загрузки (шрифты `document.fonts.ready` + арт + мин. время
  ~2.6 сек) вместо фейкового таймера; переведён на CSS-переменные (правило №3);
  версия из `CURRENT_VERSION` (changelog.ts).
- Полноэкранный арт: 3 варианта сцены таверны — `splash_wide.webp` (16:9, десктоп),
  `splash_tall.webp` (9:16, телефон), `splash_square.webp`; выбор через `pickSplashArt(w,h)`
  в `artRegistry.ts`; artEngine получил режим `fit:'cover'` (запас 4% под покачивание).
- Оптимизация: WebP ~110 КБ вместо PNG 2.4 МБ (~20× быстрее загрузка);
  preload арта в `index.html` до исполнения JS-бандла.
- Фиксы typecheck: дубликат ключа `amber` в `ActionProgressBar.tsx`;
  `addSkillXp` → `addXp` в `offlineProgress.ts`. `pnpm typecheck` чистый.
- Фикс «двух загрузочных экранов»: фолбэк-герб теперь только при реальной ошибке
  загрузки арта (ошибка/таймаут 1.5с), пока арт в пути — нейтральный тёмный фон,
  арт проявляется через fadeIn 0.5s. (Перенесён владельцем руками через Replit,
  т.к. сессия была запечатана после мержа PR #2 — отсюда и родилось правило №6.)
- В `index.css` добавлены keyframes `fadeIn`/`slideUp` (их ожидал GModal).

**Уроки/грабли:**
- Мерж PR закрывает GitHub-доступ сессии → мержить только в самом конце чата.
- Превью Arena на iPhone не работает (заглушка) → тестируем через Replit Webview.
- Туннели наружу из песочницы агента невозможны (сеть закрыта, кроме GitHub).

**Как проверить модалку «Что нового» повторно:**
`localStorage.removeItem('aethelia_last_seen_version'); location.reload();`

### Сессия 1 — Этапы 0–1 (до ведения DEVLOG)
- Базовый геймплей (6 навыков + бой), тик-менеджер, оффлайн-прогресс, стиль Wooden Tavern.
- Этап 1: `src/shared/ui/gameUI.tsx` — все G-примитивы (GPanel, GButton, GInput, GModal,
  GBadge, GAvatar, GProgressBar, GDivider, GTooltip, GCard, GTag, GEmptyState и др.).
- `src/data/changelog.ts` — типы, цвета, версия 0.1.0 «Рождение мира», утилиты
  getUnseenChangelog/markChangelogSeen.
- PR #1: canvas-движок artEngine (режимы sign/sigil/scene/cutout) + splash.png.
- В `index.css` добавлены keyframes `fadeIn`/`slideUp` (их ожидал GModal).

**Уроки/грабли:**
- Мерж PR закрывает GitHub-доступ сессии → мержить только в самом конце чата.
- Превью Arena на iPhone не работает (заглушка) → тестируем через Replit Webview.
- Туннели наружу из песочницы агента невозможны (сеть закрыта, кроме GitHub).

**Как проверить модалку «Что нового» повторно:**
`localStorage.removeItem('aethelia_last_seen_version'); location.reload();`
