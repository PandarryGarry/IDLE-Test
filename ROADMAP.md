# 🗺️ Aethelia RPG — Дорожная карта разработки

> Документ обновляется по мере выполнения этапов.
> Каждый шаг согласовывается перед реализацией.
> Разработчик уточняет детали — не самовольничает.

---

## ✅ Уже сделано

- [x] Базовый геймплей: лесорубство, горное дело, рыбалка, кулинария, кузница, огонь, бой
- [x] Тик-менеджер (requestAnimationFrame)
- [x] Оффлайн-прогресс (visibilitychange + beforeunload + saveOnLeave)
- [x] Оффлайн-сводка на Dashboard
- [x] Автовозобновление навыка при загрузке
- [x] UI стиль "Wooden Tavern" — палитра, токены, CSS-переменные
- [x] SkillCard с круговым прогресс-кольцом 270° и badge в разрыве
- [x] TopNavBar компактный (лого + язык + сохранить)
- [x] Dashboard: Герой (3 ячейки) + оффлайн-сводка
- [x] 994 иконки в репозитории + конвейер WebP (`scripts/assets/`, `iconUrl()`)
- [x] THEME_GUIDE.md — документация по токенам
- [x] SplashScreen: адаптивный WebP-арт, реальный прогресс, fade-out, «Что нового», живая вывеска и свечной свет
- [x] Этап 3 auth foundation: Supabase клиент, authStore, AuthPage на реальных формах, Google OAuth, AuthGate, guest mode
- [x] Supabase подключение описано в `SUPABASE_SETUP.md`
- [x] Кинематографическая дорога первого запуска 0–7 + фиксы iOS + сброс устройства (PR #7)
- [x] QA-фиксы дороги по live-вердикту владельца + акт 0 полноэкранным артом (PR #8)
- [x] Слой 1 адаптива: единая шкала токенов + proof-of-concept на auth (PR #8)
- [x] QA-скрипты агента в репо: `scripts/qa/` (Chromium `@sparticuz`, `tour.mjs` без облака, дорога, login/register)

---

## 📋 ПЛАН — 8 этапов

---

### ЭТАП 1: UI Foundation — Централизация компонентов
**Цель:** Создать единый файл gameUI.tsx со всеми переиспользуемыми примитивами.
Это фундамент — без него каждый экран будет "сборной солянкой".

**Что войдёт в gameUI.tsx:**
- `GButton` — кнопка (primary / secondary / danger / ghost)
- `GInput` — поле ввода (с иконкой, валидацией)
- `GModal` — модальное окно (с оверлеем, анимацией)
- `GCard` — базовая карточка (с вариантами)
- `GBadge` — значок (уровень, редкость, статус)
- `GAvatar` — аватар персонажа (круглый, с рамкой редкости)
- `GProgressBar` — полоса прогресса (XP, HP, Mana)
- `GDivider` — разделитель секций (с заголовком по центру)
- `GTooltip` — всплывающая подсказка
- `GPanel` — панель/блок (стандартный контейнер)

**Правило:** Ни один новый экран не создаётся без использования этих компонентов.

**Вопросы перед стартом:** нет, всё ясно.

---

### ✅ ЭТАП 2: SplashScreen — Загрузочный экран (завершён)
**Цель:** Красивый первый экран в стиле Wooden Tavern.

**Статус:** ✅ завершён. Финальная версия: адаптивный полноэкранный WebP-арт, реальный прогресс загрузки, fade-out, «Что нового» после заставки, отдельное спокойное покачивание вывески/щита на цепях, локальное дыхание света от фонарей/камина/свечей и мягкая полировка HUD.

**Что должно быть:**
- Герб / логотип Aethelia (анимированный, с свечением)
- Название игры крупным шрифтом Cinzel
- Полоса загрузки в нашем стиле (золотая)
- Случайный совет/лор внизу (средневековый стиль)
- Плавный fade-out при готовности
- **"Что нового"** — показывается ПОСЛЕ загрузки, если есть непросмотренные обновления

**Система changelog:**
- Файл: `src/data/changelog.ts` ✅ создан
- Правила: только важное (новые механики, контент, баланс, важные фиксы)
- НЕ пишем: визуальные правки, рефакторинг, мелочи
- Тип изменений: 🆕 new | ⚡ improve | ⚖️ balance | 🔧 fix | 🎉 event
- Отображение на Dashboard — в будущем (после Этапа 4)

**Вопросы перед стартом:** нет.

---

### ЭТАП 3: Авторизация + Регистрация (Supabase)
**Решено:** Supabase (email/пароль + Google OAuth)

**Статус:** 🟢 функциональный auth-фундамент готов; ожидает теста владельцем в Replit. Cloud-save/profile-логика — не в этом этапе, перенесена в последующие этапы.

**Уже сделано — visual foundation:**
- [x] Утверждён фон auth: живая тёплая таверна у камина, герб Aethelia с топором+пером, bard, elf+dwarf, правая часть без пустой заглушки.
- [x] Runtime asset: `public/assets/art/auth_tavern_background.webp`.
- [x] Неиспользуемая clean-печать очищена из runtime assets; герб уже читается в утверждённом фоне таверны.
- [x] `src/pages/AuthPage.tsx` — визуальная страница login/register.
- [x] Routes `/auth`, `/login`, `/register` открываются отдельным fullscreen auth-screen без sidebar/topnav.
- [x] CSS: frameless transparent glass/tint, компактные поля/кнопки, desktop/mobile адаптив, fire glow, embers/dust, reduced-motion.
- [x] Mobile: фокус на камине/гербе, auth поднят выше, поля и кнопки короче/меньше.

**Важно по дизайну:**
- Не делать общую декоративную рамку вокруг auth-интерфейса.
- Не делать рамку вокруг auth-panel; максимум лёгкий transparent tint/blur.
- Не закрывать прекрасный фон тяжёлой панелью.
- На mobile поля/кнопки должны быть короче, не во всю ширину.
- Google-кнопка коротко: `Google`.
- Не добавлять отдельную маленькую печать внутрь формы: герб уже читается в утверждённой сцене таверны.
- Подробности и rejected-направления: `STAGE3_AUTH_HANDOFF.md`.

**Что сделано — авторизация:**
- [x] Supabase-проект создан владельцем; Replit Secrets: `VITE_SUPABASE_URL` + `VITE_SUPABASE_ANON_KEY` (инструкция в `SUPABASE_SETUP.md`).
- [x] Установлен `@supabase/supabase-js`.
- [x] `src/lib/supabase.ts` — клиент + graceful missing-config state.
- [x] `src/store/authStore.ts` — `session/user/profile/loading/isGuest`; `restoreSession/signIn/signUp/signInWithGoogle/signOut/continueAsGuest`.
- [x] email/password login/register в `AuthPage`.
- [x] Google OAuth `signInWithGoogle`.
- [x] AuthGate в `App.tsx`: незалогиненный → `/login`, гость → limited shell, registered → full shell.
- [x] Session restore при старте и подписка на `onAuthStateChange`.
- [x] Sign out в Sidebar и Settings.
- [x] Guest mode: sessionStorage, лесорубство+рыбалка+24 слота, блок бой/крафт/mining/расширение инвентаря, баннер «Зарегистрируйся, чтобы сохранить прогресс.».
- [x] `vite.config.ts`: отдельный vendor-chunk для Supabase, чистый build.

**Проверить (перед следующим этапом):**
- [x] Мобильная доводка auth: централизация полей/кнопок, читаемые поля, кнопки вместо текста, порядок кнопок «Войти → Создать аккаунт → или → Google → гостем».
- [ ] В Replit: `/login`, `/register` (email/password).
- [ ] Вход/регистрация работают при реальных Supabase-ключах.
- [ ] Session restore: перезагрузка страницы не выкидывает на `/login`.
- [ ] Google OAuth (после настройки Google provider в Supabase).
- [ ] Guest mode: только лесорубство+рыбалка, 24 слота, блок остальных разделов, баннер «Зарегистрируйся, чтобы сохранить прогресс.».
- [ ] Sign out из Sidebar и Settings.

**Решено:**
- Supabase проект создаём новый (нужны URL + ANON_KEY после создания)
- "Войти как гость" — ДА, с ограничениями:
  - Прогресс только до закрытия браузера (sessionStorage)
  - Доступны: 2 навыка (лесорубство + рыбалка), базовый инвентарь (24 слота)
  - Нельзя: бой, крафт, расширение инвентаря, сохранение в облако
  - Баннер "Зарегистрируйся, чтобы сохранить прогресс."

---

### ЭТАП 4: Создание персонажа
**Статус:** ✅ смёржено в `main`: PR #6 (герой, правила, выбор, 3-уровневые сейвы, cinematic onboarding) → PR #7 (кинематографическая дорога акты 0–7: ЗНАК → КОНТИНЕНТ → ТРАКТ → ГОРОД → ПОРОГ → ТАВЕРНА → ЛОЖА → ВЫХОД; фиксы iOS; план адаптива) → PR #8 (QA-фиксы дороги по live-вердикту владельца: стабильный арт путника, жёлтый кружок у кнопки, фраза возвращения «Знакомый скрип вывески.», таймауты против вечного «Загрузка...», надпись «ТОПОР И ПЕРО» на вывеске, акт 0 без размытой подложки; слой 1 адаптива с PoC на auth). Владелец тестирует всё в Replit ПОСЛЕ мержа PR #8.

**Решено:** Один персонаж на аккаунт, раса + аватар, ник = персонажу, без классов.

**Что нужно:**
1. Онбординг: заставка → короткий вход в таверну → регистрация (без «Имя») → правила → создание героя в ложе → выход в город → «Что нового» → игра.
2. **Выбор персонажа** — кинематографичный экран на фоне выхода в город: карточка аватар+ник+раса+общее время, кнопки и мягкий переход в игру.
3. **Создание персонажа** — двухступенчатый выбор: **раса (1 из 5)** → **6 аватаров этой расы** → **ник** (глобально уникален, RPC).
4. Раса = 5 (human/elf/dwarf/orc/beastfolk); бонусы расы — **структура «2+ / 1−»** (конкретика настраивается позже, на формулы не влияет).
5. Анимированный переход в игру после создания; «Что нового» — после онбординга.

**Данные (две таблицы — аккаунт и персонаж раздельно):**

`profiles` (аккаунт): `role` (`user|admin`), `rules_accepted_at`, `rules_version`,
`donate_currency` (донат-валюта НА АККАУНТЕ), `selected_character_id`.

`characters` (персонаж): `id`, `user_id` (FK→profiles.id), `nickname` (глобально уникален),
`avatar_id`, `race_id`, `save_data` (jsonb), `last_saved_at`, `created_at`,
`has_changed_nickname/avatar`, `is_deleted` + `deleted_at` (мягкое удаление, TTL 1 мес).

**Решено:**
- Никнейм и аватар — **на персонажа**; смена 1 раз бесплатно (в настройках), дальше — за донат-валюту.
- **Донат-валюта на аккаунте**, не на персонаже: при удалении персонажа не теряется.
- Создание нового перса **жёстко удаляет** старого (перс + инвентарь); защита — двойной confirm из Settings.
- Характеристики/профессии/скиллы — РАЗНЫЕ системы (полноценные — в следующие этапы; в Этап 4 только каркас+раса+время).
- Сейвы трёхуровневые: память → локальный (~30с) → облако (~3 мин + pagehide).
- **Melvor → Aethelia** — переименовать всё и сразу (без миграции ключа сейва).

**Проверить после реализации:**
- [x] `/register` → правила → создание персонажа (раса → аватары → ник) → игра
- [x] Возврат: выбор персонажа → «Что нового» → игра (Choice-экран показывается на каждом входе)
- [x] «Создать нового», удаление перса (двойной confirm), смена ника/аватара (1× бесплатно)
- [x] Guest: НЕ попадает в создание персонажа (играет в ограниченном шелле)
- [ ] Оффлайн: единственному персу, при холодном старте (облако vs локальный, ожидает тест)

**Файлы Этапа 4 (новые):**
`src/data/characters.ts` (расы/аватары/бонусы «2+/1−»), `src/data/rules.ts`,
`src/lib/characterApi.ts` (CRUD + RPC), `src/lib/characterSave.ts` (облачный мост),
`src/store/characterStore.ts`, `src/pages/RulesPage.tsx`, `src/pages/CreateCharacterPage.tsx`,
`src/pages/SelectCharacterPage.tsx`.

---

### ЭТАП 5: «Четыре Столпа» — характеристики, ветви и специализации
**Статус:** 📜 дизайн-контракт утверждён; фундамент 5A — только в чате **после**
мержа PR #9 и «ок» владельца в Replit. Чат проверки PR #9 5A не начинает.

**Цель:** Единая понятная система, связывающая RPG-бой и idle/offline-прогресс:
**Стойкость** (выживание), **Мощь** (урон), **Сноровка** (скорость), **Чутьё** (удача и мир).

**Зафиксировано:**
- Ровно 4 верхнеуровневых столпа и 12 ветвей: по 3 у каждого.
- Шесть ролей называются **специализациями**, а не профессиями: ремёсла игры остаются
  лесорубством, рыбалкой, кузницей и т. д.
- Расы получают окончательную матрицу «2 бонуса + 1 штраф» в процентах столпов;
  Сноровка намеренно не имеет расового штрафа.
- Шесть видимых перекрёстных синергий, diminishing-return формула
  `(1 + итоговый_столп / 100) ^ 0.75` и правила безопасных cap-ов.
- Новая система мигрирует старые character saves без потери героя/инвентаря/прогресса
  и не переписывает работающий Stage 4.
- Порядок работ: data + миграция → UI/очки/специализации → combat/offline интеграция → баланс и QA.

**Полный контракт:** [`STAGE5_FOUR_PILLARS_HANDOFF.md`](./STAGE5_FOUR_PILLARS_HANDOFF.md).
Перед кодом отдельно подтверждаются источник очков, caps/стоимость, момент выбора и смены
специализации, offline-смерть/золотой тик и mapping старых combat skills/prayers.

---

### ЭТАП 6: Инвентарь — Полная переработка
**Цель:** Функциональный, красивый инвентарь с реальными иконками.

**Что сделаем:**
- Ячейки 56×56px, стиль как в референсе (тёмное дерево с рамкой)
- Фильтры: Всё / Оружие / Броня / Еда / Ресурсы / Разное
- Сортировка: по типу, редкости, количеству
- По клику на предмет — попап с полным описанием
- Drag & Drop для экипировки (опционально)
- Стопки предметов с анимацией при добавлении

---

### ЭТАП 7: Система предметов — "Пощупать предмет"
**Цель:** Каждый предмет — живой, ощутимый, с историей.

**Структура предмета (расширить items.ts):**
```ts
interface Item {
  id: string
  name: string              // "Дубовое бревно"
  description: string       // "Крепкое бревно из старого дуба..."
  lore: string              // Атмосферный текст
  category: ItemCategory
  rarity: Rarity
  weight: number            // "Вес" в инвентаре
  iconPath: string          // iconUrl('weapons/bow/t07') — WebP, не PNG
  stackable: boolean
  maxStack: number
  sellValue: number
  stats?: ItemStats         // для экипировки
  craftingInfo?: string     // откуда берётся
}
```

**Попап предмета:**
- Большая иконка слева (WebP через `iconUrl`, мастер PNG не грузить)
- Название + редкость (цветная рамка)
- Описание + лор-текст курсивом
- Характеристики (если есть)
- Кнопки: Экипировать / Использовать / Продать / Выбросить

**Привязка иконок (закон конвейера — `scripts/assets/README.md`):**
- В данных: `iconPath: iconUrl('weapons/bow/t07')` — всегда WebP, не сырой PNG.
- Новый файл: положить мастер-PNG → `node scripts/assets/optimize.mjs`.
- Дерево → `iconUrl('materials/wood/log_t01')` …
- Руда → `iconUrl('materials/metals/ore_t01')` …
- Рыба → `iconUrl('materials/food/fish/fish_01_raw')` …
- Оружие → `iconUrl('weapons/sword_1h/t01')` …
- Броня → `iconUrl('armor/plate/t01/chest')` …

---

### ЭТАП 8: Контент — Наполнение мира
**Цель:** Сделать мир живым и интересным.

**Что войдёт:**
- 12 тиров для каждого типа оружия/брони — полная привязка иконок
- Уникальные предметы (из weapons/unique/) — с особыми именами
- Монстры с лором и дропом (расширить monsters.ts)
- Система ремёсел (что из чего крафтится — визуально)
- Квесты / задания (базовая система)

---

## 🔧 Технические задачи (параллельно)

- [ ] UI follow-up Sidebar + MobileNav: шапка с аватаром/ником/боевым уровнем, иконка+уровень навыка,
  подсветка активного навыка; mobile tabs «Главная / Бой / Профессии / Инвентарь / Профиль» без дублей.
- [ ] Убрать `bankStore` (дублирует `inventoryStore`) — рефакторинг
- [ ] Переименовать все "Bank" → "Inventory" в коде
- [ ] Типизировать `items.ts` полностью (сейчас частично `any`)
- [ ] Добавить `ErrorBoundary` на каждый route
- [x] Конвейер картинок: мастер PNG → рантайм WebP, `iconUrl()`, `scripts/assets/optimize.mjs`
- [ ] Вынести PNG-мастера из `public/` в `art_masters/` (рантайм не меняется)
- [ ] PWA-манифест (иконка, название, offline-работа)

---

## ⚠️ Правила работы

1. **Перед каждым этапом** — уточняю детали у тебя
2. **Картинки** — не скачиваю в воркспейс, работаю через GitHub API
3. **Хардкод** — запрещён. Только токены и CSS-переменные
4. **Один шаг за раз** — не прыгаю вперёд
5. **Тестирование** — скриншот после каждого изменения
6. **Коммиты** — после каждого завершённого шага

---

## 📅 Текущий статус

**Завершены: Этапы 1–4 (PR #6), дорога 0–7 (PR #7), QA-фиксы + слой 1 (PR #8) — в `main`.
PR #9 открыт (hotfix вывески + QA-мок + тур + конвейер WebP-иконок) — НЕ смёржен.
Владелец на ноутбуке: дорога ок; медленные аватарки починены. Мерж — по слову «мержи».
5A — чат после мержа. Картинки: `scripts/assets/README.md`. Превью Arena не использовать.**

**Что уже сделано в Этапе 4:** Melvor→Aethelia (везде), схема Supabase (`SUPABASE_STAGE4.sql`),
онбординг (правила → раса → 6 аватаров → ник → игра), экран выбора персонажа
(cards + время в игре, always_select), перс в игре (Dashboard/Sidebar/Settings),
смена ника/аватара (1× бесплатно), удаление перса (двойной confirm), сейвы на персонажа
(локально 30с + облако ~3 мин/pagehide, reconcile «кто новее»). Добавлены утверждённые
арт-сцены входа/ложи/выхода, короткие пропускаемые перебивки и правильный момент показа
существующей модалки «Что нового» — после выбора героя и завершения сцены. lz-string остаётся опциональным.

Для Supabase нужны `VITE_SUPABASE_URL` и `VITE_SUPABASE_ANON_KEY` через Replit Secrets / `.env.local` (секреты в чат не вставлять).
