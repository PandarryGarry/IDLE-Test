# 🕸️ NITI_WINDOW_DESIGN.md — экран «Нити»: анализ и журнал выбора

> **Статус: СДЕЛАНО.** В игре — «Пульт» 3×N (`HeroHubPage` → SynergiesModule).
> Ниже — журнал раундов. PNG-мокапы `design/niti/` сняты из репозитория
> (2026-09-06): источник правды — код, не картинки поиска.

---

## 0. Что имеем сейчас

`HeroHubPage.tsx → SynergiesModule`: сетка 2 колонки (3 на ≥900px) кнопок
`.hero-thread` — иконка, имя, бейдж «горит/спит», две строки требований с
метрической полосой. Клик → `GModal` с описанием и эффектом.

Чего не хватает:
- статус читается только свечением рамки — нет явной печати/ранга;
- нет численного «чего не хватает» — главной мотивации idle-игры;
- нет рангов (обычная / высшая нить);
- визуально никак не связано со столпами, хотя нити — это именно связи столпов;
- на 390px две колонки тесноваты, модалка разрывает поток.

---

## 1. Анализ ДАННЫХ: нити — это тетраэдр

Проверил все 6 синергий (`src/data/synergies.ts`) против 4 столпов:

| Нить | Пара столпов | Пороги |
|---|---|---|
| Крепкий удар | Стойкость + Мощь | 50 / 30 |
| Неудержимый | Мощь + Сноровка | 50 / 30 |
| Быстрый глаз | Сноровка + Чутьё | 50 / 30 |
| Живучий везунчик | Стойкость + Чутьё | 50 / 30 |
| Мастер темпа | Сноровка + Стойкость | **70 / 40** |
| Разрушитель | Мощь + Чутьё | **70 / 40** |

**Это ровно все 6 неупорядоченных пар из 4 столпов — полный граф K4
(тетраэдр): ни одной лишней, ни одной пропущенной.** Значит граф-визуализация
для «Нитей» не приблизительна, а математически точна.

Второй факт: 4 нити с порогами 50/30 и 2 нити с 70/40 → естественный
**двухранговый металл** («нить» / «высшая»), по аналогии с бронза→серебро→
золото у трейтов Teamfight Tactics.

Третий факт (важен для выбора): пороги независимы, «пути обхода» нет.
См. вывод R4 ниже.

---

## 2. Что говорят современные MMO и смежные игры

1. **Teamfight Tactics, Trait Tracker** (Riot, разбор UI-дизайнером Zach
   Roberson): трейты видны **всегда**, без кликов; тиры активируются как
   бронза/серебро/золото/ирридесцент; иконки трейтов намеренно простые —
   «читаются в очень малом размере»; у каждого трейта своя цветовая схема для
   мгновенного различения.
2. **Auto Chess vs TFT**: сила интерфейса — показывать МНОГО информации сразу,
   «without needing to do extra clicking around».
3. **Albion Online, Destiny Board**: огромный граф — «a hot mess»; игроки
   открывают его посмотреть ОДНО поддерево в ~90% случаев; предложение
   dev-команды и сообщества — кликабельный категоризированный
   **список/таблица** вместо графа. Граф оправдан только там, где важен
   ПУТЬ обхода.
4. **Path of Exile**: веб-дерево оправдано именно потому, что путь = билд;
   ориентация держится на кластерах и **цветовой кодировке атрибутов**
   (red/green/blue) — цвет столпа как сквозной код.
5. **Дизайн skill-tree (Game Dev 101)**: читаемость и группировка; тематичность
   («дерево должно чувствовать себя частью мира»); фидбек подсветкой связанных
   узлов; явный показ требований/замков; не cram-ить информацию в узел;
   тестировать на целевом экране (у нас — iPhone, «ТВ-тест» наоборот).
6. **Мобильный фидбек по MMO-интерфейсам**: окна не должны перекрывать центр и
   требовать двух рук; крупные тап-цели; вертикальный скролл одной рукой.

### Синтез: 5 правил для наших «Нитей»

- **R1.** Сводка (сколько горит + текущие значения столпов) — всегда на
  экране, без кликов.
- **R2.** Статус нити явный: печать / металл / свечение, а не только рамка.
- **R3.** Численное «чего не хватает» — главная мотивация idle-прогресса.
- **R4.** Граф уместен как МЕТАФОРА и «вау», но не как основная навигация:
  пути обхода у нас нет (пороги независимы) → рабочая лошадка список/сетка.
- **R5.** Цвет столпа — сквозной код (иконка + число + полоса), как RGB-
  кодировка PoE.

---

## 3. Четыре варианта (мокапы 390px)

### Вариант A — «Тетраэдр / паутина»  ·  `variant-A.png`
4 столпа по углам ромба, 6 нитей — рёбра K4: четыре стороны прямыми, две
диагонали дугами (чтобы узелки не слипались в центре). Горящая нить — золотая
нить между своими столпами; узелок-медальон посередине — тап открывает
карточку эффекта. Шапка-сводка R1 соблюдена.
- ➕ математически точная модель; «вау»; родственно доске «Тело» (тоже граф).
- ➖ узкие тап-цели на 320px; пересечения нитей; дороже в поддержке.

### Вариант B — «Плитки рун» (сетка 2×3)  ·  `variant-B.png`
Урок TFT/Auto Chess: вся информация сразу. Плитка = руна: иконка, имя, два
требования (иконка столпа + полоса + число + ✓), металл ранга, печать
«горит/спит». Шапка-сводка со значениями столпов.
- ➕ читается мгновенно, без кликов; масштабируется на будущие нити; крупные
  тап-цели; легко ложится на существующую сетку.
- ➖ менее «магично»; похоже на инвентарь; 6 плиток = длинновато на 320px.

### Вариант C — «Гобелен» (вертикальный свиток)  ·  `variant-C.png`
Урок Albion: путь не важен → список читается лучше паутины. Строка: медальон
на «ткани», имя + ранг, две вышитые полосы требований, сургучная печать
статуса. Строка раскрывается аккордеоном: описание, эффект, «не хватает: N».
Горящие строки — тёмные с золотой печатью, спящие — каштановое дерево.
- ➕ лучшая читаемость на телефоне; одна рука; явное «чего не хватает»;
  дёшево в поддержке; продолжает язык строк «Пути».
- ➖ длинный скролл; меньше «системности»; не показывает связь со столпами.

### Вариант D — «Колесо судеб» (радиальное)  ·  `variant-D.png`
Урок Diablo IV / WoW: 4 дуги столпов по ободу с капсулами-значениями на
стыках, 6 медальонов нитей на внутреннем кольце (двухцветный штрих = пара
столпов), в центре счётчик. Выбранная нить разворачивается карточкой под
колесом.
- ➕ самый «современный AAA»; фокус на одной нити; красив на планшете/десктопе.
- ➖ центр съедает место на мобиле; одна нить за раз; Capsules режутся, если
  полотно тесное.

---

## 4. Рекомендация агента

**База — C («Гобелен»)**, потому что R1–R3 и R4 он закрывает лучше всех на
целевом экране (iPhone, одна рука), и он дешевле всех в поддержке.
Дополнить его двумя заимствованиями:
- из **A** — декоративная мини-паутина K4 в шапке панели (4 столпа-узла и 6
  нитей-штрихов, без интерактива) как «магия» и связь со столпами (R5);
- из **B** — металл ранга и явная печать статуса в строке (R2).

Получится гибрид **C + орнамент A + печати B**: читаемость списка плюс
фирменная «паутина» в шапке.
Если хочется максимум «вау» и готовы к риску узких тап-целей — брать **A**.
Если в приоритете плотность и будущее расширение списка нитей — **B**.
**D** придержать как идею для планшетной/десктопной раскладки.

---

## 5. Что потребуется при реализации (любой вариант)

1. **Цвета столпов сейчас Нигде нет** — ввести токены: `--pillar-fortitude`,
   `--pillar-might`, `--pillar-finesse`, `--pillar-instinct` в `tokens.ts` →
   `index.css` (закон: никаких хардкод-цветов). В мокапах использованы
   `#6f9fd8 / #d4705c / #62b98a / #b08ad8` — как стартовое предложение.
2. В `SynergyDef` добавить `tier: 1 | 2` (или выводить из порогов: 70/40 = 2)
   и хелпер `missingRequirements(synergy, pillars)` для строки «не хватает».
3. Верстку делать на примитивах `src/shared/ui/gameUI.tsx`; новые классы —
   в `index.css` рядом с блоком `hero-gear2` (5B).
4. Раскрытия/модалки — через существующий `GModal` или инлайн-аккордеон (C).
5. Приёмка: `pnpm typecheck` чистый + QA-снимки `scripts/qa/hero.mjs`
   (вкладка «Нити») на 390 и 1280.

---

## 7. Раунд 2 — современный стиль (вердикт владельца: раунд 1 «слишком старый»)

Владелец: «Все 4 варианта не те, слишком старые, нам нужно чуть современней
и уникальнее стиль. Перегенерируй». Поэтому раунд 1 (§3–§4) оставлен как
история поиска, а ниже — актуальная четвёрка.

### 7.1 Новая визуальная система (общая для всех четырёх)

Отказ от пергамента/дерева/филиграни в пользу современного тёмного стекла:

- база `#0e0b12 → #151020` (глубокий баклажан-чёрный), панели —
  frosted glass `rgba(255,255,255,.045)` с hairline-кромкой
  `rgba(255,255,255,.10)` и золотой `rgba(232,176,75,.28)`;
- акценты: золото `#e8b04b / #ffd98a` + arcane `#59e0c8`;
- цвета столпов (стартовое предложение для токенов):
  Стойкость `#6ea8ff`, Мощь `#ff7a6b`, Сноровка `#4fe0b0`, Чутьё `#c792ff`;
- микро-типографика: mono uppercase с letter-spacing .16–.22em для лейблов,
  serif (Cinzel) только для имён нитей и крупных чисел;
- статус = светящаяся точка + bloom; ранг = алмазный гем; полосы 4px с
  свечением у выполненных и глухим серым у невыполненных;
- никаких рамок-картинок, никакого дерева: только геометрия, свет и стекло.

### 7.2 Четыре современных варианта

- **A · Созвездие** (`concept2-A.png`) — звёздная карта судьбы: туманность,
  столпы-светила с ореолами, горящие нити-лучи с искрой, узелки-медальоны.
  Самый уникальный «вау»-экран.
- **B · Бенто-чипы** (`concept2-B.png`) — трендовая bento-сетка: широкая
  featured-карточка + чипы; вся информация сразу, дуотон-плитки, гемы, точки.
- **C · Лента-таймлайн** (`concept2-C.png`) — вертикальный feed со светящимся
  рельсом и узлами-точками; раскрытие с эффектом и «не хватает».
- **D · Орбитальный радар** (`concept2-D.png`) — HUD-астролябия: тонкие кольца,
  дуги столпов с капсулами, нити-спутники со шлейфами-кометами, центр-ридаут,
  рунические тики по ободу.

Точные wireframe-первоисточники (реальная кириллица, точные метрики):
`modern-A..D.png`; исходник `mockups-modern.html`.

### 7.3 Рекомендация раунда 2

**C · Лента-таймлайн** как основа (читаемость одной рукой, явное «не хватает»,
дешевле всех в поддержке) **+ орнамент-«созвездие» из A в шапке панели**
(мини-карта K4 без интерактива) **+ гемы/точки из B** как язык статусов.
Если владелец хочет максимум витринности — **D** на десктоп/планшет,
**A** как полноэкранный «ритуальный» вид по тапу на шапку.

### 7.4 Заметки по концептам (честно)

Концепты `concept2-*` — AI-финиш поверх точных мокапов: они передают СВЕТ и
материал, но местами вольно обошлись с мелочами (в A «СНОР» → «СКОР»,
аватары-заглушки разные, в B имена чипов обрезаны). Источник правды для
верстки — `modern-*.png` и `mockups-modern.html`, не концепты.

## 9. Раунд 3 — доработки варианта A (выбор владельца: «A за основу»)

Вердикт владельца по раунду 2: «Вариант A лучше, но его нужно сильно
доработать: нитей может быть много + он должен отличаться от нашего в
«Тело». Возьмём A за основу и посмотрим, как доработать».

### 9.1 Два требования и как они закрыты

1. **Масштаб («нитей может быть много»).** В демо во всех четырёх
   доработках показано **10 нитей**: 6 боевых + 4 будущих «скоро»
   (Гнев горна, Танец клинка, Око бури, Клятва крови) — то есть по две
   нити на одну пару столпов. Приёмы масштаба:
   - хорды одной пары разводятся по глубине дуги (не слипаются);
   - фильтр по столпу гасит несмежные нити (A1);
   - ярусы-«небеса» разносят нити по страницам (A2);
   - лента/список растут бесконечно (A3, A4).
2. **Отличие от «Тела».** `HeroBoard` — это pan/zoom-полотно 720×720 с
   радиальными «дорогами» от центрального узла и квадратными узлами.
   Все четыре доработки сознательно **без pan/zoom и без радиальной
   паутины узлов**: фиксированный кадр, хорды-ленты (A1/A2/A4) или
   лента-feed (A3). Пересечения с языком «Тела» исключены.

### 9.2 Четыре доработки

- **A1 · Аккорд-небо** (`concept3-A1.png`, wireframe `a2-A1.png`) — хорды-
  ленты между столпами + ряд фильтр-чипов по столпам; включённый фильтр
  подсвечивает смежные нити и гасит остальные. Карта = инструмент, а не
  декорация.
- **A2 · Небеса-страницы** (`concept3-A2.png`, `a2-A2.png`) — пейджер
  созвездий: Небо I (нити 4), Небо II (высшие 2), Небо III (скоро 4).
  Каждая плита чистая; новые нити = новые небеса, а не каша.
- **A3 · Звёздная лента** (`concept3-A3.png`, `a2-A3.png`) — сигил K4 в
  шапке как фирменный знак + вертикальный feed: мини-хорда (две звезды
  столпов + узелок с иконкой), полоса готовности, % / «готова» / «скоро»,
  гем ранга, точка статуса. Лучшая читаемость и бесконечный масштаб.
- **A4 · Аккорд + лента** (`concept3-A4.png`, `a2-A4.png`) — обзор-хорды
  сверху (только свет и узлы, без подписей и интерактива) + рабочий
  список-сетка снизу с синхронным выбором и карточкой «не хватает».

### 9.3 Рекомендация раунда 3

**A4** как основной экран: обзор-карта даёт ту самую «магию созвездия» и
мгновенное чувство системы, а список ниже закрывает читаемость, тап-цели
и бесконечный масштаб. Фильтр по столпу из A1 переносится в шапку списка
как опция. Если владелец хочет минимум элементов — **A3**; если хочется
ритуальности перелистывания небес — **A2**.

## 11. Раунд 4 — информационная архитектура под ~20 нитей

Вердикт владельца по раунду 3: «Не то. Перегенерируй, более продуманно.
Если будет много нитей, всё должно корректно вмещаться, показываться и
учитываться. Считай минимум на 20 нитей».

### 11.1 Смена парадигмы

Показывать все нити сразу рёбрами на одной карте при 20 нитях невозможно
(каша). Поэтому раунд 4 строит **информационную архитектуру**: группировка,
агрегаты, прогрессивное раскрытие, счётчики, фиксированная высота. Принципы:

- **Р1.** Карта/обзор показывает не нити, а **агрегаты** (пары столпов) со
  счётчиками; нити раскрываются уровнем ниже.
- **Р2.** Везде счётчики: горит / спит / скоро — ни одна нить не теряется.
- **Р3.** Экран не растёт бесконечно: вьюпорт фикс-высоты, аккордеон,
  страницы или таблица.
- **Р4.** Пустая клетка = учтённое место под будущую нить (резерв виден).

Демо-данные раунда 4 — **20 нитей**: 6 пар столпов × ярусы I–IV (пороги
40/25, 70/40, 95/60, 120/80). Реальные наши 6 синергий переложены как
I–II ярусы соответствующих пар; III–IV — демо-заглушки для проверки масштаба.

### 11.2 Четыре варианта

- **R4-A · Атлас нитей** (`concept4-A.png`, wireframe `r4-R4-A.png`) —
  уровень 1: шесть агрегированных «канатов» между столпами, на каждом
  бейдж «всего · горит». Уровень 2: тап по канату раскрывает список нитей
  пары. Карта показывает максимум 6 объектов, списки растут бесконечно.
- **R4-B · Пульт** (`concept4-B.png`, `r4-R4-B.png`) — донат-статус
  (горит/спит/скоро) + значения столпов, фильтры-счётчики, вьюпорт-сетка
  плиток 3×N фикс-высоты с градиентным добором «показано 9 из 20 · листай».
  Высота экрана постоянна.
- **R4-C · Книга нитей** (`concept4-C.png`, `r4-R4-C.png`) — шесть
  глав-пар аккордеоном: открыта одна глава со своими нитями, остальные
  свёрнуты со счётчиком «n/m ✦». Порядок стабилен.
- **R4-D · Матрица судьбы** (`concept4-D.png`, `r4-R4-D.png`) — таблица:
  строки = 6 пар, колонки = ярусы I–IV. Клетка = нить; пустая пунктирная
  клетка = учтённое место под будущую нить. Одним кадром виден весь план
  развития (20 нитей + резерв), статус точкой, ярус цветом кромки.

### 11.3 Рекомендация раунда 4

**R4-A · Атлас** как основной экран: он сохраняет «созвездие» (за что
владелец выбрал A) и решает масштаб через два уровня (канаты → списки).
Донат-статус и счётчики из R4-B — в шапку как общий элемент (переносится
в любой финал). Если владелец хочет полный обзор одним взглядом без
навигации — **R4-D · Матрица**. **R4-C** и **R4-B** — запасные/комбинируемые.

## 12. Раунд 5 — ФИНАЛ: «Пульт» в стиле игры (выбор владельца)

Вердикт владельца: «Магия созвездия не интересует — выбираю лучший внешний
вид и разнообразие. Делаем вариант „Пульт“. И главное: „в нашем стиле“ =
стиль ИГРЫ, её цветовая палитра и общая концепция» (пергамент/дерево/золото
из `index.css`, а не выдуманный стеклянный стиль раунда 2).

### 12.1 Контракт «Пульт»

- Карточка: иконка + название + **одна** полоса с %.
- Тап → модалка: что делает нить, какие столпы нужны, сколько осталось
  повысить до открытия (без CTA-кнопок, только информация).
- Два отсека: **«Активные · N» / «Неактивные · M»**, переключение кнопками;
  активные подсвечены (золотое свечение `--shadow-gold`), неактивные слегка
  затемнены (dimmer + приглушённая иконка).
- Будущие «скоро» — внутри «Неактивных» с фиолетовым бейджем, без полосы.
- Шапка-сводка остаётся: кольцо «гори N» (золото) + «скоро» (фиолет),
  счётчики горит/спит/скоро, значения столпов.
- Одна полоса = готовность по «узкому месту» (min из двух порогов).
- Сортировка: неактивные — ближайшие к открытию сверху; активные — по ярусу.

### 12.2 Три варианта карточек (все — на токенах index.css)

- **Вариант 1 · плотная сетка 3×N** — максимум карточек; имя в строку,
  полоса + % под ним; «скоро» — бейдж вместо полосы.
- **Вариант 2 · карточки 2 колонки** — крупнее: имя в 2 строки, статус-точка,
  полоса с %, чип «осталось +N» / «✓ готово».
- **Вариант 3 · горизонтальные строки** — иконка-плитка слева, имя + полоса
  в строку, % и «+N» справа; самый удобный на 320px.

Общее поведение (любой вариант): вкладка «Активные» с подсветкой, модалка
нити (что делает + два столпа 68/70 с прогрессом + «осталось повысить»).
Демо: 20 нитей (6 пар × ярусы I–IV): 6 горят, 6 спят, 8 скоро.

Артефакты: `design/niti/pult-V1..V3.png`, `pult-A.png`, `pult-M.png`
(wireframe в точных токенах), финиш `pultc-V1..V3.png`, `pultc-M.png`,
лист `pultc-sheet.png`, исходник `mockups-pult.html`.

### 12.3 Что заложить при реализации (любой выбранный вариант)

- Цвета столпов → токены `--pillar-*` в `tokens.ts`/`index.css` (синий/
  красный/изумрудный/фиолетовый).
- `SynergyDef` дополняется: `tier`, хелперы `readiness(synergy, pillars)`
  (min) и `missing(synergy, pillars)` (сумма дельт для «осталось»).
- Модалка — на `GModal`, карточки — примитивы `gameUI.tsx` + классы рядом с
  блоком `hero-gear2` в `index.css`.
- Приёмка: `pnpm typecheck` чистый, QA-снимки `scripts/qa/hero.mjs`
  (вкладка «Нити») на 390 и 1280.

## 13. Реализация («Пульт», сессия 23)

Владелец выбрал **Вариант 1 — плотную сетку 3×N**. Реализовано в
`HeroHubPage.tsx` (SynergiesModule + модалка) и `index.css` (классы
`.hero-thread-*`); старые `.hero-threads/.hero-thread[data-on]/.hero-req/`
`.hero-meter` удалены. После ревью владельца шапка и карточки переделаны
(финальный вид ниже).

- **Карточка**: крупная иконка-плитка 44px (главное), под ней имя (Cinzel,
  ellipsis) и тонкая полоса готовности с %. Активная — золотая рамка +
  свечение; неактивная — затемнена; «скоро» — запертое дерево + бейдж.
- **Отсеки «Активные / Неактивные»** — подвкладки. Активные по ярусу,
  неактивные ближайшими к открытию сверху (по суммарному дефициту очков).
- **Шапка**: «чернила по пергаменту» — «✦ Нити» + подзаголовок «связь между
  столпами» и 4 плитки столпов (иконка + число). Кольцо-прогресс и счётчики
  «горит/спит/скоро» убраны по ревью; статусы в модалке — «активна/неактивна».
- **Модалка** — информационная, без CTA: что делает (`effectRu`), какие столпы
  нужны (2 строки с прогрессом и `val/need`), «Осталось повысить: …» либо
  «Все столпы выполнены — нить активна».
- **«Скоро»**: 6 плейсхолдеров будущих ярусов, без порогов.
- **Палитра строго из index.css** (пергамент/тёмное дерево/каштан/золото/Cinzel).
- **Приёмка**: `pnpm typecheck` чистый, `pnpm test:pillars` 15/15.

## 14. Журнал решений

- 2026-09-04 (сессия 23): ревью владельца — шапка переделана на «чернила по
  пергаменту» (без кольца и счётчиков «горит/спит»), карточки стали
  «картинка-плитка 44px + подпись + тонкая полоса»; слова «активна/неактивна»
  вместо «горит/спит».
- 2026-09-04 (сессия 23): реализован «Пульт» V1 (см. §13) — вкладка «Нити»
  переведена на сетку 3×N с отсеками, шапкой-сводкой и информационной
  модалкой; typecheck и QA-ассерты зелёные.
- 2026-09-04 (сессия 22, раунд 1): анализ MMO + 4 «классических» мокапа —
  предъявлены.
- 2026-09-04 (сессия 22, раунд 1.5): 4 концепта-финиша в классическом стиле —
  владелец отклонил: «слишком старые».
- 2026-09-04 (сессия 22, раунд 2): новая современная визуальная система +
  4 варианта (Созвездие / Бенто / Лента / Радар), артефакты в `design/niti/`.
- 2026-09-04 (сессия 22, раунд 3): владелец выбрал A за основу с условиями
  «масштаб под много нитей» + «отличие от Тела». Предложены 4 доработки
  (Аккорд-небо / Небеса-страницы / Звёздная лента / Аккорд+лента) на демо из
  10 нитей; артефакты `design/niti/a2-*.png`, `concept3-*.png`.
  Рекомендация: A4. Ждём выбор владельца.
- 2026-09-04 (сессия 22, раунд 4): вердикт «не то, считай на 20 нитей» —
  смена парадигмы на информационную архитектуру (агрегаты, счётчики,
  фикс-высота, резерв). 4 варианта на демо из 20 нитей (6 пар × ярусы I–IV):
  Атлас / Пульт / Книга / Матрица. Артефакты `design/niti/r4-*.png`,
  `concept4-*.png`, `concepts4-sheet.png`, `mockups-r4.html`.
  Рекомендация: R4-A Атлас. Ждём выбор владельца.
- 2026-09-04 (сессия 22, раунд 5): финальный выбор — «Пульт» в стиле игры
  (палитра index.css, не стеклянный стиль раунда 2). Контракт + 3 варианта
  карточек (сетка 3×N / 2 колонки / строки) + общее поведение (вкладки,
  подсветка, модалка). Артефакты `design/niti/pult-*.png`, `pultc-*.png`.
  Ждём выбор варианта карточки.
