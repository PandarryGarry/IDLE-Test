# Сбор v2 — дизайн-документ

> **Статус: РЕАЛИЗОВАН** (обновлено 2026-09-10, сессия 31; перенесён из
> `artifacts/` в корень). Лежит в коде: 5 зон (`domain/professions/foraging.ts`,
> forest_clearing → secret_stash), опасность/мобы зон, оффлайн-база по самой
> частой находке, рейты — `data/balance/professions.ts`. Точные расхождения
> кода с текстом ниже фиксируем в `DEVLOG.md`. UI-доводка — на этапе
> UI/UX-унификации.
>
> Исторический заголовок: черновик на согласование → утверждён и исполнен
> (Сессия 23, PR #13/#15).
> Основной референс UI: **Вариант 1 — «Сетка зон»** (карточки локаций с таблицей дропа и опасностью), в нашей тёмной янтарной теме.
> Внешний вид дорабатывается после утверждения таблиц.
>
> РЕШЕНО (владелец, текущая итерация):
> 1. Зон — **5** (без «Зачарованной чащи» на первую версию).
> 2. Оффлайн — базовый предмет по самому частому в таблице. ✔
> 3. **Джекпот выпилен** (двух редких находок за цикл нет).
> 4. **Слабый моб доступен с 1-й зоны**, дальше мобы сильнее (лестница опасности).
> 5. Скорость «Сбора» **жёстко завязана на Темп** (характеристику героя), **кап 40 %**. Опыт гонится **через общие рейты игры** (см. §7 и §10).
> 6. Мобы для «Сбора» используем из **подготовленных картинок**: `mob_goblin`, `mob_wolf`, `mob_spider`, `mob_skeleton`, `mob_slime`, `mob_orc`.
> 7. **Остальные механики игры будут переписаны «под 0»** — их текущее состояние не является целевым. В Сборе не нужно «переживать» за совместимость с ними; правим баг общих рейтов в оффлайне глобально, потому что это конвейер, а не «чужая механика».
> 8. **Бонусы профессий делятся на 2 волны** — начальные (с первых уровней) и «мастерские» (ближе к максимуму профессии). Прокачка до максимума открывает все бонусы (стимул качать до 99).
> 9. **Экономика фиксированная.** Цены продажи не трогаем/не множим. Допускаем только **минимальную скидку на покупку** в будущей экономике — как отдельную стата профессии, чтобы не ломать баланс.
> 10. **Интуиция даёт маленький буст «Б и В»**: немного повышает качество/тир находок (лучше замечает ценное) и немного снижает шанс «пустого» цикла (внимательность). Оба — с жёсткими маленькими капами.

---

## 1. Принципы (жёсткие)

1. **Pages — только UI.** `src/pages/ForagingPage` (или `features/professions/*Page`) только читает состояние и рендерит. Никакой логики дропа/шансов/циклов в компоненте.
2. **Store — весь процесс.** Вся механика: старт/стоп, тики, броски таблиц, редкие находки, встречи, выдача лута/XP/мастерства — в `src/store/foragingStore`.
3. **Domain — только данные/правила.** `src/domain/professions/foraging` отдаёт константы и чистые функции расчёта (без состояния, без side-effect на банк).
4. **«Сбор» — только свои предметы.** Никаких брёвен/руд/слитков/рыбы из других профессий. Только дары природы + спец-находки для будущих систем.
5. **Хард-idle.** Все бонусы в % низкие, с жёсткими капами.

---

## 2. Архитектура

| Слой | Файл | Что внутри |
|---|---|---|
| UI | `src/features/professions/ForagingPage.tsx` | карточки зон, активная панель, лента находок. Только рендер. |
| Store | `src/store/foragingStore.ts` | `zoneId`, `progress`, `lastCycle`, `lootFeed`, `start/stop/tick`, бросок дропа, выдача предметов/XP, встреча → `combatStore`. |
| Data/rules | `src/domain/professions/foraging.ts` | `ZONES`, `lootTable`, `rareTable`, `danger`, формулы шансов, пороги. |

Схема цикла (в `foragingStore`, не в page):

```
tick(now)
  └─ если не активен → return
  └─ прогресс
  └─ цикл завершён?
       ├─ бросить обычный дроп (зона.lootTable) → предмет + min..max
       ├─ проверить x2 лут (Удача) → ×2 количество
       ├─ бросить редкий (зона.rareTable) → шанс rareFind
       ├─ записать в lootFeed (все предметы цикла одной строкой)
       ├─ выдать в банк + XP + мастерство
       └─ проверить встречу с мобом → если да, передать в combatStore
```

---

## 3. Зоны и прогрессия

Чем дальше зона, тем выше порог, больше XP/цикл, ценнее лут и выше опасность.
Грибы и редкие предметы закреплены за зонами — **не «глобальный пул»**, а свой список.

| # | Зона | Ур. | Цикл | XP | Мастер. | Опасность | Стиль |
|---|---|---|---:|---:|---:|---:|---|
| 1 | 🌿 Лесные заросли | 1 | 4 с | 18 | 3 | 1 % | слабый зверь |
| 2 | 🌲 Тёмная чаща | 12 | 5 с | 26 | 4 | 3 % | звери сильнее |
| 3 | 🕳️ Пещера | 25 | 6 с | 36 | 5 | 5 % | пауки |
| 4 | 🏛️ Руины | 40 | 7 с | 48 | 6 | 8 % | нежить |
| 5 | 🗝️ Тайник | 55 | 8 с | 62 | 7 | 12 % | страж + возможен босс |

> Зачарованная чаща (ур. 75) — отложена. Пороги уровней можно сдвинуть одной таблицей.

### Лестница мобов по зонам (слабый → сильный)

Мобы живут в `public/assets/icons/characters/mobs/` — подключаются через `iconUrl`.

| Зона | Мобы | Файл | Тип |
|---|---|---|---|
| 1 | Гоблин | `mob_goblin` | слабый |
| 2 | Гоблин / Волк | `mob_goblin`, `mob_wolf` | слабые–средние |
| 3 | Паук | `mob_spider` | средний |
| 4 | Скелет / нежить | `mob_skeleton`, `mob_orc` | сильные |
| 5 | Слайм-страж / Орк-босс | `mob_slime`, `mob_orc` (босс) | очень сильные, босс |

> Слайм — как «страж» на Тайнике (очень сильный). Гоблин с 1-го уровня — самый слабый.

---

## 4. Пороги по тирам грибов

Грибы уже имеют **свой тир редкости** (t1–t7). Доступность привязана и к зоне, и к уровню «Сбора», чтобы «дальше» = «лучше».

| Тир | Примеры | Открывается в зоне | Мин. ур. Сбора |
|---|---|---:|---:|
| t1 | Шампиньон, Опёнок | Лесные заросли | 1 |
| t2 | Лисичка, Подосиновик | Тёмная чаща | 12 |
| t3 | Зеленушка | Пещера | 25 |
| t4 | Белый гриб, Сморчок | Руины | 40 |
| t5 | Мухомор | Тайник | 55 |
| t6 | Светящийся гриб | Тайник | 60 |
| t7 | Трюфель | отложено (с «Зачарованной чащей») | 75+ |

---

## 5. Дроп-таблицы по зонам

> Колонка **%** — вес в таблице данной зоны (в сумме ≈ 100 %). За один обычный цикл выпадает **один предмет** из этой таблицы (если сработал x2 — ×2 количество). Редкая находка — отдельная таблица процентом ниже.
> `min–max` — количество за цикл.

### Зона 1 — Лесные заросли (ур. 1)
| Предмет | % | Кол-во |
|---|---:|---:|
| Камень | 22 | 1–2 |
| Ветка сухая | 16 | 1–2 |
| Ветка зелёная | 14 | 1–2 |
| Лист дубовый | 12 | 1–2 |
| Шишка сосновая | 10 | 1–2 |
| Шампиньон | 8 | 1 |
| Опёнок | 6 | 1 |
| Песок кварцевый | 6 | 1–2 |
| Верёвка растительная | 4 | 1–2 |
| Бечёвка жильная | 2 | 1 |

Редкие: — (на старте редких находок нет).
Опасность: 1 % → Гоблин (слабый).

### Зона 2 — Тёмная чаща (ур. 12)
| Предмет | % | Кол-во |
|---|---:|---:|
| Ветка хвойная | 14 | 1–3 |
| Шишка еловая | 12 | 1–3 |
| Лист берёзовый | 12 | 1–3 |
| Лисичка | 10 | 1 |
| Подосиновик | 8 | 1 |
| Дикие ягоды | 8 | 1–2 |
| Целебные травы | 6 | 1 |
| Верёвка растительная | 8 | 1–2 |
| Бечёвка жильная | 6 | 1–2 |
| Палка-дубинка | 8 | 1–2 |
| Палка с обмоткой | 8 | 1–2 |

Редкопул: Дикие ягоды (60 %), Целебные травы (40 %).
Опасность: 3 % → Гоблин (слабый), Волк.

### Зона 3 — Пещера (ур. 25)
| Предмет | % | Кол-во |
|---|---:|---:|
| Камень | 20 | 1–3 |
| Песок кварцевый | 14 | 1–3 |
| Глина | 10 | 1–2 |
| Смола | 8 | 1–2 |
| Зеленушка | 8 | 1 |
| Бечёвка жильная | 10 | 1–2 |
| Верёвка растительная | 8 | 1–2 |
| Палка-дубинка | 8 | 1–3 |
| Лист берёзовый | 8 | 1–3 |
| Целебные травы | 6 | 1 |

Редкопул: Глина (45 %), Смола (35 %), Целебные травы (20 %).
Опасность: 5 % → Паук (80 %), Огромный паук (16 %), сильный паук-босс (4 %).

### Зона 4 — Руины (ур. 40)
| Предмет | % | Кол-во |
|---|---:|---:|
| Камень | 16 | 1–3 |
| Ветка сухая | 10 | 1–3 |
| Смола | 10 | 1–2 |
| Целебные травы | 10 | 1–2 |
| Белый гриб | 8 | 1 |
| Сморчок | 8 | 1 |
| Зеленушка | 6 | 1 |
| Бечёвка жильная | 8 | 1–3 |
| Палочка оструганная | 8 | 1–3 |
| Светящаяся ягода | 4 | 1 |
| Корень-сердечник | 2 | 1 |

Редкопул: Светящаяся ягода (40 %), Корень-сердечник (35 %), Целебные травы (25 %).
Опасность: 8 % → Скелет (65 %), Мёртвый воин (28 %), редкий босс (7 %).

### Зона 5 — Тайник (ур. 55)
| Предмет | % | Кол-во |
|---|---:|---:|
| Смола | 14 | 1–3 |
| Глина | 12 | 1–3 |
| Мухомор | 10 | 1 |
| Сморчок | 8 | 1 |
| Белый гриб | 8 | 1 |
| Светящаяся ягода | 8 | 1 |
| Корень-сердечник | 6 | 1 |
| Светящийся гриб | 6 | 1 |
| Целебные травы | 10 | 1–3 |
| Бечёвка жильная | 10 | 1–3 |
| Верёвка | 8 | 1–3 |

Редкопул: Светящаяся ягода (35 %), Корень-сердечник (30 %), Светящийся гриб (25 %), Смола (10 %).
Опасность: 12 % → Мёртвый воин (60 %), Огненный элементаль (30 %), **босс Зелёный дракон** (10 %).

> «Зачарованная чаща» (ур. 75, трюфель) отложена на следующую итерацию.

---

## 6. Фундамент «Характеристики профессий» (для всех профессий)

> Отдельный, перспективный слой. Не хардкодим: используем существующие костяки проекта
> (`PROFESSION_FEEDS`, `computeAttributeSnapshot`, `adminConfigStore`) и расширяем их данными,
> а не зашиваем числа в UI/сторе.

### 6.1 Два вида «профессионных характеристик»

| Вид | Что это | Где живёт | Зачем |
|---|---|---|---|
| **Собственные характеристики профессии** | Статы, которые профессия даёт сама себе за прокачку («Следопыт», «Собиратель», «Мастер-находчивость» и т.д.), каждый даёт свой бонус | отдельный файл `professionStats` | смысл качать профессию: растут её внутренние бонусы |
| **Вклад профессии в персонажа** | Профессия кормит столп/ветвь героя (реализация `PROFESSION_FEEDS`) | `balance/professions.ts` + `characterAttributes.ts` | смысл качать профессию ради персонажа |

### 6.2 Архитектура (файлы)

```
src/domain/professions/professionStats.ts        ← данные: определения характеристик профессий (все профессии)
src/store/professionStatsStore.ts                ← РАСЧЁТ эффективных значений (default базы + админ-правки)
src/store/adminConfigStore.ts                    ← расширить: professionStatOverrides (+ professionFeedOverrides)
src/features/admin/AdminStatsPanel.tsx           ← новая вкладка/блок «Характеристики профессий»
src/domain/attributes/characterAttributes.ts     ← уже умеет professionLevels; оживить professionBonus по данным
```

### 6.3 Схема данных (предлагаемая)

```ts
type ProfessionStatKind = 'flat' | 'percent' | 'rating';
type ProfessionStatTier = 'early' | 'master'; // начальная / мастерская (ближе к 99)

interface ProfessionStatDef {
  id: string;                 // напр. 'forage_speed'
  skillId: SkillId;           // какая профессия
  nameRu: string;
  descriptionRu: string;
  kind: ProfessionStatKind;
  tier: ProfessionStatTier;   // волна открытия бонуса
  unlockLevel: number;        // уровень профессии, с которого стата активна
  base: number;               // значение на unlockLevel
  perLevel: number;           // прирост за уровень после unlockLevel
  cap?: number;               // жёсткий кап (percent) / асимптота (rating)
  k?: number;                 // для rating: рейтинг → % по асимптоте
  icon?: string;
}
```

### 6.3.1 Волны бонусов профессии

- **`early`** — открываются с первых уровней, дают ощутимый старт.
- **`master`** — открываются ближе к максимуму (напр. 70/80/90/99), дают «добить профессию» смысл.

### 6.3.2 Характеристики «Сбора» (предлагаемое наполнение)

| id | Название | tier | unlock | kind | base | perLevel | cap |
|---|---|---:|---|---:|---:|---:|---:|
| `forage_speed` | Скорость сбора | early | 1 | percent | 0 | 0.4 | 40 |
| `forage_double_loot` | Двойная находка | early | 1 | percent | 2 | 0.05 | 10 |
| `forage_rare_find` | Редкая находка | early | 5 | percent | 0.5 | 0.05 | 8 |
| `forage_shop_discount` | Скидка на покупку | master | 70 | percent | 0 | 0.05 | 3 |
| `forage_attention` | Внимательность | master | 80 | percent | 0 | 0.05 | 3 |
| `forage_full_profession` | Мастер-собиратель | master | 99 | flat | 0 | 1 | 1 |

- **Скидка на покупку** — минимальная, не ломает будущую экономику (кап 3 %).
- **Внимательность** — небольшой буст «Б и В»: лучше замечает ценное и меньше «пустых» циклов.
- **Мастер-собиратель** — финальная стата: добавляет один «бонусный» результат в конце цикла (включается на 99).
- `forage_sell_bonus` **удалён** — экономику фиксируем, цены продажи не трогаем.

> Эти характеристики — данные, редактируются в админке. Значения черновые — баланс уточним до кода.

### 6.4 Связь с персонажем

- `characterAttributes.computeAttributeSnapshot` уже принимает `professionLevels`.
- Оживляем `professionBonusStub`: берём `PROFESSION_FEEDS` + админ-`professionFeedOverrides` и считаем реальный вклад в столпы (в 5A — по умолчанию 0, чтобы ничего не сломать).
- Панель героя показывает «Ремесло → столп» (уже есть задел в `HeroHubPage`), плюс новые «Характеристики профессии» отдельным блоком.

### 6.5 Редактирование в админке (без кода)

- Добавляем в `adminConfigStore`:
  - `professionStatOverrides: Record<string, Partial<ProfessionStatDef>>`
  - `professionFeedOverrides: Record<string, Partial<ProfessionFeed>>`
- Экспорт/импорт JSON — как для предметов.
- **Новая вкладка «Профессии»** в админке — туда переносим всё, что связано с профессиями: рейты профессий (XP/мастерство/скорость/дроп), тумблеры доступности профессий, характеристики профессий. **Цену продажи НЕ включаем.**
- Внутри вкладки: список профессий → карточка каждой статы → поля название/tier/unlock/база/perLevel/cap → сброс к дефолту.
- Админка **не правит код**: меняет только значения, `.ts` остаётся эталоном дефолтов.

### 6.6 «Не хардкодь» — что НЕ делаем

- НЕ зашиваем числа/названия статов в `ForagingPage` или `foragingStore`.
- НЕ дублируем формулы в UI.
- НЕ создаём «свой» конфиг для Сбора — используем общий `adminConfigStore`.
- НЕ трогаем чужие механики вплоть до их полной переписи (зафиксировано выше).

## 7. Характеристики → бонусы Сбора (замкнутый смысл прокачки)

Профессия и персонаж кормят друг друга, чтобы было **ради чего качать и то, и другое** (хард-idle, бонусы небольшие).

| Бонус | Какая характеристика | Формула | Кап |
|---|---|---|---:|---:|
| Скорость «Сбора» | **Темп** (столп Сноровка, ветвь Темп) | база 100 % + 1 % за очко Темпа | максимум **+40 %** |
| Редкая находка | **Находчивость** (столп Чутьё, ветвь Ресурсность) | база 0.5 % + 0.05 %/ур. Сбора + 0.05 %/очко Находчивости | 8 % |
| Двойной лут | **Удача** (столп Чутьё, ветвь Удача) | база 2 % + 0.05 %/очко Удачи | 10 % |
| Внимательность | **Интуиция** (столп Чутьё, ветвь Интуиция) | маленький буст качества находок + меньше «пустых» циклов | маленький кап (черновик 3 %) |

- «Двойной лут» = **x2 количество** предмета за цикл (не «больше разных предметов»).
- **Джекпот (два редких за цикл) удалён** — не вводим.
- **Продажу находок убрали** — экономика фиксированная, цену продажи не множим.
- Профессия тоже кормит персонажа: `PROFESSION_FEEDS` добавляется запись
  `{ skillId: 'foraging', pillar: 'instinct', branch: 'resourcefulness', percentCapStub: 0 }`.
  В 5A бонус заглушечный (0 %), но **магистраль «профессия → персонаж» заводится сразу**,
  чтобы позже дать реальный % без переделки.

## 8. Опыт и общие рейты игры (обязательно)

Сбор **не выдумывает свою систему опыта** — он идёт через тот же конвейер, что все навыки:

1. `skillRegistry.getXpPerAction('foraging')` → базовый XP за цикл из зоны.
2. `gameStore.tick` берёт **общие рейты**: `xpMultiplier`, `masteryXpMultiplier`, `actionSpeedMultiplier`.
3. Итоговый интервал действия тоже считается через общий рейт скорости: `baseInterval / actionSpeedMultiplier` (минимум 100 мс).

Таким образом админ-рейты уже правят «Сбор» как весь остальной контент: поднял XP множитель — Сбор ускорился по опыту, поднял скорость — циклы короче. Никаких отдельных «тайных» множителей в Сборе.

> ⚠️ Контрольный момент при коде: оффлайн-расчёт тоже обязан применять рейты, а не игнорировать их.
> В текущем `offlineCalc` рейты **не применяются** — это общий баг всей игры. Для «Сбора» я не буду прятать это,
> и отдельно предложу поправить оффлайн-рейт для всех навыков (иначе «Сбор» и остальные профессии расходятся).

---

## 9. Встреча с мобом

### Онлайн
- Редкий шанс (см. `danger` зоны) → цикл завершается, но вместо следующего **Сбор ставится на паузу**, игрок переходит в бой (`combatStore`).
- В ленте находок появляется строка «⚔ Моб преградил путь → [имя]».
- После боя игрок **сам возвращается** на страницу Сбора (в будущем — красивый переход и возврат).
- Если бой отключён админкой — моб не встречается.

### Оффлайн (согласовано — вариант A)
- **Мобы не атакуют оффлайн.**
- Оффлайн считается только «чистый сбор»: XP + **базовый предмет** зоны (самый частый в таблице), 1 шт. за каждое действие.
- Редкие находки, x2 и мобы **не применяются** оффлайн — простая предсказуемая награда без наказаний и перекоса.
- Кап — 24 часа. Инвентарь заполнен — лишнее не начисляется.
- **Рейты применяются** — тот же конвейер, что и онлайн.

---

## 10. Что даёт прокачка (для инфо-панели)

- **1 ур.** — Лесные заросли + слабый зверь (очень редко). Активны `forage_speed` и `forage_double_loot`.
- **5 ур.** — Открывается `forage_rare_find` (редкая находка растёт с уровнем).
- **12 ур.** — Тёмная чаща + грибы t2 + спец-ягоды/травы.
- **25 ур.** — Пещера + глина/смола + гриб t3.
- **40 ур.** — Руины + т4 + светящаяся ягода/корень.
- **55 ур.** — Тайник + т5/т6 + сильные мобы, возможен босс.
- **70 ур.** — `forage_shop_discount` (минимальная скидка на покупку).
- **80 ур.** — `forage_attention` (внимательность: качество + меньше пустых циклов).
- **99 ур.** — `forage_full_profession` (Мастер-собиратель: финальный бонус).
- Каждый уровень: +0.05 % к шансу редкой находки (кап 8 %).
- Каждый очко **Темпа** даёт скорость «Сбора» (+40 % максимум) — ради этого и качается Сноровка.

---

## 11. Что в UI (Вариант 1 — «Сетка зон»)

### Карточка зоны
- Шапка: иконка локации + название + бейдж «Ур. N».
- **«Возможная добыча»**: до 4–6 иконок предметов, под каждой — **% шанс + кол-во**.
- **«Опасность»**: иконки мобов + **% встречи**; босс выделен золотой рамкой.
- Низ: XP за цикл / время / темп / кнопка «Начать».
- Закрытая зона — затемнена, с замком и требованием уровня.

### Активная панель (при сборе)
- Название зоны + прогресс-бар + таймер цикла + XP за сессию.
- **Живая лента находок**: каждый цикл отдельной строкой, все предметы цикла одной строкой (`+2 Ветка +1 Глина`), x2 отмечен бейджем.

### Мобильная версия
- Компактные карточки (иконка, уровень, мини-дроп-строка, «звёзды опасности»), липкая нижняя панель с текущим циклом.

---

## 12. Решённые вопросы

1. **Зон:** 5 на первую версию («Зачарованная чаща» отложена). ✔
2. **Оффлайн-базовый предмет:** самый частый из таблицы зоны. ✔
3. **Джекпот:** выпилен. ✔
4. **Мобы:** слабый моб с 1-й зоны, дальше сильнее; используются готовые иконки `mob_goblin/wolf/spider/skeleton/orc/slime`. ✔
5. **Скорость:** завязана на Темп (кап +40 %). ✔
6. **Характеристики профессий:** отдельный файл `professionStats` + связь с персонажем через `PROFESSION_FEEDS` + админ-редактирование (см. §6). ✔
7. **Рейты:** Сбор идёт через общие `xpMultiplier`, `masteryXpMultiplier`, `actionSpeedMultiplier`; оффлайн тоже (глобальный фикс оффлайн-рейта). ✔
8. **Остальные механики** — будут переписаны «под 0»; в Сборе не ориентируемся на их текущее состояние. ✔ зафиксировано.

## 13. Осталось уточнить перед кодом (фундамент характеристик)

1. **Характеристики Foraging** — приняты: `forage_speed`, `forage_double_loot`, `forage_rare_find` (early), `forage_shop_discount`, `forage_attention`, `forage_full_profession` (master). ✔
2. **Формат админки** — отдельная вкладка **«Профессии»**, переносим всё связанное с профессиями. Цену продажи туда **не** включаем. ✔
3. **Связь бонусов** — профессия **даёт** доп. характеристики персонажу (через `PROFESSION_FEEDS`); с каждым этапом профессии растут и дают больше. Готово к коду. ✔

> Осталась одна балансировочная правка: таблицы значений (`base`/`perLevel`/`cap`) — черновые; перед кодом сверим точные числа, чтобы хард-idle был на месяц+.
